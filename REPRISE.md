# Reprise — TEKKIShop

> Document factuel, sans récit. Objectif : qu'une session sans aucune mémoire des échanges puisse reprendre le travail depuis cet état, pas depuis un fil de conversation. Suivi en git depuis le 2026-08-10 (voir §1) — plus un fichier local uniquement, référencé depuis `AI_RULES.md` §0.1.
>
> Dernière mise à jour : 2026-08-17.

---

## État actuel — à lire en premier, 2026-08-17

**La Livraison 1 est en production.** Le merge `refonte-start` → `main` → `dev` a été exécuté et poussé le 2026-08-17 (détail complet en §21). Tout ce qui, dans les sections ci-dessous, est formulé au conditionnel ou au futur au sujet d'une mise en production (« pas encore en production », « au déploiement de la Livraison 1 », « une fois fusionné »...) décrit un état antérieur à cette date — à lire comme de l'historique, pas comme l'état courant. `www.tekki.shop` sert aujourd'hui la landing v6, `/start` v4, le modèle `free_orders`, la commission différenciée par pays, les frais de retrait facturés au marchand, et l'authentification séparée des country managers (`/cm/login`).

**§0 et §1 ci-dessous sont un instantané figé du 2026-08-12/13**, décrivant la préparation de la Livraison 1 avant qu'elle ne parte. Conservés tels quels comme trace de décision (pourquoi le périmètre a été gelé, quel ordre de réconciliation a été suivi) — mais **le plan en 7 étapes du §0 et l'« ordre de réconciliation » du §1 ont été exécutés dans les faits par un chemin différent de celui écrit** : `main` a reçu un hotfix direct (PR #4, `e2414cd`) hors de cette séquence, `dev` est resté figé pendant que `refonte-start` accumulait des semaines de travail supplémentaire (commission, pays, auth CM, etc. — voir §4.24 à §4.34), et le merge réel du 2026-08-17 est reparti de cet état-là, pas de celui du 12/13 août. Ne pas prendre le détail des étapes du §0/§1 comme une description du merge final — seul §21 décrit ce qui s'est réellement passé en dernier lieu.

---

## 0. Périmètre de la livraison 1 — figé le 2026-08-12

**Rien de nouveau n'y entre.** Elle ne contient que ce qui existe déjà et n'a jamais été vu en production, plus un correctif de performance :

1. Fusion de la PR #4 (`088`/`089`) dans `main`.
2. Relecture et commit du Lot 1 sur `refonte-start` (l'utilisateur regroupe), avec remise en place de `086`/`087` depuis `supabase/migrations-pending/` au moment du commit.
3. Commit des fichiers orphelins du chantier des montants : `090`, `091`, `scripts/check-order-totals-invariant.sql`, `scripts/check-stock-not-orphaned.sql`.
4. Correctif favicon, avec nouvelle mesure LCP immédiatement après (§4 point 2 ci-dessous) — c'est cette nouvelle mesure qui fait foi, pas celle de production actuelle.
5. Fusion de `main` dans `refonte-start`, puis de `refonte-start` dans `dev`.
6. Recette manuelle sur la préversion (par l'utilisateur).
7. `dev` vers `main` après validation.

**Explicitement hors périmètre, aucun de ces sujets ne doit s'ouvrir avant cette mise en production** — tous réels, tous tracés dans ce document : la refonte du tunnel de commande (livraison 2, voir §4 point 0), la mesure segmentée par motif de sortie d'essai, `BF`/`BK`, le `FCFA` en dur dans le tunnel, les pays au-delà des six couverts, Stripe Connect jamais validé en conditions réelles, l'unification du calcul de commission, la garde sur `resend-digital`, l'idempotence du webhook Bictorys, la restitution du stock à l'expiration/l'annulation.

**Contexte qui a motivé ce figement** : inventaire du 2026-08-12 sur `refonte-start` (landing v6, `/start` v4, modèle à 3 commandes, redesign boutique) — l'essentiel de ce qui semblait rester à construire existe déjà en code, sur une branche jamais mise en production. 1 541 boutiques existantes n'ont jamais vu ce travail.

**Étape 0 exécutée le 2026-08-12** : `.gitignore` corrigé (fusion propre, deux zones distinctes des changements de `main` et d'aujourd'hui), `AI_RULES.md`/`REPRISE.md`/`SPEC-refonte-tunnel-commande.md`/`SPEC-v2-refonte-boutiques-publiques.md` trackés — commit `3ffa5df`, non poussé. Sans ce commit, `git merge main` aurait refusé de continuer (fichiers non trackés qui auraient été écrasés).

**Étape 1 faite** : PR #4 fusionnée dans `main` (`e2414cd`, 2026-08-12) — `088`/`089` y sont désormais.

**Étape 3 faite, hors séquence, sur décision explicite de l'utilisateur** : `090`/`091`/les deux scripts de contrôle committés seuls (`9424e22`), avant le Lot 1.

**Étape 2 (Lot 1) faite le 2026-08-13**, relecture terminée fichier par fichier (`layout.tsx`, `produit/[id]/page.tsx`, `commande/[token]/page.tsx`, `ReviewForm.tsx`, `SettingsForm.tsx`, plus `products.ts`/`plan-features.ts` revus la veille) — commit `00c9590`, `086`/`087` remis en place depuis `supabase/migrations-pending/` au moment du commit comme prévu, `git status` et `npm run build` vérifiés propres après coup. `legal/cgu/page.tsx`/`legal/privacy/page.tsx` (modifications de l'utilisateur, hors lot) explicitement exclus du commit. Un commit séparé (`c5d37f5`, juste avant) retire la ligne SMS de `PricingV6.tsx` — hors sujet du Lot 1, isolé sur décision explicite plutôt que d'être mélangé au message du Lot 1.

`refonte-start` a 2 commits d'avance sur `origin/refonte-start` (`c5d37f5`, `00c9590`), pas encore poussés. **Prochaine étape : étape 5, fusion `main` → `refonte-start` → `dev`.**

**Favicon (étape 4) — fait, poussé.** Cible de 20 Kio abandonnée par l'utilisateur le 2026-08-12 comme mal posée : seul compte ce que le navigateur charge sur une page ordinaire, pas le poids sur disque de fichiers qui ne se chargent qu'à l'ajout à l'écran d'accueil. **Vérifié par requête réelle sur serveur local** (`npm run dev`, `curl` sur les URLs déclarées dans le HTML rendu, pas une supposition) : une page boutique déclare `favicon-16x16.png` (2,6 Kio) et `favicon-32x32.png` (4,1 Kio) — **4,1 Kio au maximum sur le chemin qui compte**, contre 834 Kio en production aujourd'hui. `apple-touch-icon-180x180.png` (40,6 Kio) et `icon-192x192.png` (45,3 Kio) confirmés présents dans le HTML mais jamais requis par une navigation normale. `theme_color` corrigé vers `#0252EA` dans `manifest.json` **et** dans le `viewport` de `layout.tsx` (même valeur obsolète trouvée aux deux endroits, corrigée aux deux). `src/app/icons/[size]/route.tsx` laissé intact. Commit `365e479`, poussé sur `origin/refonte-start`. **Nouvelle mesure LCP sur ABI&CO/Glow Eternel reste à faire** — seulement possible une fois ce commit sur une préversion réelle, pas en local.

**Plan de recette — étape 6, complété le 2026-08-12 :**
- **Préalable, avant tout déploiement** : captures de référence sur la production actuelle, boutique `legacy`, tableau de bord + boutique publique + fiche produit + tunnel, en 375 px et desktop. Sans elles, « strictement identique » n'est pas vérifiable après coup. **Non exécuté par l'agent** — pas d'outil de capture d'écran/navigateur disponible dans cet environnement ; à faire par l'utilisateur ou dans une session outillée pour ça.
- Boutique `legacy` inchangée (tableau de bord, boutique publique, tunnel) — voir vérification composant par composant ci-dessous, §1.
- Landing, `/start` (2 segments, 1 pays couvert + 1 non couvert), premier produit publié, 3 commandes offertes + alerte 2ᵉ + fermeture 3ᵉ, commande retenue (coordonnées masquées), fin d'essai Cas A et Cas B, activation, commande avec code promo + remise quantité, `/onboarding` encore fonctionnel, plafond produits Découverte.
- **Ajouté le 2026-08-12** : reprise de paiement — passer une commande en ligne, abandonner au moment de payer, revenir sur `/commander/pay?order_id=…&token=…`, vérifier qu'on peut encore payer. C'est le chemin qui porte le taux d'aboutissement de 12 % mesuré le 2026-08-11 — état à connaître avant la livraison 2 (refonte tunnel).
- **Ajouté le 2026-08-12** : boutique avec paiement en ligne coupé ou paiement à la livraison coupé — le tunnel doit rester cohérent (une seule méthode restante affichée comme une phrase, pas un choix à un seul bouton — §7 de `SPEC-refonte-tunnel-commande.md`, déjà la règle cible, à vérifier sur le comportement actuel avant refonte).
- **Ajouté le 2026-08-13, suite au correctif `54aacf9` (MAX_HELD_ORDERS 3→1, comptage aligné, contact WhatsApp)** :
  - Boutique `free_orders` `expired`, une commande physique retenue : le bouton de commande doit être fermé dès cette première commande (seuil à 1, plus à la troisième).
  - Même boutique, mais avec uniquement des commandes retenues **digitales** (aucune physique) : le bouton doit rester **actif**, quel que soit leur nombre — c'est l'invariant vérifié sur scénarios construits en session, jamais sur une commande réelle faute de donnée en base ; premier vrai test possible seulement une fois une boutique `free_orders` réelle en production.
  - Écran de succès d'une commande retenue : bouton WhatsApp visible et préempli (référence + montant, sans promesse ni relance) quand `phone_whatsapp` est renseigné ; absent proprement (pas de bloc vide) sinon ; le bouton générique « Contacter la boutique » plus bas ne doit pas apparaître en double pour ce cas.
  - Ne pas interpréter l'absence de SMS pendant la recette comme une régression : le canal Lafricamobile est confirmé non délivré (§15/§16), ticket ouvert côté fournisseur, indépendant de ce qui part en production aujourd'hui.
  - Vérifier que `PricingV6` (déployée avec cette livraison) n'affiche plus « Notifications SMS automatiques » nulle part (retiré dans `c5d37f5`).

---

## 1. État technique

**Appliqué en base (Supabase, projet lié), vérifié :**
- `088_lockdown_activate_free_orders_shop.sql` — verrouille `activate_free_orders_shop` (`EXECUTE` retiré à `anon`/`authenticated`, restauré à `service_role` seul).
- `089_lockdown_remaining_functions.sql` — verrouille les 17 fonctions restantes du schéma `public` (hors `088` et hors les 3 `get_my_*` intouchées).

**Git :**
- Branche `hotfix/lockdown-activate-free-orders-shop` : `088` + `089` + `scripts/check-function-privileges.sql`, poussée sur origin.
- ~~**PR #4 ouverte, non fusionnée**~~ : https://github.com/ibukamicrosaas/ministore/pull/4 — base `main`, autonome (pas de bundle avec la refonte boutique). Fusion prévue par l'utilisateur lui-même après relecture, pas automatique. **Incohérence interne à ce §1, repérée en relecture à froid le 2026-08-17 : cette ligne date d'avant la fusion de la PR, jamais mise à jour après — le §0 (ligne 27) documente correctement sa fusion le 2026-08-12 (`e2414cd`). Les deux se contredisaient déjà avant ce soir ; ligne conservée barrée plutôt que supprimée, pour ne pas effacer la trace de l'état réel au moment où §1 a été écrit.**
- **Politique `.gitignore` sur les `.md` inversée sur `main`, en deux commits** : les `.md` sont désormais suivis par défaut (l'ancien blanket `*.md` faisait disparaître chaque nouvelle spec sans qu'on le remarque — trois fichiers sauvés de justesse le 9-10 août). Suivis en conséquence : `AI_RULES.md`, `REPRISE.md`, `SPEC-refonte-tunnel-commande.md`, `SPEC-v2-refonte-boutiques-publiques.md`, `NOTE-cadrage-refonte-boutiques.md`, `BRIEF-REFONTE-LANDING-TEKKISHOP.md`, `ADDITIF-argent-commandes-retenues.md` (récupéré depuis `~/Downloads`, cité par son nom dans `route.ts`), `SPEC-3-commandes-offertes.md`, `NOTE_MASQUAGE_COMMANDES.md`, `NOTE_DUPLICATION_PAYOUT_BICTORYS.md`, `NOTE_ERREURS_SUPABASE_NON_VERIFIEES.md`, `ARCHITECTURE.md`, `PRD.md`, `SECURITY.md`, `SCALING.md`, `bictorys-api-integration.md`, `lafricamobile-integration.md`, `DIGITAL_PRODUCTS_SPEC.md`, `chatbot-tekkishop.md`, `README.md`, plus les maquettes `tekkishop-boutique-accueil.html`, `tekkishop-boutique-produit.html`, `tekkishop-landing-v6.html`. Historique clos déplacé (suivi quand même — un document clos garde sa valeur) dans `docs/archive/` : `BACKLOG.md`, `NEXT_STEPS.md`, `ONBOARDING_SPEC.md`, `PRODUCT.md`, `TECH.md`, `analyse-landing.md`, `VENTE_LISTING.md`, `MIGRATION_REPAIR_PLAN.md`. Non tranchés, laissés visibles mais non suivis : `AGENTS.md`, `CLAUDE.md` (non mentionnés, pas décidés seul), `IMPROVEMENTS.md` (voir §8).
- `refonte-start` : plafond committé réel = `085` (vérifié par diff, pas supposé). `086_product_variants.sql`/`087_shops_verification.sql` (migrations du Lot 1) existent en fichiers mais ne sont pas commitées.
- **Tout le Lot 1 (redesign boutique) est non commité sur `refonte-start`, mêlé aux modifications de CGU/politique de confidentialité de l'utilisateur dans le même arbre de travail.** Fichiers Lot 1 : les deux migrations ci-dessus, `src/lib/plan-features.ts`, `src/lib/actions/product-limit.ts` (nouveaux), `SettingsForm.tsx`, `ReviewForm.tsx`, `[shop-slug]/layout.tsx` (pose de `--brand`, retrait de la surcharge `--color-primary`), `[shop-slug]/produit/[id]/page.tsx`, `[shop-slug]/commande/[token]/page.tsx`, `import-csv/route.ts`, `onboarding.ts`, `products.ts`.

**Renommage `--brand` revérifié en profondeur le 2026-08-12, sur `src/` entier, pas seulement `[shop-slug]/`.** Zéro occurrence de `--color-primary` dans `[shop-slug]/` ni dans un composant partagé qu'il consomme — confirmé, le renommage n'a rien oublié. Les seules occurrences restantes de `--color-primary` dans `src/` sont légitimes : `globals.css` (design system propre à TEKKIShop), `/aide`, `/admin`, et les écrans de `dashboard/` (interface marchand elle-même, jamais vue par un client) — aucune n'est rendue côté boutique publique.
**Découverte séparée, pas un risque du renommage** : `[shop-slug]/page.tsx:71` (page d'accueil boutique) et `src/components/pwa/ShopBusinessLayout.tsx:65,83` **recalculent `shop.primary_color ?? '#0EA5E9'` localement**, en dehors de `--brand`, exactement le pattern proscrit par `AI_RULES.md` (« couleur du marchand via `var(--brand)`, jamais un recalcul local »). Sans risque immédiat — même valeur de repli que `--brand`, donc aucune boutique ne « repasse au bleu TEKKIShop » aujourd'hui — mais ces deux fichiers divergeraient silencieusement d'un futur changement sur `--brand` (la règle de contraste automatique de `SPEC-v2-refonte-boutiques-publiques.md` §7, par exemple). Pré-existant, pas introduit par le Lot 1, non traité ici — signalé pour ne pas le perdre. CGU/privacy : `src/app/legal/cgu/page.tsx`, `src/app/legal/privacy/page.tsx` — jamais à modifier par un agent, seulement à afficher pour relecture.

**Troisième occurrence du même anti-pattern, trouvée et corrigée le 2026-08-17 (lot A de la refonte du tunnel de commande, voir §26) : `commander/page.tsx` recalculait aussi `shop.primary_color ?? '#0EA5E9'` avant de le passer en prop à `OrderForm`.** Corrigé — la prop `primaryColor` vaut désormais la chaîne littérale `'var(--brand)'`, consommant la valeur déjà posée par `[shop-slug]/layout.tsx:116`, sans nouveau recalcul.

**Quatrième occurrence, trouvée et corrigée le 2026-08-18 (lot B, §27) : `commander/success/page.tsx` faisait le même recalcul**, y compris un cas particulier — `` `${color}20` `` (astuce alpha hexadécimale sur un `#RRGGBB`) — incompatible avec une chaîne `var(--brand)` et remplacé par `color-mix(in srgb, var(--brand) 12%, white)`. **Restent les deux occurrences d'origine ci-dessus, toujours en attente, toujours rattachées aux Lots 2-5 boutiques publiques (§4 point 5) — ni celle-ci ni la troisième n'en font partie.**

**Ce « Lot 1 » et le « Lot 1 — Fondations » de `SPEC-v2-refonte-boutiques-publiques.md` §14 sont le même chantier, décrit deux fois sous deux angles** : ici par ses fichiers modifiés, dans la spec par sa fonction (table `product_variants`, matrice de plan réelle, `--brand` unifié, colonnes de vérification). Les deux se recoupent effectivement : migrations `086`/`087`, `--brand` posé dans `[shop-slug]/layout.tsx`, `plan-features.ts` créé. Noté ici pour ne pas rouvrir la question à chaque session.

**Désynchronisme découvert le 2026-08-11 entre l'historique de migration distant et le contenu réel de la base — ne pas relancer `supabase db push --include-all` avant réparation.** `npx supabase migration list --linked` montre les migrations `062` à `083` (22 fichiers, y compris `071` et `081`) présentes localement mais **absentes de l'historique distant**, alors que `033`-`061` et `084`-`085` sont présentes des deux côtés. Une tentative de `db push --include-all` a échoué sur `062` (`CREATE POLICY` déjà existante, SQLSTATE 42710) après une série de `already exists, skipping` — preuve que le contenu de `062` est déjà appliqué en base, seul le suivi ne le sait pas. **Vérifié objet par objet le 2026-08-11, sans Docker, via `supabase db query --linked` (accès direct, en lecture, à `information_schema`/`pg_catalog`)** : les 39 objets structurels attendus des 22 migrations (tables, colonnes, index, policies, triggers, fonctions, contraintes) sont **tous présents**, y compris les versions finales des objets réécrits plusieurs fois dans la séquence (`activate_free_orders_shop` retourne bien 3 colonnes comme `083`, pas 2 comme `074`/`077` ; `handle_free_order_quota` contient bien le garde `is_active=true` de `078` ; `shops_public_read` correspond exactement à la définition de `078`, pas à celle de `069` ; `orders_status_check` autorise bien `'completed'` comme posé par `082` ; `payouts_payout_method_check` inclut bien `mobicash`/`maxit` comme posé par `064`). Le commentaire de `081` et l'effet de données de `071` sont également conformes. **Un seul policy sur `country_manager_payouts`** (pas de doublon) — confirme que la tentative de push échouée n'a rien laissé à moitié appliqué. **Réparé le 2026-08-11** : `supabase migration repair --status applied 062...083 --linked`, exécuté après la vérification objet par objet ci-dessus. `supabase migration list --linked` confirme `001` à `085` alignés Local=Remote de bout en bout.

**`088`/`089` réparées le 2026-08-12, même méthode.** Ces deux fichiers n'existent que sur la branche `hotfix/lockdown-activate-free-orders-shop` (jamais sur `main` ni `refonte-start`) — appliquées en base directement lors de la session précédente, jamais enregistrées dans l'historique. Vérifié indépendamment avant réparation (pas fait confiance à la note « Appliqué en base, vérifié » de ce même document) : les 18 fonctions ciblées testées une à une avec `has_function_privilege` — `anon`/`authenticated` bloquées sur les 18 sans exception (la propriété de sécurité tient). `service_role` exécute 7 fonctions sans `GRANT` explicite dans `089` — expliqué au §2 (`pg_default_acl`, bénin). **Obstacle technique rencontré et sa solution, à réutiliser si le cas se reproduit** : `supabase migration repair` exige que le fichier de migration existe localement (glob), même si l'opération elle-même ne touche que la table de suivi. Contourné en matérialisant temporairement `088_*.sql`/`089_*.sql` depuis `hotfix/…` via `git show hotfix/...:chemin > fichier` (jamais `git checkout`, jamais `git add` — fichiers non trackés, working tree de `refonte-start` non touché autrement), réparation lancée, fichiers supprimés immédiatement après. `git status` vérifié propre avant et après.

`supabase migration list --linked` confirme désormais `001` à `085` (Local=Remote) et `088`/`089` (Remote seul, normal — pas de fichier local sur cette branche). Restent en écart, attendus : `086`/`087` (Lot 1 non commité), `090`/`091` (écrites, en attente d'application — voir ci-dessous).
- Requête de préservation des badges "Pro" : **écrite, pas seulement décrite** — commentée dans `supabase/migrations/087_shops_verification.sql:25-26` (`UPDATE shops SET verification_status = 'verified', verified_at = NULL WHERE plan = 'pro'`). Ni décommentée ni appliquée. Correspond à SPEC-v2 §8 ("Point commercial, pas technique").

**Ordre de réconciliation avec `main` — à respecter strictement, ne pas inverser :**
1. Relecture du diff du Lot 1 par l'utilisateur (en cours).
2. Commit du Lot 1 sur `refonte-start` (l'utilisateur indique lesquels regrouper — ne pas décider seul). **Prérequis explicite ajouté le 2026-08-12 : avant ce commit, remettre `086_product_variants.sql` et `087_shops_verification.sql` de `supabase/migrations-pending/` vers `supabase/migrations/`.** C'est à ce moment précis que le risque d'oubli se referme — voir note ci-dessous.
3. Seulement ensuite : fusionner `main` (qui contiendra `088`+`089` une fois la PR #4 fusionnée) dans `refonte-start`.

Ne jamais fusionner `main` dans `refonte-start` tant que l'arbre de travail contient encore le Lot 1 et les modifications CGU non commitées — fusionner dans un arbre sale mélangerait ce que la session a explicitement pris soin de séparer.

**`086`/`087` déplacés vers `supabase/migrations-pending/` le 2026-08-12 — provisoire, pas une solution.** Tant qu'ils restaient dans `supabase/migrations/` sans être prêts, aucun `db push` n'était sûr sur cette branche (règle écrite dans `AI_RULES.md`, section « Application des migrations »). Le déplacement ferme ce risque immédiatement, mais **la vraie sortie est le commit du Lot 1 (étape 2 ci-dessus)** : une fois commités, ces fichiers ne partent en production que le jour de la fusion de `refonte-start`, et redeviennent visibles pour l'outillage au moment normal. Le déplacement est un pansement en attendant la relecture, à annuler — remettre les fichiers dans `supabase/migrations/` — au moment du commit du Lot 1, pas avant, pas oublié après.

---

## 2. La découverte plateforme

**`ALTER DEFAULT PRIVILEGES ... REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC` ne protège pas les fonctions futures sur cette base Supabase.** Un mécanisme de plateforme — invisible dans `pg_proc`, `pg_event_trigger` et `pg_default_acl` — ajoute `EXECUTE` à `PUBLIC` de façon inconditionnelle à la création d'une fonction dans `public`, quel que soit l'état des privilèges par défaut.

**Méthode d'élimination** (voir commentaire complet dans `supabase/migrations/089_lockdown_remaining_functions.sql`, au-dessus de la clause `ALTER DEFAULT PRIVILEGES`) :
1. `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC, anon, authenticated` appliqué et vérifié par `SELECT` direct sur `pg_default_acl` — la ligne ne contenait effectivement plus `PUBLIC`/`anon`/`authenticated`.
2. Une fonction créée juste après, dans la même transaction, obtenait quand même `EXECUTE` pour `PUBLIC` (`proacl` le montrait explicitement).
3. Les 7 event triggers de la base lus un par un pour écarter cette piste : `rls_auto_enable` ne cible que les `CREATE TABLE` ; `grant_pg_cron_access`, `grant_pg_net_access`, `grant_pg_graphql_access` ne se déclenchent que sur `CREATE EXTENSION` ; `pgrst_ddl_watch` se contente d'un `NOTIFY`. Aucun n'accorde `EXECUTE` sur une fonction.
4. Conclusion : le mécanisme responsable n'est identifiable par aucune requête SQL disponible — probablement un hook de plateforme (type `supautils`), hors de portée de ce dépôt.

**Décision prise en conséquence :** pas d'event trigger maison pour contourner ce mécanisme — tant qu'il reste une boîte noire, un trigger pourrait s'exécuter avant une éventuelle réinjection et donner un faux sentiment de protection sans le signaler. La protection fiable est le `REVOKE` explicite posé dans la même migration que chaque `CREATE FUNCTION` (règle dans `AI_RULES.md` §3), avec `scripts/check-function-privileges.sql` comme filet rejouable à la demande — il détecte l'anomalie quelle qu'en soit la cause, sans dépendre de comprendre le mécanisme.

**Reste à faire, hors ingénierie** : demander au support Supabase quel composant ajoute `EXECUTE` à `PUBLIC` sur les fonctions de `public`, et s'il est désactivable.

**Pendant bénin de cette injection, trouvé le 2026-08-12 en vérifiant indépendamment 088/089** : `pg_default_acl` contient une ligne `role=postgres, schema=public, objtype='f' (function)` dont l'ACL inclut `service_role` — un privilège par défaut préexistant qui accorde automatiquement `EXECUTE` à `service_role` sur toute nouvelle fonction créée par `postgres` dans `public`, indépendamment de tout `GRANT` explicite écrit dans une migration. Constaté sur 7 fonctions de `089` (`cleanup_old_login_attempts`, `cleanup_pin_resets`, `increment_promo_used_count`, `handle_new_user`, `update_updated_at`, `rls_auto_enable`, `handle_free_order_quota`) : la migration ne leur écrit aucun `GRANT` explicite, `service_role` peut pourtant toutes les exécuter — pas une anomalie, cette ligne de `pg_default_acl` l'explique entièrement. Sans risque : `service_role` est déjà la clé de confiance maximale du projet, jamais exposée côté client. À la différence de l'injection `PUBLIC` ci-dessus, celle-ci est visible et expliquée dans `pg_default_acl` — pas une boîte noire.

---

## 3. L'enquête de ce soir — décomposition des montants de commande

Déclenchée par un constat d'affichage sur une commande de test (§7) : le récapitulatif de suivi de commande n'affiche ni sous-total ni remise, seulement le total. Investigation menée jusqu'à la portée réelle du problème avant d'écrire une migration.

**Constat central : la décomposition d'une commande n'est pas persistée, seul le total final l'est.**
- `order_items.line_total` intègre déjà la remise sur quantité (le prix unitaire est réduit avant insertion) — pas de perte là.
- `orders.delivery_price` est persisté correctement.
- La remise promo, elle, est calculée en mémoire dans `src/app/api/orders/route.ts` puis jetée : ni le code promo, ni son pourcentage, ni le montant de remise ne sont écrits sur `orders` ou `order_items`.
- Le taux d'acompte (`deposit_percentage`) subit le même sort : seul le montant final (`orders.deposit_amount`) est conservé, jamais le taux qui l'a produit.
- `reserve_promo_code`/`release_promo_code` (`supabase/migrations/045_security_promo_atomic.sql`) ne touchent que `promo_codes.used_count` — aucun lien order↔code n'existe nulle part, même indirectement.

**Deux agrégats de chiffre d'affaires par produit surestiment le CA sur les commandes remisées** (le CA global, lui, est fiable partout — vérifié poste par poste) :
- `src/app/dashboard/rapports/page.tsx:156-165` (« Top produits du mois »)
- `src/lib/ai/tools.ts:223-236` (`get_products`, assistant IA marchand)

Les deux somment `order_items.line_total` au lieu d'imputer la remise par ligne. **Le schéma validé ci-dessous ne corrige pas ces deux endroits** — la remise est stockée au niveau de la commande, pas répartie par ligne. Point ouvert, non tranché, à trancher en tout début de prochaine session :
- soit répartir la remise au prorata sur les lignes à la création de la commande (colonne supplémentaire sur `order_items`, arrondi à gérer pour que la somme des parts retombe sur le total) ;
- soit renommer honnêtement l'indicateur en chiffre d'affaires brut par produit dans les deux endroits, et l'assumer comme tel.

**Ampleur mesurée, base de production, 514 commandes au total :**
- Invariant vérifié : `somme(order_items.line_total) + orders.delivery_price − orders.total_price` — **14 commandes** à écart non nul, **aucune** à écart négatif ou inexpliqué (l'invariant tient à 100 %, aucune composante inconnue).
- Montant total concerné : **54 325 FCFA**, réparti sur des commandes du 22 juin au 9 août 2026 (dont les deux commandes de test de ce soir).
- **Écart non résolu, à signaler tel quel** : `promo_codes.used_count` ne totalise que **7** sur l'ensemble des codes (4 codes à usage non nul), contre 14 commandes montrant une remise effective. Cause non vérifiable depuis cet environnement (lignes `promo_codes` potentiellement supprimées/recréées — motif plausible vu la répétition de paniers identiques dans les 14, mais c'est une inférence, pas une preuve). **Le nombre qui fait foi pour l'ampleur historique est 14 (invariant), pas 7 (`used_count`).**

**Schéma validé, à écrire dans une migration au tout début de la prochaine session :**
- `orders.promo_code text NULL`, `orders.promo_discount_pct integer NULL`, `orders.discount_amount integer NOT NULL DEFAULT 0`, `orders.deposit_percentage integer NULL`.
- `order_items.quantity_discount_pct integer NULL`.
- `line_total` et `unit_price` ne changent pas de sens — du code les lit déjà, ne pas y toucher.
- Backfill historique : `discount_amount` est dérivable pour les 514 commandes existantes (via l'invariant ci-dessus) — seul champ rétro-rempli. `promo_code`, `promo_discount_pct`, `deposit_percentage`, `quantity_discount_pct` restent `NULL` pour tout ce qui précède la migration : définitivement perdus, jamais devinés.
- Affichage : une ligne « Réduction » apparaît dès que `discount_amount > 0` (donc aussi pour les 14 commandes historiques, avec le bon montant) ; jamais de code/pourcentage affiché si `promo_code IS NULL`. Jamais de « 0 % » ou « aucun code » là où la donnée est simplement absente.
- Vérification auto-portante : pas de `CHECK` (Postgres interdit les sous-requêtes inter-tables dedans), pas de trigger vivant sur le chemin de paiement — un script rejouable `scripts/check-order-totals-invariant.sql`, même principe que `scripts/check-function-privileges.sql` pour `089`.

**Séquencement validé** : migration séparée, **avant** le sous-lot 2a (voir §4) — les deux touchent `order_items`, mais pas les mêmes colonnes ; celle-ci d'abord évite que 2a doive fusionner son diff avec un calcul de totaux encore en mouvement.

---

## 4. Travaux dus, dans cet ordre

0. **Refonte du tunnel de commande — confirmée comme livraison 2, priorité immédiatement après la livraison 1, avant tout autre point ouvert de cette liste.** Décidé le 2026-08-12 : c'est elle qui porte le taux d'aboutissement des paiements en ligne actuellement mesuré à 12 % (voir l'enquête « Paiements en ligne jamais aboutis », §6). Ne la fait pas attendre derrière les autres sujets hors périmètre de la livraison 1 — elle n'est elle-même pas dans ce périmètre (rien de la refonte tunnel n'entre en livraison 1), mais elle passe devant `BF`/`BK`, la commission, `resend-digital`, l'idempotence webhook et la restitution de stock dès que la livraison 1 est en production. Spec : `SPEC-refonte-tunnel-commande.md`, 4 lots A-D, aucun commencé.
1. **Migration de la décomposition des montants** (§3 ci-dessus) — schéma validé, prête à écrire.
2. **Favicon — plan détaillé le 2026-08-12, prêt à exécuter.**
   **État vérifié** : `public/favicon.png` (269 Kio, 500×500) **est déjà tracké par git** — la note précédente (« modifié localement jamais poussé ») est obsolète, il partira avec le prochain déploiement. `src/app/layout.tsx:86-87` le référence déjà (`icon`/`apple`), une seule taille, pas de jeu responsive. `public/old-favicon.png` (853 Kio, 1254×1254, non suivi) correspond exactement au fichier actuellement servi en production (834,4 Kio mesurés) — **c'est la source d'avant remplacement, pas un fichier sans rapport** : `old-icone-tekkishop.svg`/`old-logo.svg`/`old-logo_white.svg` (non suivis eux aussi) sont vraisemblablement du même lot, à confirmer avant de les supprimer.
   `public/manifest.json` : `theme_color: "#0EA5E9"` — confirmé obsolète, ne correspond pas au bleu de marque actuel `#0252EA` (`AI_RULES.md` §1). Son tableau `icons` référence déjà `/favicon.png` en 192×192 (déclaré) et `/icons/icon.svg`.
   `src/app/icons/[size]/route.tsx` **existe mais n'est référencé nulle part ailleurs dans le code** (vérifié par recherche exhaustive) — mort ou orphelin, à laisser tel quel pour l'instant (pas de nettoyage en marge de cette tâche), pas à supprimer sans décision séparée.
   **Étapes** : (1) produire un jeu 16/32/180/192px depuis la source, chacun sous la cible < 20 Kio — `sips` (déjà disponible sur cette machine) suffit, pas d'outil externe nécessaire ; (2) mettre à jour `layout.tsx` pour référencer les tailles au lieu d'un seul fichier ; (3) corriger `theme_color` vers `#0252EA` dans `manifest.json` ; (4) laisser `/icons/[size]/route.tsx` en l'état. **Nouvelle mesure de référence LCP/poids immédiatement après, sur ABI&CO et Glow Eternel** (mêmes boutiques que la mesure initiale, §7) — c'est elle qui fera foi pour le §10.1 de `AI_RULES.md`, pas les chiffres actuels.
3. **Unification du calcul de commission — sens corrigé le 2026-08-10 par l'utilisateur, à ne pas réinverser.** **Le taux plat de 3 % est correct et voulu, il ne dépend jamais du plan.** Le circuit (§6) : l'argent transite par le compte Bictorys de la plateforme, qui paie 1,5 % à Bictorys sur l'encaissement et 1 à 1,5 % à l'opérateur au retrait — le 3 % couvre ces frais réels et est dû sur tout retrait qui passe par la plateforme, quel que soit le plan. Le 0 % n'est pas un avantage accordé par plan : c'est ce qui se produit mécaniquement quand une boutique a configuré ses propres clés Bictorys (`shops.bictorys_secret_key IS NOT NULL`) — ses clients la paient en direct, l'argent ne transite jamais par la plateforme, donc aucun retrait n'est jamais demandé via ce circuit. **La condition doit donc porter sur `shops.bictorys_secret_key`, jamais sur `shops.plan`** — même si en pratique une boutique à clés propres ne passe jamais par ce chemin aujourd'hui, la règle doit rester vraie si le flux change. `COMMISSION_RATES` (plan-aware) est donc l'anomalie à supprimer, pas le taux plat.
   **9 endroits calculent ce taux, aucun ne partage de code.** Classés selon la règle corrigée :

   | # | Fichier:ligne | Comportement actuel | Conforme à la règle ? |
   |---|---|---|---|
   | 1 | `src/lib/actions/payouts.ts:21-25,94` (`COMMISSION_RATES`) | 0 % si `plan==='pro'` | **Non — priorité, c'est lui qui calcule le montant réellement transféré côté cron/admin** |
   | 2 | `src/lib/ai/tools.ts:4,310` | ternaire dupliqué, 0 % si `plan==='pro'` | **Non — priorité absolue : c'est ce que l'assistant IA affirme au marchand.** Aujourd'hui il annonce 0 % à un Pro qui paiera 3 % — une affirmation fausse faite à un client, pas juste un calcul interne |
   | 3 | `src/app/api/payouts/request/route.ts:83-84` | flat 3 %, aucune branche | Oui dans son résultat actuel (aucune boutique à clés propres ne passe par cette route) — à migrer vers la fonction partagée pour rester vrai si le flux change |
   | 4 | `src/app/dashboard/revenues/page.tsx:83-85,212,227-228` | flat 3 % | Oui, idem — à migrer |
   | 5 | `src/app/dashboard/orders/page.tsx:110` | flat 3 % | Oui, idem — à migrer |
   | 6 | `src/app/dashboard/orders/[id]/page.tsx:144` | flat 3 % | Oui, idem — à migrer |
   | 7 | `src/lib/billing/shop-status.ts:53` | flat 3 % | Oui, idem — à migrer |
   | 8 | `src/app/admin/finances/page.tsx:147-153` | constante locale redéclarée `=3` | Oui, idem — à migrer, agrège tous plans confondus donc pas de risque de branche fausse |
   | 9 | `src/app/api/admin/payouts/manual/route.ts:52,55` | constante locale redéclarée `=3`, masque l'import | Oui, idem — à migrer |

   Correctif à concevoir : une seule fonction partagée, taux à 0 % uniquement si `shops.bictorys_secret_key IS NOT NULL`, jamais dérivé de `plan`. `COMMISSION_RATES` disparaît. Traiter #1 et #2 en premier (seuls calculs faux aujourd'hui) ; #2 avant #1 puisque c'est une affirmation directe à un marchand, pas un calcul interne.
4. **Sous-lot 2a** — bascule des variantes vers la table relationnelle : lecture/écriture schéma→table, `POST /api/orders`, nouvelles RPC `decrement_variant_stock_v2`/`increment_variant_stock_v2`. Plan avant code, comme d'habitude. **`REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated` explicite dans la même migration qui crée ces deux RPC** — règle `AI_RULES.md` §3, issue de la découverte du §2 ci-dessus.
5. **Lots 2 à 5** du redesign boutique (Lot 3 inclut le découpage de `updateBusinessDesign`, trop large aujourd'hui, le calcul de `response_time_minutes`, **et — ajouté le 2026-08-12 — la réécriture de `[shop-slug]/page.tsx:71` et `src/components/pwa/ShopBusinessLayout.tsx:65,83`, qui recalculent `shop.primary_color` localement au lieu de consommer `--brand`** : ce sont exactement les deux gabarits que la Règle A du §1 de `SPEC-v2-refonte-boutiques-publiques.md` — un seul gabarit pour les trois plans — doit fusionner. Pas de correctif isolé avant, la fusion des gabarits règle ça de fait).
6. **Page Tarifs** — différée, pas encore ouverte. À l'ouverture, séparer explicitement ce qui peut partir immédiatement (corrections de texte, ce qui est déjà tranché — voir §5) de ce qui dépend de la matrice `plan-features.ts` définitive (celle-ci reflète encore une version antérieure, voir §5) : ne pas mélanger les deux dans un même chantier.
   **Décisions arrêtées le 2026-08-13 sur le plafond produits, rattachées au Lot 5, aucune n'entre en livraison 1 :**
   - **Comportement du plafond confirmé correct, rien à changer** : une boutique en essai (`plan='trial'`) hérite de la limite Découverte (10 produits actifs) — cohérent, l'essai fait découvrir le produit, il n'ouvre pas le plan le plus généreux.
   - **Compteur de produits visible en permanence dans le tableau de bord** — « 7 produits sur 10 » sur la page produits, pour que la limite se voie approcher plutôt que se découvrir au blocage. Le point qui compte le plus des trois, et le meilleur levier vers Business.
   - **Message d'erreur** (`src/lib/actions/product-limit.ts:27`, toast via `ProductForm.tsx:335`) — nommer « Business » explicitement et porter un lien cliquable vers `/dashboard/upgrade`, plutôt qu'un toast transitoire sans action.
   - **Page Tarifs, tableau détaillé** : une ligne indiquant la limite de 10 produits pendant l'essai — à faire avec les autres corrections Tarifs déjà consignées au §5.
7. **Gating des codes promo** (Business/Pro uniquement, codes Découverte existants continuent de fonctionner) — **non chiffré, à instrumenter** : il manque le nombre de boutiques Découverte ayant des codes actifs. Ne pas confondre avec le chiffre du §7 (9 boutiques Découverte à 11-17 produits) : celui-là concerne la limite de produits du plan, pas les codes promo — deux mesures distinctes.
8. **Gating des SMS par plan** — conception décidée, chiffres manquants. Décidé : notifications par e-mail via Resend sur les trois plans, SMS automatiques en plus pour Business et Pro uniquement. Aucun palier ne perd d'information. Trois pistes de réduction à arbitrer une fois les chiffres connus : réserver le SMS à l'acheteur et non au marchand, qui dispose déjà du tableau de bord et de l'e-mail ; ne déclencher que sur les statuts qui comptent ; plafonner mensuellement par plan. Ajouter un compteur de SMS par boutique et par mois. Non chiffré : volume mensuel par plan, coût correspondant à 15 FCFA l'unité, part des commandes payées à la livraison, taux de remplissage du champ e-mail du tunnel. **À relire à la lumière du §16** : le canal « WhatsApp » n'a jamais existé (toujours du SMS Lafricamobile), ce qui change la base de ce chantier.
9. **Renommer `sendWhatsApp` en `sendSms`** (`src/lib/notifications/whatsapp.ts` et tous ses appelants) — **Livraison 2, décidé le 2026-08-13.** Le nom trompe depuis l'origine (aucune intégration WhatsApp réelle n'a jamais existé, voir §16) ; ajouter un commentaire au-dessus de la fonction précisant que WhatsApp n'est pas intégré. Renommage seul, aucun changement de comportement.
10. **`webhooks/stripe/route.ts` n'envoie pas d'e-mail de confirmation de commande, contrairement à `webhooks/bictorys/route.ts`** — **à corriger avant l'ouverture Europe/Canada, décidé le 2026-08-13** (§16 point identifié, pas encore chiffré séparément). Les clients payant par carte (Stripe, majoritairement EU/CA) n'ont aujourd'hui aucune confirmation e-mail automatique, contrairement aux paiements Bictorys.
11. **E-mail de livraison numérique, en plus du SMS (jamais à la place)** — **Livraison 2, en tête du groupe SMS/notifications, chiffré au §15 point 3.** Corrige directement les 4 échecs sur 20 mesurés (§14 point 2) : gabarit à écrire (clone de `sendOrderConfirmationEmail`), deux points d'appel à modifier (`webhooks/bictorys/route.ts:531`, `webhooks/stripe/route.ts:154`), condition sur `client_email` non vide. Couverture mesurée 60 % chez les acheteurs de produits numériques (§15 point 3), largement au-dessus du plafond général de 7,6 %/19,3 %.
12. **Compteur de visites (`shop_visits`/`VisitBeacon`) ouvert à toutes les boutiques, pas seulement `trial_model='free_orders'`** — **Livraison 2, juste après le point 11 ci-dessus, décidé le 2026-08-13.** Envisagé pour la livraison 1, écarté : faire basculer 1 543 boutiques sur un endpoint qui n'a jamais servi qu'une fois (§11/§16) ajouterait un volume inconnu au pire moment, le jour même de la mise en production. Le déploiement de la livraison 1 donnera les premières boutiques `free_orders` réelles — observer leur comportement sur ce point avant de l'ouvrir à toutes, pas l'inverse.
13. **Dette technique — historique de migrations pas dans l'ordre chronologique d'application, constaté le 2026-08-13.** `086`/`087` ont été appliqués et enregistrés (`migration repair`) *après* que `088`/`089`/`090`/`091` l'étaient déjà — l'ordre numérique des fichiers ne correspond plus à l'ordre réel d'application dans `supabase_migrations.schema_migrations`. Rien de cassé aujourd'hui (vérifié : les 6 objets de `086`/`087` existent, l'historique est complet 001-091). **Mais tout futur `supabase db push --linked` retombera sur le refus déjà rencontré** (« Found local migration files to be inserted before the last migration on remote database », réclame `--include-all` — interdit par AI_RULES.md §3). Une session future qui ne lit pas ce point redécouvrira le problème de zéro. Pas d'action requise tant qu'aucune nouvelle migration n'a besoin d'être poussée par le chemin normal ; le jour où c'est le cas, repartir directement sur le chemin hors séquence (`db query -f` + `migration repair`), pas sur `db push`.
14. **`NOTE_MASQUAGE_COMMANDES.md`, à faire dès que ce fichier arrive sur `refonte-start`** (il existe sur `main` depuis `7a162af`, absent d'ici avant la fusion `main → refonte-start`) — ajouter un renvoi vers l'en-tête de `src/lib/orders/redact.ts` pour la dérogation `admin/payments/page.tsx` (documentée le 2026-08-13, commit `52a5b69`), pas dupliquer le texte à deux endroits.
15. **Rappels J-1 avant livraison — code d'envoi supprimé, pas cassé, constaté en recette le 2026-08-14.** `api/cron/reminders/route.ts` : le cron tourne toujours et marque les commandes comme rappelées (`reminder_sent_at`), mais n'envoie plus rien — commentaire dans le fichier, datant d'avant Lafricamobile (« Rappels SMS clients désactivés pour limiter la consommation Twilio »). Retiré du plan Business (`plans.ts`, `AFRICA_PLANS`) le 2026-08-14 : c'était vendu depuis l'époque Twilio, jamais fonctionnel depuis. **Aucune réparation du ticket Lafricamobile ne le ramènera** — à reconstruire entièrement (le send a été retiré du code, pas seulement en échec) avant de le revendre un jour.
16. **Motif « charge créée d'abord, écriture locale ensuite, avalée si elle échoue » — Livraison 2, à auditer comme famille de bug, pas cas par cas.** Constaté deux fois, même forme : `lib/actions/payouts.ts` (le reversement part chez Bictorys avant que `payments`/`payouts` ne soit mis à jour — risque de double reversement déjà noté dans `AI_RULES.md` §3, « Vérification des erreurs ») et `api/payments/bictorys/create/route.ts:117-129` (la charge Bictorys est créée avant l'insert dans `payments` — trouvé le 2026-08-14 en cherchant le paiement réel d'une boutique de recette : commande `d89a50b2…`, aucune ligne `payments`, alors que l'argent a pu réellement bouger côté Bictorys). Les deux cas avalent l'échec d'écriture en `console.error` seul, « on ne bloque pas le paiement/le reversement ». Objet du chantier : lister tous les points d'appel qui suivent ce motif (pas seulement ces deux), décider d'une réponse commune (Sentry `fatal` a minima, comme déjà fait ailleurs dans `payouts.ts` — voir la marche à suivre déjà écrite dans `scripts/check-post-deploy-48h.sql`, point D), avant d'en découvrir un troisième en production.
17. **Une boutique `free_orders` restée en `draft` ne peut pas être publiée depuis le tableau de bord classique — seul `/start` le fait, constaté le 2026-08-14 en corrigeant l'Assistant IA.** `activateTrialShop` (`start/actions.ts:167`, qui fait passer `status` de `'draft'` à `'trial'` et pose `is_active=true`) n'est appelée que depuis `StartFlow.tsx:268`, le dernier écran du parcours `/start`. `lib/actions/products.ts` (`createProduct`, le chemin normal « Produits → Ajouter un produit ») ne l'appelle jamais. Un marchand qui quitte `/start` avant l'écran 10 puis ajoute un produit depuis le tableau de bord classique se retrouve avec un produit actif sur une boutique qui reste `draft`, invisible, sans qu'aucun chemin du tableau de bord ne la débloque — seul un retour à `/start` le peut. **Confirmé réel dans le parcours nominal, pas seulement en théorie** : le premier produit s'ajoute normalement juste après la création du compte, avant que le marchand n'atteigne son tableau de bord — la fenêtre où il peut en sortir entre les deux existe bel et bien. **Chiffré le 2026-08-14** (colonnes vérifiées avant la requête — `shops.trial_model`/`status`, `products.is_active`/`shop_id`) : **0 boutique dans cet état exact aujourd'hui** (`trial_model='free_orders' AND status='draft'` avec au moins un produit actif, jointure directe) — mais sur une population de seulement **3 boutiques `free_orders` au total** (`Nala Store`, `draft`, 0 produit actif ; `Amani` et `Moulina`, les deux boutiques de recette, déjà `expired`/`trial` avec 1 produit chacune, donc déjà passées par l'écran 10 avec succès). Le zéro ne prouve pas l'absence du problème, il reflète une population trop petite pour l'avoir encore produit — à re-mesurer après le déploiement, quand `free_orders` aura de vrais volumes. Pas corrigé — l'Assistant IA (§ ci-dessus) a été adapté pour renvoyer vers `/start` plutôt que d'affirmer qu'ajouter un produit suffit, mais le vrai correctif est côté `createProduct`/`toggleProductActive`, pas fait ici.
18. **`Nala Store` — vérifié, pas un artefact : c'est le comportement normal d'un `/start` abandonné entre l'écran 7 et l'écran 9.** `phone_whatsapp` est `NULL`. **Deux requêtes directes, deux résultats à zéro** : `SELECT ... FROM profiles WHERE shop_id = '2677d1a7…'` (aucune ligne) et `SELECT count(*) FROM auth.users u JOIN profiles p ON p.id = u.id WHERE p.shop_id = '2677d1a7…'` (`comptes_auth_lies = 0`) — aucun profil ni aucun compte `auth` ne référence cette boutique, par les deux angles. Cohérent avec `createDraftShop` (écran 7, avant tout compte) sans `completeSignupFromStart` (écran 9, qui pose le téléphone et le lien profil↔boutique) derrière. Rien à corriger, c'est exactement la trace qu'un abandon à ce stade est censé laisser.
19. **Panne SMS = perte d'accès au compte, pas seulement une notification manquée — conséquence distincte, plus grave, du même problème Lafricamobile (§13-§16).** `requestPinReset` (`lib/actions/auth.ts:255-280`) est le seul chemin de récupération d'un compte dont le marchand a oublié le code PIN — aucune alternative par e-mail n'existe (l'identifiant de connexion est le téléphone, pas un e-mail réel, voir §16 point 2 sur `@beautydesk.app`). Le code y est correctement géré (§13 point 3 : erreur explicite renvoyée à l'écran, pas avalée) — **mais tant que Lafricamobile ne délivre rien, cette erreur explicite est la seule chose que le marchand reçoit : jamais le code lui-même.** Un marchand qui oublie son PIN aujourd'hui n'a structurellement aucun moyen de rentrer dans son compte, pas juste un inconfort — à traiter avec la même urgence que le reste de la panne SMS, pas comme une notification secondaire.
20. **L'Assistant IA suppose Afrique/FCFA sans exception — faux pour une boutique EU/CA, pas corrigé, trouvé le 2026-08-14 en fournissant les vrais prix à `system-prompt.ts`.** La section « PLANS ET TARIFS » du prompt (et `monthlyPriceLabel`, sa seule source de prix) ne lit que `AFRICA_PLANS` — jamais `EU_CA_PRO_PLAN_EUR`/`EU_CA_PRO_PLAN_CAD` ni `getPlansForCountry(shop.country)`, qui existent déjà précisément pour cette bascule (utilisés par le vrai `/dashboard/upgrade`, pas par l'Assistant). Pour une boutique d'Europe ou du Canada, l'Assistant décrirait aujourd'hui trois plans en FCFA (Découverte/Business/Pro à 2 900/4 900/9 900) au lieu de l'unique plan Pro réel, en EUR ou CAD (14,90 €/19,90 CAD selon `plans.ts`) — une boutique de la diaspora recevrait une réponse structurellement fausse sur son propre tarif, pas juste incomplète. `buildSystemPrompt` reçoit déjà `shop.country` : le nécessaire pour corriger existe, rien n'est branché. **À traiter avant de considérer l'Assistant IA fiable pour ces boutiques précises** — distinct du chantier de ce soir, qui n'a jamais touché ce cas.
21. **Offre de lancement « 1 mois offert » (plan Business, 62 jours au lieu de 31 au premier paiement mensuel) — désactivée le 2026-08-14, décision explicite de l'utilisateur.** Mécanisme (`activate-plan.ts:39-49` avant retrait) : à la première transaction `activated` d'une boutique, si le plan choisi est Business en cycle mensuel, la durée accordée passait de 31 à 62 jours. Retiré du code (plus jamais accordé pour une nouvelle première activation) et du texte (`plans.ts:31`, badge `promo` retiré avec l'emoji). **Mesuré avant coupure** (`subscription_transactions` × `shops`, requête et sortie brute dans la conversation du 2026-08-14) : **47 boutiques** en ont bénéficié depuis la mise en place du mécanisme ; parmi elles, **22** étaient, au moment de la coupure, dans un abonnement encore actif et jamais renouvelé depuis — donc littéralement en train de vivre les 62 jours ce jour-là. **Aucun impact rétroactif** : `subscription_ends_at` est écrit en base au moment de l'activation passée, la coupure du code ne raccourcit rien pour ces 22 boutiques, elle empêche seulement que de nouvelles boutiques en bénéficient à l'avenir. Vérifié dans le dépôt : aucune autre mention de cette offre (landing, `PricingV6`, `SubscriptionCheckoutForm.tsx`, page `/dashboard/billing`, FAQ `aide/page.tsx`, gabarits WhatsApp) — les occurrences de « 2 mois offerts » trouvées ailleurs concernent la remise annuelle, un avantage distinct, toujours valide, non touché. **Hors dépôt (site vitrine externe s'il existe, argumentaire de vente, messages déjà envoyés à des marchands) non vérifiable depuis le code — à vérifier par l'utilisateur.**
22. **Checkout Stripe : un marchand EU/CA voyait la liste de fonctionnalités « Afrique » au lieu de la sienne — corrigé le 2026-08-14.** `StripeSubscriptionCheckoutForm.tsx` portait sa propre liste `PLAN_FEATURES` codée en dur, dupliquée de `plans.ts` et divergente de `EU_CA_PRO_PLAN_EUR` (par exemple `.pro` y mélangeait des libellés des deux sources). Cette liste étant utilisée aussi bien pour les vrais marchands EU/CA (`planKey` toujours `'pro'`, `checkout/page.tsx:70`) que pour les marchands africains payant par carte (`method=stripe`) — et les deux cas envoient `currency='EUR'`, qui ne permet donc pas de les distinguer à lui seul —, un marchand EU/CA recevait la liste pensée pour l'Afrique (domaine `.tonsite.com`, formulation Meta Pixel africaine) au lieu de la sienne. **Corrigé** : `checkout/page.tsx` transmet désormais `isEuCa` (déjà calculé, `shopEuCa`) en prop à `StripeSubscriptionCheckoutForm`, qui sélectionne `(currency==='CAD' ? EU_CA_PRO_PLAN_CAD : EU_CA_PRO_PLAN_EUR).features` si `isEuCa`, sinon `AFRICA_PLANS.find(p => p.key === planKey)?.features` — même source que `/dashboard/upgrade` et `TrialEndScreen`, plus aucune liste dupliquée dans ce fichier. Vérifié par exécution directe de la fonction extraite (`tsx`, hors build) : un marchand EU (EUR) et un marchand CA (CAD) reçoivent tous deux la vraie liste `EU_CA_PRO_PLAN_EUR`/`CAD`, un marchand africain en carte reçoit la vraie liste `AFRICA_PLANS` de son plan. **Au passage, retirée à la source (`plans.ts`) dans le même lot** : la bulle « Boutique en ligne immédiatement » (Découverte et `EU_CA_PRO_PLAN_EUR`) — fausse pour une boutique `free_orders` déjà en ligne depuis longtemps quand elle atteint cet écran ; cette source alimentant `/dashboard/upgrade`, `TrialEndScreen` et ce checkout, un seul retrait suffit pour les trois.
23. **Trois corrections de texte, dashboard, regroupées ici pour mémoire — 2026-08-14, même lot que les points 21-22.** Badge de statut `PlanBadge` (`Sidebar.tsx`) : « Essai terminé » → **« Plan requis »** pour une boutique `free_orders` `expired` (évite le mot « essai », le marchand vend, il n'essaie pas). Bannière du haut sur boutique `free_orders` `expired` avec commandes retenues (`layout.tsx`) : « {N} commande(s) t'attend(ent). Active ta boutique pour les traiter. » → **« {N} commande(s) retenue(s), en attente que tu actives un plan. »** (l'ancien texte laissait penser que c'était le client qui n'avait pas payé). Checklist de démarrage (`SetupChecklist.tsx`), étape « Active ton site » : bifurquée selon `trial_model` — légèrement inchangée pour `legacy`, devient **« Passe à un plan payant » / « Pour continuer à recevoir des commandes sans limite, au-delà des 3 offertes »** pour `free_orders` (l'ancien texte affirmait à tort qu'un marchand déjà en ligne devait payer pour « être visible »). Les trois corrections partagent le même diagnostic que les points 1-A à 1-E de la conversation du 2026-08-14 : du texte pensé uniquement pour `legacy`, jamais adapté à `free_orders`.
24. **Un client pouvait payer en ligne malgré les deux modes de paiement désactivés par le marchand — trouvé et corrigé le 2026-08-14, entré en Livraison 1 par exception au périmètre gelé (§0).**

    **Gravité.** Découvert en testant le point « boutique avec un mode de paiement coupé » de la liste de recette du §0. En désactivant `accept_online_payment` **et** `accept_cash_on_delivery` dans Paramètres → Ventes, le tunnel de commande public affichait une section « Mode de paiement » vide (les deux options masquées, cohérent) mais laissait quand même passer la confirmation — le client atterrissait sur un vrai écran de paiement Wave/MaxIt (Bictorys) et pouvait payer réellement, sans qu'aucune vérification, nulle part dans la chaîne jusqu'à la création de la charge, ne consulte la configuration du marchand. Atteignable sans friction : les deux interrupteurs sont indépendants, aucune validation n'empêchait de les couper tous les deux, aucun avertissement ne signalait l'état résultant — un marchand pouvait s'y retrouver par erreur ou en pensant n'en couper qu'un.

    **Cause.** `OrderForm.tsx:353-356` initialisait `paymentType` avec un ternaire à deux branches (`acceptCashOnDelivery ? 'on_delivery' : 'online'`) qui ne lisait jamais `acceptOnlinePayment` — écrit pour deux états (COD actif, ou COD coupé donc en ligne par défaut), jamais conçu pour le troisième (les deux coupés). Le rendu masquait bien les deux cartes de paiement dans ce cas, mais le bouton de confirmation restait actif et soumettait quand même `payment_type: 'online_full'`. Recherché sur toute la chaîne aval (`api/orders/route.ts`, `api/payments/bictorys/create/route.ts`) : **aucun des deux ne lisait `accept_online_payment`/`accept_cash_on_delivery`**, la commande et la charge Bictorys se créaient sans jamais revérifier le consentement du marchand.

    **Aucun incident réel** : découvert et vérifié entièrement sur `viensonsconnait`, boutique de test de l'utilisateur (Pro, `legacy`) — les deux flags remis à `true` après les tests, aucun paiement réel n'a été tenté au-delà des vérifications de rejet (400).

    **Portée des correctifs, trois couches, commit `d2be1a6` :**
    - **A — client** (`OrderForm.tsx`) : `hasAnyPaymentMethod` bloque la confirmation avec un message explicite (« Cette boutique n'accepte aucun mode de paiement en ce moment. »), à l'étape « Mode de paiement » et sur le bouton de confirmation lui-même, quel que soit l'écran atteint. **Inclut le bug annexe même famille** : pour un produit digital, `payment_type` retombait sur `'on_site'` (sans aucun sens, un digital n'a pas de site où passer) quand `acceptOnlinePayment` était faux — `hasAnyPaymentMethod` couvre aussi ce cas (`isDigital ? acceptOnlinePayment : ...`), rendant ce chemin inatteignable.
    - **B — serveur, deux points indépendants** : `api/orders/route.ts` revérifie le mode de paiement réellement retenu (après le reroute existant « boutique expired ») contre la configuration réelle du marchand, rejette en `400` sinon — protège contre un appel direct à l'API contournant l'UI. `api/payments/bictorys/create/route.ts` revérifie une seconde fois, indépendamment, juste avant la création de la charge — dernier verrou avant que l'argent ne bouge réellement, ne fait confiance ni au client ni à `payment_type` déjà enregistré sur la commande.
    - **C — réglages** (`lib/actions/settings.ts`, `updateShop`) : refuse d'enregistrer les deux interrupteurs à faux en même temps, avec un message explicite au marchand — empêche d'atteindre l'état plutôt que de seulement le gérer une fois atteint.

    **Testé en conditions réelles le 2026-08-14**, sur préversion `dev` (bypass de la protection Vercel utilisé pour le test serveur) : les quatre scénarios validés — écran + bouton remplacés côté client, rejet `400` sur appel direct à `/api/orders` (message exact confirmé), garde bloquant l'enregistrement côté réglages, cas produit digital confirmé sur un vrai produit (« Et Pourtant, on s'aimait », `viensonsconnait`).

    **Entré en Livraison 1 par exception explicite au périmètre gelé du §0** : la refonte du tunnel de commande reste hors périmètre, mais ce correctif n'en fait pas partie — trois points de code isolés pour un bug de sécurité/argent démontré atteignable sans friction, pas une refonte.
25. **`/onboarding` — collision de slug invisible via RLS, erreur avalée, "Réessaie" ne répare rien — trouvé le 2026-08-14 en testant le point recette du §0, non corrigé, Livraison 2.**

    **Cause.** `startOnboarding` (`lib/actions/onboarding.ts:52-59`) vérifie l'unicité du slug via le client de session (soumis au RLS), puis crée réellement la boutique via le client `admin` (qui contourne le RLS). La policy `shops_public_read` actuelle (migration `078`) cache toute boutique `trial_model='legacy'` avec `is_active=false` à tout le monde sauf son propriétaire — la vérification d'unicité ne voit donc jamais ces boutiques-là, même quand leur slug est déjà pris. Si le nom choisi collisionne avec l'une d'elles, la boucle conclut à tort que le slug est libre ; l'insert réel percute alors l'index unique partiel `shops_slug_unique_non_draft` (`slug` unique WHERE `status <> 'draft'`). L'erreur Postgres réelle (`23505 duplicate key value violates unique constraint`) est avalée en `console.error` seul (ligne 75) — jamais renvoyée au client, jamais en Sentry — remplacée par le message générique « Impossible de créer la boutique. Réessaie. ». **"Réessaie" ne répare jamais rien** : le nom ne changeant pas, le même slug "faussement libre" est recalculé à l'identique à chaque tentative — un piège permanent pour ce nom précis, pas un échec transitoire.

    **Reproduit directement** (insert admin avec un slug déjà pris par une boutique `legacy` `is_active=false` réelle) :
    ```
    {"code":"23505","message":"duplicate key value violates unique constraint \"shops_slug_unique_non_draft\"","details":"Key (slug)=(ma-boutique) already exists."}
    ```

    **Portée volontairement limitée à la Livraison 2** : `startOnboarding` n'a qu'un seul appelant dans tout `src/` (`OnboardingWizard.tsx`), lui-même atteignable uniquement via la garde middleware sur `/dashboard/*` pour un profil sans `shop_id`/`onboarding_completed` — vérifié par recherche exhaustive, pas supposé. Après déploiement de la Livraison 1, ce chemin ne s'ouvre plus que par la même fenêtre résiduelle étroite déjà documentée au §16 (interruption de `completeSignupFromStart` entre `auth.signUp()` et le second `profiles.update`) — le parcours normal de création passe par `/start`, pas par `/onboarding`. Contrairement au §24 (argent, atteignable sans friction sur n'importe quelle boutique), ce bug n'est atteignable qu'à travers cette fenêtre étroite : pas de fusion argent, pas d'exception au périmètre gelé.
26. **Reprise de paiement, "Impossible d'initier le paiement" sur préversion — cause confirmée : `BICTORYS_API_URL` mal configurée sur Vercel Preview, Production saine — trouvé et corrigé (config) le 2026-08-14/15.**

    **Cause.** `lib/payments/bictorys.ts:4` : `process.env.BICTORYS_API_URL ?? 'https://api.bictorys.com/pay/v1'` — le code traite `BICTORYS_API_URL` comme devant **déjà inclure** le segment `/pay/v1`, sa propre valeur de repli le montre. Or le tableau "URLs par environnement" de la documentation officielle Bictorys (partagée par l'utilisateur) liste les Base URL **sans ce suffixe** (`https://api.bictorys.com` / `https://api.test.bictorys.com`), et son exemple `.env` fait pareil. La variable `BICTORYS_API_URL` de l'environnement **Preview** sur Vercel avait été configurée en suivant cette doc au pied de la lettre (`https://api.test.bictorys.com`, sans `/pay/v1`) — l'appel réel devenait donc `{domaine}/charges` au lieu de `{domaine}/pay/v1/charges`, une route inexistante chez Bictorys : 404, corps vide. **Vérifié dans Sentry (`tekki-studio`/`javascript-nextjs`, connecté au projet mais jamais consulté jusqu'ici — cause du silence apparent, pas une absence réelle d'instrumentation)** : événement `JAVASCRIPT-NEXTJS-19`, breadcrumb `POST https://api.test.bictorys.com/charges?payment_type=wave_money → 404`, `merchantReference` confirmant la commande de test exacte (`dd32adc8…`). Écarté au passage : pas un problème de clé publique/privée (la doc confirme `BICTORYS_API_KEY` publique est correcte pour `/charges`, hypothèse initialement avancée puis retirée après vérification) ; pas un problème de syntaxe `payment_type` en paramètre de requête (un 400 historique — « Wrong payment type or disabled:orange_money », dans la même liste d'issues Sentry — prouve que Bictorys reconnaît et évalue ce paramètre quand la route est correcte).

    **Production confirmée saine, Preview seule affectée** : `BICTORYS_API_URL` en Production inclut déjà `/pay/v1` (vérifié directement par l'utilisateur dans Vercel) — aucun risque sur de vrais paiements. La variable Preview est corrigée en ajoutant le suffixe manquant, redéploiement nécessaire pour que la nouvelle valeur s'applique (les variables Vercel ne s'appliquent jamais rétroactivement à un déploiement déjà construit).

    **Trouvé au passage, découverte distincte de ce bug précis, à garder en tête** : la liste d'issues Sentry montre plusieurs autres erreurs Bictorys plus anciennes sur le même endpoint (`400 Wrong payment type or disabled:orange_money`, `500 Wave payment requires a redirection URL`) — un historique de pannes avalées (même motif que le §16, charge créée/erreur non remontée) qui vient seulement de redevenir visible parce que Sentry a enfin été consulté. **Pas traité ici, à auditer séparément** une fois ce point-ci refermé — possiblement plus d'un problème dans ce chemin de paiement, pas seulement celui de ce soir.

    **Recommandation pour la Livraison 2, pas appliquée ce soir (décision explicite de l'utilisateur)** : rendre la construction de l'URL robuste à cette classe d'erreur plutôt que de compter sur la variable d'environnement pour porter le suffixe — normaliser `BICTORYS_BASE_URL` en code (ajouter `/pay/v1` si absent) au lieu de deux formats attendus selon que la variable est définie ou non. Une variable mal renseignée redeviendrait alors sans effet plutôt qu'un 404 silencieux — corrige la classe d'erreur, pas seulement l'instance déjà trouvée. Recommandé, décision finale à l'utilisateur.
27. **Recette sur préversion et webhooks tiers — chantier structurel, deux causes distinctes qui se cumulent, Livraison 2, pas de code ce soir.**

    **Découvert en poursuivant le §26** : le paiement Bictorys aboutit désormais (404 corrigé), mais aucune trace ne remonte côté TEKKIShop après un paiement réellement reçu (confirmé sur le dashboard Bictorys, transaction `tekkishop-5b739008`, « Paiement reçu »). Deux causes structurelles distinctes identifiées, non départagées faute d'historique de livraison de webhook exploitable côté Bictorys (leur dashboard ne montre que la transaction, pas les tentatives d'appel webhook) :

    - **URL de webhook instable.** `webhookUrl` envoyé à la création de charge (`create/route.ts:118`) utilise `${APP_URL}/api/webhooks/bictorys`, où `APP_URL` retombe sur le domaine de **Production** (`NEXT_PUBLIC_APP_URL` non posée sur Preview) — pas une URL Vercel figée comme d'abord soupçonné, mais le mauvais domaine quand même pour un test de préversion. Par ailleurs, le dashboard Bictorys (compte Sandbox) a deux webhooks actifs enregistrés séparément : un vers le domaine Production (`tekki.shop`), un autre vers une URL de déploiement Vercel précise, périmée dès le déploiement suivant — chaque déploiement Vercel génère une URL unique, un webhook configuré dessus ne survit pas au déploiement d'après. **Vercel fournit un alias stable par branche** (`<projet>-git-<branche>-<équipe>.vercel.app`, confirmé résolu pour ce projet le 2026-08-15 : `ministore-git-dev-ibukamicrosaas-projects.vercel.app`) — à utiliser à la place d'une URL de déploiement figée, aussi bien dans `NEXT_PUBLIC_APP_URL` (Preview) que dans la configuration webhook côté Bictorys.
    - **Protection de déploiement Vercel.** Indépendamment de la question d'URL : toute requête serveur-à-serveur non authentifiée vers une URL Preview (stable ou non) se heurte à la protection SSO de Vercel — vérifié directement (`curl` sans en-tête sur l'alias stable → 302 vers `vercel.com/sso-api`). Bictorys ne peut porter aucune session SSO. Sans contournement explicite, **aucun webhook tiers ne peut jamais atteindre une préversion**, quelle que soit la stabilité de l'URL. Vercel expose un contournement en paramètre de requête (`?x-vercel-protection-bypass=<secret>`, secret "Protection Bypass for Automation" du projet) pour exactement ce cas d'usage — solution en cours de test par l'utilisateur au moment de la rédaction, non confirmée en conditions réelles.

    **Hypothèse annexe, non confirmée** : le webhook vers `tekki.shop` (Production) enregistré sur le compte *Sandbox* Bictorys — si Bictorys l'appelle réellement pour un paiement sandbox, la requête atteint du vrai code Production (base de données partagée avec Preview, §19), mais échouerait la vérification de signature (`BICTORYS_WEBHOOK_SECRET` de Production, presque certainement différent du secret configuré côté compte Sandbox) — rejet silencieux en 401, jamais capturé par Sentry sur ce chemin précis (`webhooks/bictorys/route.ts:69`, aucun `Sentry.captureException`). Non vérifiable sans historique de livraison webhook.

    **Pour la Livraison 2** : la recette sur préversion nécessite un mécanisme documenté et reproductible pour les webhooks tiers, pas une reconfiguration manuelle à chaque déploiement. Deux pistes, aucune tranchée ce soir :
    - Alias de branche stable + paramètre de contournement Vercel dans la configuration webhook du fournisseur (Bictorys, et tout futur intégrateur tiers).
    - Désactiver la protection Vercel spécifiquement sur les routes `/api/webhooks/*` plutôt que par contournement — **à évaluer, pas confirmé possible ni sûr depuis cette session** (dépend de ce que Vercel permet réellement au niveau du panneau de configuration, pas vérifiable depuis le code).
    ~~Ajouter aussi `Sentry.captureException` sur le rejet de signature webhook (`webhooks/bictorys/route.ts:69`) — silencieux aujourd'hui, aurait évité une partie de l'incertitude de ce chantier s'il avait été visible dès le départ.~~ **Fait, voir §28** — le seul des trois points listés ici traité ce soir, les deux autres restent Livraison 2.
28. **Suite du §26/§27 — rejet de signature webhook désormais instrumenté, cause du silence encore non départagée — 2026-08-15, chantier ouvert.**

    **Fait ce soir** : `Sentry.captureException` ajouté sur le rejet de signature (`webhooks/bictorys/route.ts:69`, commit `528a294`) — pur ajout d'observabilité, aucun changement de comportement. Jusqu'ici ce rejet était invisible (`console.error` seul), rendant impossible de distinguer "webhook jamais atteint" de "webhook atteint, signature rejetée" : les deux produisent la même absence totale de trace. Confirmé en re-testant un paiement après les correctifs du §26 : toujours aucune trace Sentry, mais **cette absence ne permettait pas de conclure** — le rejet de signature étant lui-même silencieux à ce moment-là, il aurait produit exactement la même absence d'événement.

    **Deux causes restent ouvertes, non départagées ce soir, l'instrumentation ci-dessus doit trancher au prochain test** :
    - Si un événement de rejet de signature apparaît désormais dans Sentry : le webhook est bien atteint (le contournement SSO posé sur le Webhook 5 côté Bictorys fonctionne) — il ne reste alors qu'un mismatch entre `BICTORYS_WEBHOOK_SECRET` (Vercel Preview) et le secret réel du Webhook 5 (compte Sandbox Bictorys) à aligner, une pure question de config.
    - Si rien n'apparaît toujours : le webhook n'atteint jamais l'app — `webhookUrl` envoyé à la création de charge (`create/route.ts:118`, `${APP_URL}/api/webhooks/bictorys`) ne porte aucun paramètre de contournement, contrairement à ce qui a été configuré manuellement côté dashboard Bictorys. Reste alors à déterminer si Bictorys respecte le champ `webhookUrl` par requête ou seulement sa configuration de dashboard — question non tranchable depuis le code, à confirmer par un test ou la documentation Bictorys.

    **Problème distinct, découvert au même moment, à ranger dans le chantier structurel du §27, pas un correctif de ce soir** : `successRedirectUrl`/`errorRedirectUrl` (`create/route.ts:116-117`) partagent le même motif `${APP_URL}/...` sans contournement — mais ici c'est le **navigateur du client** qui est redirigé vers une URL Preview protégée, pas un appel serveur-à-serveur. Aucun contournement par en-tête n'est possible pour une redirection de navigateur ; seul un paramètre d'URL le pourrait, avec le même arbitrage d'exposition que pour le webhook. Explique directement le symptôme observé (« pas de redirection vers la page de succès ») — cause distincte de celle du statut de commande resté « En attente », même racine structurelle (§27).

    **Second signal indépendant trouvé le 2026-08-16, en marge de l'estimation des pages pays Mali/Bénin/Burkina Faso — pointe vers la même cause.** Sur les commandes réelles `payment_type='online_full'` de ces trois pays (14 commandes annulées au total, échantillon petit), 5 ont une ligne `payments` réelle (donc un appel Bictorys qui a bien abouti à la création d'une charge) : 3 se terminent en `failed` côté Bictorys (échec réel, pas un abandon), et **2 restent bloquées en `pending`, jamais résolues ni en succès ni en échec** — une au Bénin (commande `df1bc6c0…`) et une au Burkina Faso (commande `9420b0d1…`, boutique codée `BK`). C'est exactement le symptôme déjà observé sur `viensonsconnait` (charge créée, statut jamais mis à jour côté TEKKIShop) — trouvé cette fois sur deux boutiques marchandes réelles, indépendamment du test de ce chantier. Renforce l'hypothèse que le webhook (ou son traitement) est en cause au-delà du seul cas de test suivi jusqu'ici, pas une preuve supplémentaire à elle seule, mais un second signal qui va dans le même sens.

    **Lacune d'instrumentation confirmée au passage, à ajouter à la liste des chemins financiers encore silencieux (même famille que le §4.16)** : le `catch` de `api/payments/bictorys/create/route.ts:149-152` ne fait que `console.error` — aucune trace Sentry, aucune écriture DB sur l'échec de l'appel Bictorys lui-même. Une charge qui échoue avant la création de la ligne `payments` (donc avant que quoi que ce soit ne soit persisté) est aujourd'hui invisible, indiscernable d'un abandon client précoce sans regarder les logs Vercel bruts. À corriger dans le même geste que l'instrumentation déjà posée sur le rejet de signature webhook (`Sentry.captureException` dans ce `catch`), pas nécessairement ce soir mais à ne pas reperdre.

    **Statut** : chantier ouvert, pas fermé ce soir. Prochaine étape, dès que possible : relancer un test de paiement complet sur `viensonsconnait` — l'instrumentation de ce soir donnera enfin une réponse directe sur laquelle des deux causes restantes est la bonne, sans avoir à deviner davantage.
29. **Landing/licence/pages pays — lot indépendant du chantier webhook (§27/§28), aucun recouvrement de fichiers ni de logique, traité en parallèle le 2026-08-16.**

    **Landing principale** (commit `02d4234`) : badge hero « 11 pays » → « 6 pays », retrait du mot « directement » dans la promesse de retrait mobile money (formulation jugée trop forte au regard du délai réel), mockup téléphone du hero passé de placeholders à de vraies images (`public/logo-vosc.png`, `jeu.png`, `ebook.jpg`), et le mockup boutique unifié entre le hero et la section « Une vraie boutique » dans un composant partagé (`ShopMockup.tsx`) — les deux sections montraient la même boutique (`viensonsconnait`) via deux copies de JSX divergentes, un seul écart aurait suffi à les désynchroniser silencieusement. TikTok corrigé vers `@tekki.shop` (deux occurrences, `LandingFooter.tsx`). **`PricingV6.tsx` devient la source unique de vérité sur les prix**, dérivée de `AFRICA_PLANS`/`plans.ts` au lieu de chiffres tapés en dur : ça a fait apparaître un vrai écart, le prix annuel affiché ne correspondait plus à `annualPrice` réellement facturé (§4.6 déjà notait la Page Tarifs comme non ouverte — celle-ci est `PricingV6`, la section tarifs de la landing, distincte, elle a été ouverte ce soir uniquement pour cet écart précis, pas pour la refonte différée du §4.6). Ligne « 0 % de commission » du plan Pro reformulée en « 0 % de commission avec tes propres clés de paiement », même condition que celle déjà arrêtée au §4.3 (`bictorys_secret_key`, jamais le plan) — appliquée ici au texte marketing, pas seulement au calcul réel déjà couvert par ce point. Ligne « codes promo » retirée du tableau comparatif (comparait une restriction qui n'existe pas réellement en code).

    **Pipeline candidature `/licence`** (commit `93c142e`) : le formulaire était purement cosmétique jusqu'ici (`e.preventDefault(); setSubmitted(true)`, aucune écriture). Ajouté : table `licence_applications` (migration `092`, RLS `USING (false)`, écriture uniquement via `createAdminClient()` — même schéma de verrouillage que `country_managers`), action serveur `submitLicenceApplication` avec rate limiting 5/h par IP (réutilise `login_attempts` avec `attempt_type: 'licence'`, `checkRateLimit` de `lib/rate-limit.ts` étant incompatible avec les Server Actions — il exige un `NextRequest` réel, absent hors Route Handler), e-mail de notification à `ibuka.ndjoli@gmail.com`, écran admin `/admin/licence-applications`. **Vérifié avant de committer, sur demande explicite** : les 8 sites qui font un `count`/lecture sur `login_attempts` (`auth.ts` ×4, `start/actions.ts`, `orders/route.ts`, `subscription-reminder`, `trial-onboarding`, `rate-limit.ts`) filtrent tous explicitement sur leur propre `attempt_type` — aucune métrique existante ne peut se retrouver polluée par les soumissions du formulaire licence. Les deux seuls accès non filtrés sont des purges cron par ancienneté (`cron-health-check`, `cleanup`), pas des métriques. Au passage : compteur de boutiques actives par pays sur `/licence` (Bénin/Mali/Burkina Faso, `BF`+`BK` groupés — voir la duplication de code pays déjà documentée au §9), et correction d'un `revalidate` manquant qui aurait figé ces compteurs au build.

    **`/togo` migré vers le design v6** (commit `80d4bd8`) : tournait encore sur l'ancien système (`CountryLandingPage.tsx`, police/couleurs Outfit/DM Sans, header et footer propres, jamais réconciliés avec la landing principale). Nouveau composant `CountryLandingV6.tsx`, structure de sections identique à `page.tsx`, réutilisant tels quels tous les composants déjà partagés (`LandingNav`, `LandingFooter`, `PricingV6`, `FAQv6`, `ShopMockup`, `icons.tsx`, `DemoModal`). Contenu adapté au Togo depuis `country-configs.ts` (déjà en place, jamais branché sur le nouveau design avant ce soir) : paiements Flooz/T-Money, villes Lomé/Kara/Sokodé, les 5 vraies étapes d'onboarding avec numéro et lien togolais, timeline et témoignages locaux, compteur de boutiques live filtré `country = 'TG'`. Vérifié à l'écran, section par section, captures à l'appui — pas seulement `tsc`/`build`.

    **Nettoyage** (commit `5213566`) : `CountryLandingPage.tsx` et ses 11 sous-composants exclusifs supprimés, confirmé par grep un par un qu'aucun autre fichier ne les importait plus. Une dépendance cachée trouvée en vérifiant : le type `PaymentLogo`, exporté par `PaymentScroll.tsx`, était encore consommé par `country-configs.ts` (donc transitivement par `CountryLandingV6.tsx`) — rapatrié inline dans `country-configs.ts` avant de supprimer `PaymentScroll.tsx`, pour ne rien casser silencieusement.

    **Aucun chevauchement avec le chantier webhook (§27/§28), toujours ouvert, statut inchangé depuis le 2026-08-15** : aucun fichier touché ce soir n'appartient au tunnel de paiement, aux webhooks ni à la configuration Preview. La prochaine étape sur ce point reste celle du §28 — relancer un test de paiement complet sur `viensonsconnait` pour que l'instrumentation Sentry pose enfin le rejet de signature webhook (ou son absence) et tranche entre les deux causes restantes.
30. **Unification du code pays Burkina Faso (`BF`→`BK`) — trouvé en vérifiant avant de lancer la page pays Burkina Faso, corrigé et déployé le 2026-08-16, en avance sur le plan du §9 qui reportait ça après la livraison 1.**

    **Pourquoi maintenant plutôt qu'après la livraison 1 comme prévu au §9** : sur le point de publier une page dédiée recrutant activement des marchands burkinabè, promettant explicitement que le paiement en ligne fonctionne dans ce pays — le risque de laisser la duplication s'aggraver au moment précis où on pousse de nouvelles inscriptions ne valait pas l'attente.

    **Deux chemins identifiés, un seul réellement dangereux.** Le paiement client (`api/payments/bictorys/create/route.ts`) est protégé la plupart du temps : le code pays envoyé à Bictorys est écrasé par la détection depuis le téléphone du client dès qu'il en fournit un (obligatoire au checkout), donc `shops.country` brut n'intervient que si ce téléphone ne matche aucun préfixe reconnu — pas observable en pratique, aucune commande n'existe pour une boutique `BF` (0 sur les 52). **Le retrait marchand (`lib/actions/payouts.ts:81`) n'avait aucune protection du tout** : `shop.country` partait tel quel vers `createBictorysPayout`, sans passer par le mapping `toBictorysCountry()` qui existait déjà dans le code mais n'était utilisé qu'à un seul endroit (l'affichage des moyens de paiement pendant le quiz `/start`, jamais l'appel réel). Aucun retrait n'a encore été tenté depuis les 52 boutiques `BF` (0 ligne dans `payouts`) — bug confirmé par lecture du code, pas encore observé en échec réel.

    **Ampleur réelle plus large que prévu, trouvée en creusant `OrderForm.tsx`** : au-delà de `shops.country`, la colonne `shops.target_countries` (JSONB, pays de livraison acceptés, migration 032) portait aussi `'BF'` dans son `DEFAULT` — **1498 boutiques sur ~1550 concernées**, l'écrasante majorité de la base, héritage direct de ce `DEFAULT` jamais corrigé depuis 2026. Sans ce second correctif, la seule mise à jour de la liste de pays dans `OrderForm.tsx` (vers `'BK'`) aurait fait disparaître le Burkina Faso du sélecteur de pays client sur pratiquement toutes les boutiques de la plateforme, un régression bien plus grave que le bug initial.

    **Vérifié avant d'écrire la migration 093, en direct sur la base liée** (pas seulement dans les fichiers de migration) : aucune contrainte `CHECK` ni clé étrangère sur `shops.country` (`pg_constraint` interrogé), aucun trigger spécifique au pays (`pg_trigger` interrogé, seul `shops_updated_at` existe). Migration en 3 parties : (1) `shops.country`, 52 boutiques ; (2) `shops.target_countries`, 1498 boutiques, transformation JSONB testée en dry-run avant écriture, aucune boutique n'avait déjà les deux valeurs (donc pas de risque de doublon) ; (3) `DEFAULT` de la colonne corrigé pour les prochaines boutiques. Appliquée et revérifiée après coup : `SELECT country, count(*) FROM shops GROUP BY country` ne montre plus aucune ligne `BF` (128 en `BK`, fusion des deux anciens comptes) ; `target_countries @> '["BF"]'` retourne 0 boutique.

    **Code corrigé dans le même commit** (`e958ae7`) : toutes les listes locales de pays qui écrivaient encore `'BF'` — `country-groups.ts` (source censée être unique pour `/start`, ne l'était pas pour les autres), `SettingsForm.tsx`, `OnboardingWizard.tsx` (avec sa propre fonction `detectCountryFromPhone` dupliquée, distincte de celle de `lib/payments/bictorys.ts`), `LoginForm.tsx`, `OrderForm.tsx`. Le mapping `ISO_TO_BICTORYS_COUNTRY`/`toBictorysCountry()` devient inutile une fois la source unifiée — supprimé, avec son unique appelant (`start/data.ts:getPaymentInfo`) simplifié pour lire directement le code. `COUNTRY_LABELS` de l'Assistant IA (`system-prompt.ts`) corrigé aussi — sans ça, l'Assistant aurait affiché le code brut `BK` au lieu de « Burkina Faso » pour ces boutiques après la migration, une régression que la migration elle-même aurait provoquée si ce fichier n'avait pas été trouvé dans le même passage. Garde-fou ajouté dans `payouts.ts` (double protection demandée explicitement) : normalise `'BF'` vers `'BK'` même si une future écriture échappait à la source désormais unifiée.

    **`OnboardingForm.tsx` non touché** : liste `'BF'` trouvée dans ce fichier aussi, mais confirmé sans aucun consommateur nulle part dans le dépôt (grep exhaustif) — code mort, laissé tel quel, aucun effet sur le comportement réel.

    **Hors de ce chantier, laissé pour plus tard comme convenu** : l'absence d'instrumentation Sentry sur `api/payments/bictorys/create/route.ts` (même lacune que celle déjà notée au §28 pour ce même fichier).
31. **Commission différenciée par pays + frais de retrait facturés au marchand — 2026-08-16, chantier fermé. Infra associée (PITR, sauvegarde Storage) traitée dans le même lot, statuts distincts.**

    **Pourquoi** : le taux plat de 3% ne couvrait plus le coût réel Bictorys (encaissement + retrait cumulés) dans plusieurs pays, vérifié contre leur grille tarifaire officielle fournie par l'utilisateur. Deux changements produit/finance décidés par l'utilisateur, plan technique validé avant tout code.

    **Commission PAY IN** (`lib/billing/commission.ts`, `getCommissionRate(country, hasOwnBictorysKeys)`) : SN/CI 3% (inchangé), BJ/TG 4%, BK 5%, ML 5% temporaire (grille Bictorys Mali non fournie par l'utilisateur, aligné sur BK en attendant confirmation directe auprès de Bictorys). 0% uniquement si `bictorys_secret_key` configuré — jamais dérivé du plan. **Corrige au passage l'anomalie plan-aware déjà documentée au §4.3** (`payouts.ts` donnait 0% à tout marchand Pro sans clés propres) : vérifié avant de trancher si un rattrapage individuel était nécessaire — **zéro ligne `payouts` avec `commission_amount=0` sur tout l'historique (14 retraits complétés, 223 901 FCFA de brut cumulé)**, donc aucune perte réelle à rattraper, correctif pour l'avenir uniquement, décision de l'utilisateur. 9 points qui calculaient ce taux séparément migrés vers la fonction partagée ; `admin/finances` et `admin` (page d'accueil) agrègent désormais ligne par ligne (pays + clés propres de chaque paiement) plutôt qu'un taux plat sur une somme globale.

    **Frais de retrait PAY OUT** (`lib/billing/payout-fees.ts`, `getPayoutFeeRate(country, method)`) : jusqu'ici absorbés par TEKKIShop, désormais facturés au marchand et affichés séparément de la commission (nouvelle colonne `payouts.payout_fee_amount`, migration 094 — jamais fondus dans `commission_amount`). Grille réelle opérateur + pays + 0,5% Bictorys fixe, pas une moyenne. Un pays/méthode non confirmé (Mali) bloque le retrait avec un message explicite plutôt que de facturer 0% par erreur. `RequestPayoutButton` affiche le taux exact et le montant net avant confirmation — transparence explicitement demandée par l'utilisateur, pas une déduction silencieuse.

    **Pages publiques** : `PricingV6` reçoit le pays en prop sur les pages pays (taux exact affiché), fourchette générique ailleurs (landing principale, `/produits-digitaux`, `/aide`) où le pays du visiteur n'est pas connu. Assistant IA : taux exact injecté dans le contexte de la boutique plutôt que supposé à 3% dans le prompt statique — sans ce correctif, `system-prompt.ts` aurait aussi affiché le mauvais taux après la migration.

    **Infra, statuts distincts au sein du même lot** :
    - **PITR reporté, à la demande explicite de l'utilisateur** après confirmation du coût (~115 $/mois pour 7 jours de rétention : +5 $/mois de calcul "Small" obligatoire — le projet tourne aujourd'hui sur "Micro" — + 100 $/mois de PITR, remplace les sauvegardes quotidiennes gratuites actuelles). Sauvegardes quotidiennes actuelles inchangées. À réactiver la question quand la volumétrie le justifiera.
    - **Sauvegarde Storage (logos, photos produits, ~2,5 Go) : workflow écrit et poussé** (`.github/workflows/storage-backup.yml`), `rclone copy` (jamais `sync`, pour ne rien supprimer côté sauvegarde) depuis l'API S3-compatible Supabase Storage vers un bucket Cloudflare R2, nightly + déclenchable manuellement. **Ne peut pas encore tourner** : nécessite 7 secrets GitHub (identifiants Supabase S3 + Cloudflare R2) que l'utilisateur a indiqué créer de son côté, à tester manuellement une fois en place avant de compter sur l'exécution automatique.

32. **Authentification séparée pour les country managers (e-mail + mot de passe, `/cm/login`) — 2026-08-16/17, chantier fermé, avec un point ouvert distinct laissé de côté volontairement (voir en fin de section).**

    **État trouvé avant de commencer, vérifié en direct plutôt que supposé** : les country managers empruntaient jusqu'ici exactement le même chemin que les marchands — `/login`, téléphone + PIN, même e-mail synthétique (`phoneToEmail`), même bassin `auth.users`. Seule différence : `signIn` (`lib/actions/auth.ts`) vérifiait après coup l'appartenance à `country_managers` pour rediriger vers `/country-admin`. **La création d'un country manager était cassée depuis l'origine, jamais fonctionnelle** : `addCountryManager` cherchait un compte existant via le champ natif `auth.users.phone` — or aucun marchand ne renseigne ce champ (ils signent avec un e-mail synthétique, le vrai numéro ne va que dans `raw_user_meta_data`). Vérifié sur la base : **1622 comptes `auth.users`, zéro avec `phone` natif renseigné.** Même en admettant qu'un compte soit trouvé, l'insertion aurait quand même échoué : `country_managers.name` est `NOT NULL` sans défaut, et l'ancien formulaire admin ne collectait que téléphone + pays, jamais de nom. **Confirmé en base : zéro ligne dans `country_managers`** — rien n'a jamais été créé avec succès, aucun compte de test à préserver.

    **Nouvelle structure, construite sur cette base propre** :
    - `/cm/login` — nouvelle page, e-mail + mot de passe, visuellement distincte de `/login` (même thème sombre que `/country-admin`, pas le design marchand). Nouvelle action `cmSignIn` (`lib/actions/cm-auth.ts`) : `signInWithPassword({email, password})` direct, sans passer par `phoneToEmail` ; vérifie ensuite `country_managers`, déconnecte et refuse explicitement si le compte n'y figure pas (défense en profondeur, même bassin `auth.users` que les marchands). Rate limiting propre : `attempt_type: 'cm_login'`, jamais mélangé aux tentatives marchands.
    - **Création par invitation, jamais par recherche d'un compte existant** : `addCountryManager` prend désormais e-mail + nom + pays. Compte déjà existant (deuxième pays, ré-invitation) → réutilisé par e-mail. Sinon → `admin.auth.admin.generateLink({type:'invite', email})` crée le compte ET renvoie le lien d'invitation, envoyé **via Resend** (`sendCountryManagerInviteEmail`, `lib/notifications/email.ts`) — jamais le mailer intégré Supabase, déjà écarté cette session pour sa limite de 2-4 e-mails/heure (voir l'audit infra du 2026-08-16). Le CM définit lui-même son mot de passe au clic, jamais transmis en clair par un admin.
    - Formulaire admin (`admin/country-managers/page.tsx`) : champ téléphone remplacé par nom + e-mail, liste affichée par nom/e-mail au lieu du numéro.
    - Trois redirections corrigées vers `/cm/login` (`country-admin/layout.tsx`, `lib/actions/country-admin.ts` ×2 fonctions) — y compris le cas « authentifié mais pas country manager », qui renvoyait vers `/dashboard` (l'espace marchand, désormais sans rapport) et renvoie maintenant vers `/cm/login`.
    - Branche de détection `country_managers` retirée de `signIn` marchand (`lib/actions/auth.ts`) — devenue inutile, un CM ne passe plus jamais par `/login`.

    **Point ouvert, laissé de côté volontairement, pas oublié** : les retraits des country managers (`country_manager_payouts`, distincts des `payouts` marchands) sont **entièrement manuels aujourd'hui** — `api/country-admin/request-payout` crée une demande, `api/admin/cm-payouts/[id]/complete` la marque « payée » après qu'un admin ait viré l'argent lui-même, aucun appel `createBictorysPayout` nulle part sur ce chemin. Pas de frais Bictorys à leur appliquer puisqu'il n'y a pas d'appel Bictorys du tout. Décision explicite : **ne pas automatiser tant que le nombre de country managers reste faible (moins de 10 attendus)** — à reconsidérer quand le volume justifiera de reconstruire ce retrait sur le même modèle que celui des marchands (`getCommissionRate`/`getPayoutFeeRate`, §4.31 ci-dessus).
33. **Vérification du chantier commission (§4.31) + corrections `/dashboard/upgrade`, checkout carte, formulation Pro — 2026-08-17.**

    **Vérification du calcul, sans rien fusionner** : `getCommissionRate`/`getPayoutFeeRate` exécutées en direct (`tsx`) contre des boutiques réelles des quatre pays concernés — tous les montants (commission, frais de retrait, net) exacts, cohérents avec la grille validée. Aucune boutique Bénin/Togo/Burkina Faso n'a de solde brut réel positif aujourd'hui (vérifié), donc les cas BJ/TG/BK ont été testés avec un montant hypothétique rond (100 000 FCFA) sur une vraie boutique active de chaque pays — seul le cas Sénégal (`viensonsconnait`) portait un vrai solde (14 924 FCFA).

    **Vérification en conditions réelles, faite par l'utilisateur** : retrait réel sur `viensonsconnait` (Sénégal, 3%) après consultation de la fenêtre de retrait — frais Bictorys affichés avant confirmation (1,5% Wave), net correspondant exactement au calcul. **Deuxième test à taux modifié (Bénin/Togo 4%) reporté à dessein** : aucune boutique de ces pays n'a de solde réel, décision de l'utilisateur de ne pas créer de transaction artificielle — on surveillera le premier vrai retrait d'un marchand réel dans ces pays après le merge vers `main`, pas avant.

    **Bug trouvé en marge de cette vérification, distinct du chantier §4.31 lui-même** : `AFRICA_PLANS.features` (`lib/billing/plans.ts`), consommé par `/dashboard/upgrade` et par `TrialEndScreen` (écran de fin d'essai) via `getPlansForCountry()`, tapait encore "3% de commission" en dur — jamais mis à jour lors du chantier §4.31, qui n'avait touché que `commission.ts`/`payout-fees.ts` et les pages publiques. Corrigé à la racine : `AFRICA_PLANS.features` porte désormais un marqueur (`{{COMMISSION}}`) résolu par `getPlansForCountry()` avec le vrai taux du pays — corrige `/dashboard/upgrade` et `TrialEndScreen` d'un seul geste, sans toucher directement à ce dernier. Le checkout carte (`StripeSubscriptionCheckoutForm.tsx`) avait sa propre copie de la même logique (`AFRICA_PLANS.find(...).features` en dur) — corrigé pour recevoir le taux déjà résolu en prop depuis `checkout/page.tsx`.

    **Formulation "0% de commission avec tes propres clés Bictorys"** (nommant Bictorys explicitement plutôt que "agrégateur de paiement" générique, sur demande de l'utilisateur — le mécanisme réel ne fonctionne qu'avec une clé Bictorys propre) appliquée aux six emplacements où l'ancienne formulation ("propres clés de paiement") existait : `plans.ts`, `PricingV6.tsx` (×3), `produits-digitaux/page.tsx`, `FAQv6.tsx`. Bouton `/dashboard/upgrade` : "Activer ce plan" → "Choisir ce plan".

    **Point ouvert, laissé de côté volontairement, pas un bug** : `PricingV6.tsx` (landing) maintient sa **propre** liste de fonctionnalités par plan, écrite indépendamment d'`AFRICA_PLANS.features` — contenu réellement différent (la landing mentionne les paliers de messages Assistant IA, absents d'`AFRICA_PLANS` ; `AFRICA_PLANS` mentionne Meta Pixel/Export CSV/Statistiques avancées, absents de la landing), pas juste une divergence de taux. **Décision de l'utilisateur : ne pas fusionner ce soir** — le seul point réellement inexact (le taux de commission) est corrigé aux deux endroits, le reste reste deux listes distinctes (marketing landing avec mécanisme d'items verrouillés pour inciter à l'upgrade, vs liste plus exhaustive côté dashboard) jusqu'à nouvelle décision. Pas urgent, à reprendre si une vraie confusion apparaît entre les deux surfaces.
34. **`seller_stage`/`selling_channel`/`pain_point` (`shops`, écrits par `/start`) — collectés mais jamais relus, piste future pour l'Assistant IA, pas urgent — 2026-08-17.**

    **Vérifié en répondant à une question sur la personnalisation de l'écran de transition `/start` (étape 6, "Révélation")** : le titre et le paragraphe de cet écran sont bien personnalisés selon la réponse à l'étape 3 ("Où en es-tu aujourd'hui ?", `seg` A/B/C) — vraie logique, pas générique. La liste à puces qui suit ("Ton plan d'acquisition sur 7 jours…") est *aussi* réellement personnalisée, mais par une question différente et plus tardive du même parcours (étape 5, "Qu'est-ce qui te bloque le plus ?", `blocage`) — les deux mécanismes coexistent dans le même écran, ce qui peut prêter à confusion sur laquelle des deux réponses pilote quoi.

    **Trouvé au passage, hors de la question posée** : les trois réponses (`seller_stage`, `selling_channel`, `pain_point`) sont bien écrites dans `shops` à la création (`start/actions.ts:146-148`, colonnes réelles avec contrainte `CHECK` en base), mais **plus jamais relues nulle part ailleurs dans le dépôt** (recherche exhaustive) — ni dashboard, ni prompt de l'Assistant IA, ni notification. La personnalisation qui utilise ces réponses a déjà eu lieu côté client, dans l'écran de transition lui-même, avant même l'écriture en base — ces trois colonnes ne servent aujourd'hui qu'à de la segmentation analytique potentielle, jamais à adapter quoi que ce soit après la création du compte.

    **Piste future, non planifiée** : l'Assistant IA pourrait s'appuyer sur `pain_point`/`seller_stage` pour adapter ses premières suggestions au marchand (ex. un marchand `pain_point='clients'` pourrait recevoir des conseils d'acquisition en priorité, un `seller_stage='C'` un accompagnement plus pas-à-pas). Pas un bug à corriger, une donnée déjà collectée et prête à être exploitée le jour où ce chantier est ouvert.

---

## 5. Décisions produit arrêtées

**Règle qui gouverne la matrice des trois plans (Découverte, Business, Pro) :** rien de ce que voit l'acheteur ne dépend du plan du marchand ; seuls la capacité (nombre de produits, limites), l'outillage (fonctionnalités de gestion) et l'appropriation de la marque (personnalisation) en dépendent. Cette règle tranche les cas ambigus futurs.

Conséquences déjà actées :
- **Images de description** : ouvertes aux trois plans (ce que voit l'acheteur).
- **Liens sociaux** : ouverts aux trois plans, même raison.
- **Image de couverture** : descendue en disponibilité — accessible dès le plan Business, plus réservée à Pro.
- **Typographie boutiques publiques et landing** : Bricolage Grotesque + Inter (documentée dans `AI_RULES.md` §1). Les maquettes utilisent Instrument Sans dans les fichiers de design, mais c'est Inter qui fait foi en production.

**Coups de cœur** : déjà universels dans le code, sans aucune condition de plan (`ProductGrid.tsx`, seul `is_featured` compte). La page Tarifs les annonce à tort comme un avantage Pro — c'est la page qui est fausse, pas le code. Aucune fermeture : ce serait retirer une fonctionnalité en usage.

**Corrections décidées de la page Tarifs**, à porter dans `PricingSection.tsx` et le tableau dépliant, les deux devant dire la même chose : la ligne « 0 % de commission » de la carte Pro devient un encaissement sur son propre compte Bictorys, présenté comme une option qu'ouvre le plan Pro, jamais comme une réduction automatique — sans clés propres, un Pro paie 3 % comme les autres, ce qui couvre 1,5 % Bictorys plus 1 à 1,5 % d'opérateur au retrait. Les Coups de cœur sortent de la carte Pro. L'image de couverture entre dans la carte Business et sort de sa liste barrée. La liste barrée de Découverte se complète des notifications SMS, des codes promo et du tableau de bord avancé — elle ne montrait jusqu'ici que ce que Pro ajoute, jamais ce que Business débloque. Le badge de vérification n'apparaît nulle part sur cette page et ne doit pas y être ajouté : il devient éligible sur les trois plans, sur dossier.

Les 3 boutiques Pro sans clé Bictorys propre (§7) sont un choix assumé de leur part : elles préfèrent payer 3 % plutôt que d'intégrer un agrégateur. Ce n'est pas un défaut à corriger.

**`src/lib/plan-features.ts` (Lot 1, non commité) implémente une version antérieure de cette matrice** — `coverImage` et `richDescription` y sont encore réservés à `pro` uniquement, alors que la décision ci-dessus les ouvre plus largement. **À revoir explicitement quand la page Tarifs sera ouverte** (§4, point 6) — ne pas corriger isolément avant.

**§6.4 (revue de la refonte boutique) — clos.** Un bug suspecté (montant incohérent entre deux captures d'écran) s'est révélé être un artefact du test lui-même : un article retiré du panier entre les deux captures, pas une régression de code. Seul point conservé : une vérification de cohérence des montants à titre de garde-fou.

---

## 6. Points ouverts du tunnel de commande

Spec complète : `SPEC-refonte-tunnel-commande.md` (racine du dépôt). Maquette de référence : `tekkishop-commande-v2.html` (racine du dépôt). **Lecture requise en complément, avant tout travail touchant aux commandes retenues ou aux produits digitaux** : `SPEC-3-commandes-offertes.md` (modèle d'essai à 3 commandes, déjà implémenté) et son additif `ADDITIF-argent-commandes-retenues.md` (ce qu'il advient de l'argent d'une commande retenue — le principe qui gouverne directement le point 3 ci-dessous : « on ne prend l'argent du client que si on peut le livrer sans le marchand »).

Les trois points du §15 de `SPEC-refonte-tunnel-commande.md`, à remonter par écrit avant le lot correspondant, sans trancher seul :

1. La migration de `paymentType` vers un acompte explicite, et son comportement sur un panier à plusieurs articles dont certains seulement portent un acompte (§7 de la spec).
2. Le traitement de `delivery_type` pour les commandes entièrement digitales, avec le coût des deux options (§8 de la spec).
3. **Vérifié le 2026-08-10.** Ce qui autorise l'accès au fichier téléchargeable : `src/app/api/download/[token]/route.ts:15-31` ne vérifie ni `orders.status` ni un paiement encaissé — uniquement l'existence d'un `download_tokens` valide, non expiré, sous son quota. `orders` **n'a pas de colonne `payment_status`** (vérifié par lecture de toutes les colonnes de la table). Les seuls créateurs de token sont `src/app/api/webhooks/bictorys/route.ts:509-520` et `src/app/api/webhooks/stripe/route.ts:137-144` (paiement en ligne confirmé par webhook) et `src/app/api/dashboard/orders/[id]/resend-digital/route.ts:72-82` (action manuelle du marchand connecté, **sans aucune vérification de paiement** — seulement `isOrderBlocked`, ligne 48-50). `src/app/api/orders/route.ts` (création de commande) ne crée jamais de token. Conséquence : le flux automatique ne fuite rien à un client (pas de token sans webhook de paiement confirmé), mais **`resend-digital` fait confiance au marchand sans aucune preuve d'encaissement dans le système** — rien n'empêche techniquement un marchand de se déclencher lui-même le lien sans avoir été payé.
   **Commandes `on_site` portant un produit digital, en base de production, requête directe sur l'API REST Supabase** : **1 commande** — `8c485241-8c93-44c5-a24e-d238f5b7ea08`, boutique « K.B.Y multi services » (`k-b-y-multi-services`, plan Découverte, `trial_model='legacy'`), créée le 2026-07-19, `total_price=2000`, **statut `cancelled`**. Sur cette commande, **5 `download_tokens` ont été générés via `resend-digital`** entre 15:08 et 16:41 le même jour — un token a été téléchargé 5/5 fois, un autre 1/5 — donc **le fichier a bien été distribué, 6 téléchargements au total, sur une commande jamais payée en ligne et ensuite annulée**. Ce n'est pas un scénario théorique : il s'est produit au moins une fois. Reste à déterminer avec l'utilisateur si c'est un test du marchand sur son propre produit ou un cas réel à traiter.

**Contexte de `resend-digital`, apporté par l'utilisateur le 2026-08-10** : ce bouton a été ajouté en réponse à un incident réel — un acheteur avait payé son ebook et n'avait jamais reçu son lien. C'est un dépannage, pas une fonctionnalité de distribution ; il ne doit pas disparaître avant que la cause de cet incident soit comprise.

**Portée réelle du webhook silencieux, creusée le 2026-08-10 — plus large que le seul cas digital, correction d'une conclusion trop hâtive écrite plus tôt dans la même session.**

Mécanisme exact : `src/app/api/webhooks/bictorys/route.ts`. L'ordre réel des opérations est : idempotence (253-273) → paiement échoué (275-284) → validation du montant (286-305) → **`payments` marqué `completed` (307-350, inconditionnel dès que le paiement est reconnu valide)** → confirmation de la commande, gardée par `.eq('status','pending')` (353-369) → `if (!orderData) { console.log(...); return NextResponse.json({ok:true}) }` (371-373), **sans `Sentry`, sans alerte, HTTP 200 donc pas de rejeu côté Bictorys** (le fichier n'importe `Sentry` nulle part — contrairement à `webhooks/stripe/route.ts`, qui vérifie et logue chaque écriture). Ce même early-return court-circuite tout ce qui suit : confirmation WhatsApp et e-mail au client, alerte WhatsApp au marchand, passage à `'completed'`, création des `download_tokens`.
**Point clé** : la table `payments` est écrite *avant* ce garde, donc une commande dans cet état a normalement quand même une ligne `payments`. **Vérifié sur `79e7f687` : ce n'est pas le cas — aucune ligne `payments` n'existe pour cette commande.** La cause n'est donc probablement pas la course `.eq('status','pending')` décrite ci-dessus (qui laisserait une trace dans `payments`), mais un échec plus en amont et tout aussi silencieux : le webhook n'a peut-être jamais atteint le serveur, ou la validation de montant (ligne 297-304, `console.error` seul, pas de Sentry) l'a rejeté. **Correction explicite : l'affirmation précédente selon laquelle cette course était « la cause identifiée » de l'incident `79e7f687` n'est pas prouvée par la donnée — retirée.** La vulnérabilité structurelle (early-return silencieux, aucun Sentry, HTTP 200) reste réelle et vérifiée dans le code, seule son application à ce cas précis n'est pas démontrée.

**Décompte en base, élargi à tous les types de produits (pas seulement digital)** : sur 239 commandes en ligne (`payment_type IN (online_full,online_deposit)`), 55 ont dépassé `'pending'`. Parmi elles, **6 n'ont strictement aucune ligne `payments`** — la commande a progressé comme si elle était payée, sans aucune trace de paiement dans le système :
| Commande | Statut | Montant | Produit | Boutique | Créée |
|---|---|---|---|---|---|
| `bf8c898e…` | ready | 17 000 | physique | `861b30d0…` | 2026-07-23 17:39 |
| `57a6df3d…` | ready | 17 000 | physique | `861b30d0…` | 2026-07-23 17:37 |
| `9b806229…` | ready | 10 000 | physique | `da407507…` | 2026-06-26 |
| `98598e9e…` | delivered | 14 000 | physique | `12d6a93e…` (Viens on s'connaît) | 2026-06-23 |
| `f28e2535…` | delivered | 12 600 | physique | `2ba2570e…` | 2026-07-17 |
| `79e7f687…` | delivered | 4 500 | digital (ebook) | `12d6a93e…` (Viens on s'connaît) | 2026-06-25 |

**75 100 FCFA au total, 5 commandes physiques sur 6.** **Trois des six écartées par l'utilisateur le 2026-08-11, identifiées par le nom du client, pas par une supposition sur les montants : `98598e9e…` et `79e7f687…` (client « Yatma Ndiaye », +221781362728) sont l'identité de test de l'utilisateur — ce sont ses propres commandes, ce qui explique aussi que l'incident d'origine soit tombé sur l'une d'elles. `9b806229…` (client « dfvd dvdsv », +2250101010102) est une saisie clavier, pas un client réel.** **Les 3 cas réels — clos le 2026-08-11, réconciliés par l'utilisateur directement chez Bictorys.** `bf8c898e…` et `57a6df3d…` (Social technologie, 17 000 chacune) et `f28e2535…` (Themaïs Africa, 12 600) : **aucun paiement n'a abouti côté agrégateur** pour ces tentatives — figurent en « échoué » ou « en attente » chez Bictorys, jamais en réussi. L'absence de ligne `payments` est donc exacte, pas un webhook manqué : rien n'a jamais été encaissé, donc rien à enregistrer. **Sur les 6 commandes sans trace de paiement identifiées initialement : 3 étaient des commandes de test, 3 étaient des tentatives de paiement réellement échouées. Aucune perte, aucun marchand à créditer, aucune anomalie du système de webhook impliquée dans ces 6 cas précis.** L'hypothèse de deux clients distincts sur un produit qui avait de la traction ce jour-là (plutôt qu'une seule tentative retentée) est la bonne.

**Ce que ça déplace : la vraie question n'est pas le webhook silencieux, c'est le volume de paiements en ligne qui échouent ou sont abandonnés avant d'aboutir — voir « Paiements en ligne jamais aboutis » ci-dessous, ouverte le 2026-08-11.**

---

### Paiements en ligne jamais aboutis — ouvert le 2026-08-11

Précision de l'utilisateur : ces échecs viennent surtout d'un solde mobile money insuffisant ou d'un mauvais choix de méthode — l'acheteur arrive jusqu'au paiement, constate le problème, abandonne. Ce sont des ventes perdues au dernier mètre sur des acheteurs déjà décidés, pas des pannes techniques. Personne n'en est informé aujourd'hui : ni le marchand, ni l'acheteur, ni la plateforme.

**1. Trace laissée par une tentative échouée/abandonnée** : oui, une ligne `payments` existe dès l'initiation — `src/app/api/payments/bictorys/create/route.ts:120-129` insère `status:'pending'` **avant** la redirection vers Bictorys, dès que `createBictorysCharge` retourne une URL de paiement. Si le webhook renvoie ensuite un échec, `bictorys/route.ts:275-284` la fait passer à `status:'failed'`. **Mais un abandon pur (le client ferme l'onglet sans jamais échouer/réussir explicitement chez Bictorys) peut ne déclencher aucun webhook du tout** — la ligne reste alors `'pending'` indéfiniment, indistincte d'un paiement en cours. Si l'insert lui-même échoue (ligne 130-133), c'est avalé en `console.error` seul, « on ne bloque pas le paiement » — aucune remontée Sentry sur ce chemin.

**2. Ce que reçoit le webhook sur échec, et ce qu'il en fait** : `src/app/api/webhooks/bictorys/route.ts:277-284`. `payload.status` différent de `succeed`/`succeeded`/`authorized` → si une ligne `payments` existe déjà (`existingByCharge` ou `existingByOrder`), elle passe à `status:'failed'` ; sinon rien. `console.log` seul, retour `{ok:true}`. **Aucune notification au client, au marchand, ni à la plateforme sur ce chemin.**

**3. Combien de commandes en ligne ne sont jamais abouties, par mois et boutique, depuis le lancement** — mesuré le 2026-08-11, `payment_type IN (online_full,online_deposit)` et (`status='pending'` OU `cancellation_reason='expired'`) :

| Mois | Commandes | Montant (FCFA) |
|---|---|---|
| 2026-05 | 10 | 1 211 250 |
| 2026-06 | 75 | 1 647 145 |
| 2026-07 | 63 | 789 050 |
| 2026-08 (partiel) | 13 | 168 150 |
| **Total** | **161** | **3 815 595** |

**34 boutiques distinctes concernées.** Ce total **inclut la boutique de test de l'utilisateur** (« Viens on s'connaît », `12d6a93e…` : 13 commandes, 149 400 FCFA) — à retirer pour une mesure « marchands réels » (161→148 commandes, ≈3 666 195 FCFA). Boutiques les plus touchées en montant : Supreme Business (3 commandes, 1 770 000 — un seul montant énorme, à vérifier isolément avant de le traiter comme représentatif), Le Bazar d'Ibuka (8, 439 000), Auto école khadija (3, 225 000), DermaSecret (10, 168 000).
**Mesure basse, pas un total** : identique à la réserve du §6 ci-dessus — un paiement dont le webhook n'atteint jamais le serveur ne laisse pas plus de trace ici. Inclut aussi les commandes réellement encore en cours au moment de la mesure (moins de 24h), pas seulement des échecs consommés.

**Le dénominateur, mesuré le 2026-08-11 : sur les 239 commandes en ligne au total (population identique à hier, confirmée stable), seulement 37 ont un paiement `payments.status='completed'` enregistré — 15,5 %.** Le résiduel exact recoupe le tableau ci-dessus (161 = pending + expired) plus 41 commandes supplémentaires jamais réconciliées proprement : 22 annulées pour une autre raison, et **19 qui ont quand même avancé (`confirmed` → `delivered`) sans paiement tracé, 554 800 FCFA** — vraisemblablement payées (un marchand ne livre pas gratuitement), en cours de recoupement par l'utilisateur avec ses relevés Bictorys, comme pour les 6 initiales. **Tant que ce recoupement n'est pas revenu, le taux d'aboutissement s'exprime en fourchette : entre 15,5 % (aucune des 19 n'a été payée) et 23,4 % (les 19 l'ont toutes été) — jamais comme une valeur unique.** Hors boutique de test : 210 commandes réelles, 12,4 % à 23,3 % selon le même recoupement.
Détail des 19, transmis à l'utilisateur en conversation le 2026-08-11 pour réconciliation — inclut 2 commandes Social technologie supplémentaires (`c2dbae68…`, `ab12962a…`) non signalées lors du premier passage sur cette boutique, à vérifier avec les deux déjà closes.

Répartition par méthode de paiement : **non disponible plus finement que Bictorys/Stripe.** `orders.payment_method` est `NULL` sur les 239 commandes en ligne sans exception (le webhook Bictorys ne le renseigne jamais). Seul `payments.payment_method` distingue quelque chose : 37/37 paiements complétés sont `bictorys`, 0 `stripe_card` — aucune granularité Wave/Orange Money/Moov nulle part dans le schéma.

**Supreme Business vérifié, pas une saisie aberrante** : 4 commandes en ligne (une de plus que le premier passage, celle-ci avec paiement complété — 350 000 FCFA non comptés dans le tableau ci-dessus). Produits : MacBook Pro 2020 à 350 000 FCFA, iPhone 17 Pro 128 Go à 710 000 FCFA — prix réels et plausibles au Sénégal pour ces produits, boutique d'électronique haut de gamme où un échec de paiement en ligne coûte particulièrement cher.

**4. Devenir d'une commande abandonnée** — `supabase/migrations/059_order_expiry_24h.sql` : `expire_pending_orders()`, cron quotidien `0 9 * * *` (`src/app/api/cron/expire-pending/route.ts`) → `status='cancelled'`, `cancellation_reason='expired'` si `status='pending'`, `deposit_paid=false`, `payment_type` en ligne, `created_at` de plus de 24h. Délai réel entre 24h et ~48h selon l'heure de création, le cron ne passant qu'une fois par jour.
**Stock : jamais restitué.** `expire_pending_orders()` ne touche que `orders` — aucun appel à `increment_product_stock`/`increment_variant_stock`. Ces RPC n'existent que dans `rollbackStock()` (`src/app/api/orders/route.ts:276-294`), déclenchée uniquement en cas d'échec **dans la même requête** de création de commande (une décrémentation ultérieure qui échoue), jamais à l'expiration différée.
**Conséquence concrète, vérifiée, pas seulement théorique** : toute commande en ligne abandonnée immobilise son stock et, si un code promo était appliqué, son usage, pendant 24 à 48h puis définitivement au-delà.

**Chiffré le 2026-08-11 — c'est le point qui coûte le plus cher, à la minute où il est mesuré.** Sur les 180 commandes en ligne définitivement mortes (157 expirées + 23 annulées manuellement — `cancelOrder`, `src/lib/actions/orders.ts:132-142`, ne restitue pas non plus le stock ni le code promo, exactement comme le cron), 198 lignes d'articles, **72 produits/variantes distincts, 218 unités de stock immobilisées.** 36 sont à stock illimité (non affectées), 32 vérifiables en stock limité, 2 produits depuis supprimés (non vérifiables).
**Deux produits actifs de vrais marchands sont actuellement affichés en rupture (stock 0) à cause de ce seul mécanisme** : « Formule Standard » (Auto école khadija, 3 unités immobilisées) et « MacBook Pro 2020 » (Supreme Business, 2 unités). Trois autres produits dans le même cas : trois sur la boutique de test, un chez ABI&CO mais désactivé (`is_active=false`, sans impact commercial actuel).
**Vérifié avant toute action** : sur ces 180 commandes, 98 lignes `payments` existent — 1 `completed` (la commande test annulée après paiement, voir point ouvert ci-dessous), 28 `failed`, **69 encore `pending`**. Une ligne `payments` restée `pending` sur une commande déjà `cancelled` ne peut plus faire progresser la commande elle-même (`status='cancelled'` est terminal dans `STATUS_TRANSITIONS`, `src/lib/actions/orders.ts:29-37` — aucune transition sortante), donc libérer le stock ne crée pas de risque de survente par ce chemin, même si un webhook tardif venait un jour compléter un de ces paiements en attente. Le seul cas déjà observé où un paiement a fini par aboutir sur une commande annulée (`6d10e926…`) l'a fait sans jamais faire bouger le statut de la commande — cohérent avec cette lecture.
**Migration `091_release_orphaned_stock_reservations.sql` écrite, non appliquée, corrigée le 2026-08-11 après trois observations de l'utilisateur** : seuil figé à l'instant exact de la mesure (`2026-08-11T11:43:14.600036+00`, pas une date postérieure) plutôt qu'au 12 comme la première version l'écrivait par erreur ; **1 commande exclue** car un paiement `completed` existe (`6d10e926…`, boutique de test, 14 000 FCFA — son stock a été consommé légitimement) ; preuve écrite que `rollbackStock()` ne peut structurellement pas avoir produit une des 180 commandes (la Phase 1 de `src/app/api/orders/route.ts` retourne 409 avant l'`.insert()` sur `orders` — les deux chemins sont mutuellement exclusifs par construction, pas par exclusion ajoutée) ; le fichier capture et rapporte maintenant un delta réel de `stock_count` nommé par produit, pas seulement un nombre de lignes traitées. **Ordre obligatoire inscrit dans le fichier** : `091` doit s'appliquer avant tout correctif de la cause sur `expire_pending_orders()`/`cancelOrder()`, sous peine de double crédit sur les commandes charnière.
**Trou variantes chiffré séparément** (le script de contrôle ne couvre que le stock simple) : 10 variantes distinctes, 3 boutiques, 14 unités immobilisées sur la même population — vérifié qu'aucune de ces 10 n'est aujourd'hui à un stock affiché ≤ 0, le trou est réel mais ne bloque aucune vente à ce jour.
`orders.promo_id UUID REFERENCES promo_codes(id)` ajouté au schéma de `090` (non encore appliquée) pour que la correction de cause puisse un jour appeler `release_promo_code` — un code texte ne désigne pas une ligne stable (codes supprimés/recréés, hypothèse déjà avancée pour l'écart `used_count`).
**Appliquées le 2026-08-12, dans l'ordre, via le chemin hors séquence de `AI_RULES.md` (`db query -f` puis `migration repair`, jamais l'un sans l'autre).**
- **`090`** : 5 colonnes ajoutées sur `orders` (`promo_id`, `promo_code`, `promo_discount_pct`, `discount_amount`, `deposit_percentage`), 1 sur `order_items` (`quantity_discount_pct`). Backfill : **14 commandes** — identique au chiffre du 2026-08-09, confirmé par comptage direct (`discount_amount > 0`) plutôt que par le `RAISE NOTICE` du fichier, **illisible depuis cet outil** (le mode de connexion de `supabase db query` ne restitue pas les messages `NOTICE`, seulement les lignes de résultat — découvert en testant, à garder en tête pour toute migration future qui compte dessus pour son rapport). Invariant vérifié sur la totalité de `orders` après coup : 0 écart.
- **`091`, élargie** : exécutée une seule fois (vérifié avant via `schema_migrations` qu'elle ne l'avait pas déjà été). Avant/après capturés par des requêtes séparées (même raison : `NOTICE` invisible). **51 produits à stock simple concernés, 51 avec un delta réel**, dont nommément : `MacBook Pro 2020 : 0 -> 2`, `Formule Standard : 0 -> 3`, `Pour Les Amoureux : 0 -> 4`, `Pour Les Familles : 0 -> 3`, `Pour les Couples : 0 -> 2`, `Pour Les Amis : 0 -> 1`, `PORTE-MONNAIE MIXTE DARK : 0 -> 3`, `PORTE-MONNAIE MIXTE MARRON : 0 -> 2`. Les deux produits de marchands réels signalés bloqués (Auto école khadija, Supreme Business) confirmés débloqués.
- **Contrôle après coup** : `check-order-totals-invariant.sql` et `check-stock-not-orphaned.sql` exécutés, aucune erreur — **revérifié indépendamment des scripts eux-mêmes** par deux requêtes directes (0 commande à invariant cassé, 0 produit encore faussement en rupture).
- **Enregistrement confirmé** : `supabase migration list --linked` montre `090` et `091` alignées Local=Remote, pas seulement exécutées.
- **Reste ouvert** : le trou variantes (10 variantes/3 boutiques/14 unités sur la population en ligne, non re-mesuré sur la population élargie) n'est pas couvert par `check-stock-not-orphaned.sql` — inchangé depuis le chiffrage du 2026-08-11.

**Contrainte d'ordre de la 091 levée le 2026-08-12 : elle est appliquée.** Le correctif de cause sur `expire_pending_orders()` et `cancelOrder()` (§4 point 4, restitution du stock et libération du code promo à l'expiration/l'annulation, jamais construite jusqu'ici) **peut désormais partir sans risque de double crédit** — la population qu'il traiterait à l'avenir (commandes qui passeront `cancelled` après le seuil figé de `091`, `2026-08-11T11:43:14.600036+00`) ne recoupe plus rien de ce que `091` a déjà traité. Pas de prérequis à attendre sur ce point.

**Code promo : jamais libéré**, même mécanisme — `release_promo_code` n'est appelé que dans ce même `rollbackStock()`, jamais par le cron d'expiration. **Recoupement avec l'écart déjà noté au §3 (`used_count`=7 vs 14 commandes à remise réelle)** : une partie de cet écart est peut-être structurelle plutôt qu'une donnée perdue — un code réservé sur une commande qui expire reste compté « utilisé » sans jamais avoir servi. Hypothèse à vérifier, pas confirmée. **Vérifié le 2026-08-11 : aucun code promo d'un marchand réel n'est aujourd'hui bloqué par son plafond.** 4 codes ont un `used_count > 0`, dont 2 au plafond exact (`SOSO80` 2/2, `SORAYA95` 2/2) — les deux appartiennent à la boutique de test. `GLOW26` (Glow Eternel, marchand réel) est à 1/50. **Aucun lien order↔code promo stocké nulle part** (confirmé) : impossible de dire combien des usages comptés correspondent précisément à des commandes mortes, seulement de constater qu'aucun plafond réel n'est actuellement franchi.

**Point ouvert, non investigué, à ne pas traiter maintenant (2026-08-12) : que devient l'argent quand un marchand annule une commande déjà payée ?** Découvert via `6d10e926…` (boutique de test, paiement `completed` 14 000 FCFA, annulée par le marchand sans raison enregistrée) — la question vaut pour toutes les boutiques, pas seulement ce cas. Reste-t-il crédité au solde reversable du marchand ? Existe-t-il un chemin de remboursement vers l'acheteur, ou aucun ? Non vérifié.

**5. Chemin de reprise de paiement existant** : oui, déjà en place. `/{slug}/commander/pay?order_id=...&token=...` (`src/app/[shop-slug]/commander/pay/page.tsx`) prend la commande par identifiant + jeton et affiche à nouveau le sélecteur de méthode de paiement — c'est déjà la page vers laquelle pointe le lien « Réessayer le paiement » affiché après une annulation immédiate (ligne 36-40). **Le mécanisme existe, rien ne le renvoie après coup** : aucun rappel (WhatsApp, e-mail) ne redirige vers ce lien pour une commande abandonnée plus tôt que ça — c'est l'écart entre « le chemin existe » et « l'acheteur y est ramené ».

**Trois pistes de l'utilisateur, à garder en tête sans rien construire** : rendre l'échec visible au marchand avec le numéro du client, pour relance WhatsApp ; envoyer à l'acheteur un rappel avec le lien de reprise déjà existant ci-dessus ; revoir l'écran de choix de méthode, une partie des échecs venant d'un choix fait sans information suffisante.

**Asymétrie `payments`/Stripe — résolue le 2026-08-10, ce n'est pas un défaut.** `src/lib/payments/stripe.ts:181` : `payment_intent_data: { transfer_data: { destination: payload.merchantAccountId } }` — paiement carte en Stripe Connect à destination directe. L'argent va **directement sur le compte Stripe du marchand**, ne transite jamais par la plateforme : structurellement équivalent au cas `bictorys_secret_key` (§4 point 3) — rien à reverser, donc l'absence de ligne `payments` est cohérente, pas une fuite. **Vérifié en base : 0 boutique avec `stripe_connect_enabled=true`, 0 commande `payment_method='stripe_card'`** — ce chemin n'a jamais servi en production, l'asymétrie est aujourd'hui sans effet réel. **Point à garder en évidence, décision commerciale à prendre par l'utilisateur avant d'activer ce chemin, pas après (2026-08-11)** : `payment_intent_data` ne porte pas d'`application_fee_amount` — le jour où `stripe_connect_enabled` s'active pour une boutique, la plateforme ne prélève rien sur ses paiements par carte. Ce n'est pas un bug, c'est le pendant Europe/Canada de la règle de commission du §4 point 3 (3 % par défaut sur tout ce qui transite par la plateforme, 0 % uniquement quand l'argent ne transite jamais par elle) — sauf qu'ici, contrairement à `bictorys_secret_key`, rien n'implémente encore la perception d'un taux avant que le chemin ne soit ouvert à un marchand. À trancher avant la première activation, pas en la découvrant après coup. La page « Revenus » (`revenues/page.tsx:66-70`) est calculée exclusivement sur `payments` — cohérent avec ce qui précède, mais un marchand utilisant Stripe Connect ne verrait aucune de ces ventes sur cette page ; à documenter côté produit plutôt que corriger, le jour où ce chemin s'active.

**Ce qui a permis à ces commandes d'avancer sans paiement tracé** : `advanceOrderStatus` (`src/lib/actions/orders.ts:39-76`), l'action marchand de progression de statut, **ne vérifie aucun paiement** — seulement `isOrderBlocked`. Le bouton correspondant n'est masqué côté UI que si `isDigitalOrder` (`src/app/dashboard/orders/[id]/page.tsx:122-130`), calculé en **live** depuis `products.product_type` *actuel*, pas depuis un instantané pris à la commande — un produit retypé après coup rend ce garde-fou rétroactivement inopérant pour les commandes passées.

**Décision de l'utilisateur sur `advanceOrderStatus`, 2026-08-10 : ne pas bloquer la transition** — un marchand payé par un autre canal doit pouvoir faire avancer sa commande. **Ce qui manque n'est pas le garde-fou, c'est la trace.** Design décidé, non construit : la transition d'une commande à paiement en ligne sans paiement enregistré doit demander une **confirmation explicite** au marchand (texte expliquant ce qui manque), et laisser une **trace horodatée de qui l'a confirmée**. Sans quoi le cas K.B.Y. et l'incident d'origine restent indistinguables après coup.

**Points ouverts, non construits maintenant :**
- Remplacer le garde `.eq('status','pending')` par une confirmation idempotente sur l'identifiant de commande (pas sur son statut courant), pour que `advanceOrderStatus` ne puisse plus faire manquer la fenêtre de confirmation webhook.
- Sur tout échec inattendu de ce chemin, remonter à Sentry et renvoyer autre chose qu'un `200`, pour que Bictorys rejoue au lieu de considérer le webhook traité.
- `advanceOrderStatus` : confirmation explicite + trace horodatée décrites ci-dessus, à construire.
- `isDigitalOrder` doit se calculer depuis un instantané pris à la commande, pas depuis `product_type` en direct — même logique que `variant_name` figé à la commande (SPEC-v2 §3).
- Garde `resend-digital` : fermer le contournement (cas K.B.Y.) sans recours self-service pour un incident identique — acceptable, à condition qu'un chemin support existe : une action admin tracée, avec l'identité de qui la déclenche et pourquoi. L'asymétrie Stripe étant résolue (ci-dessus), le signal `payments.status='completed'` redevient utilisable tel quel pour Bictorys ; pour Stripe, la question ne se pose pas aujourd'hui (0 commande) mais devra être reposée si ce chemin s'active.

Prérequis explicite pour le Lot D de la spec (reprise du relevé complet sur confirmation/suivi/e-mail) : la migration de décomposition du §3 doit être appliquée avant — sans elle, ce lot n'a rien à afficher.

**Circuit de l'argent, définitif — répond à la « question bloquante » de `ADDITIF-argent-commandes-retenues.md`, confirmé par l'utilisateur le 2026-08-10, ne plus reposer la question.** L'acheteur paie via une méthode Bictorys ; l'argent arrive sur **le compte Bictorys de TEKKIShop** (Cas A de l'additif — TEKKIShop contrôle les règlements) ; le marchand voit la somme collectée sur sa page « Revenus », déduction faite de la commission ; depuis cette page il demande un retrait, ce qui appelle l'API Payout de Bictorys et verse la somme sur le numéro mobile money renseigné dans ses paramètres. **Seules les boutiques ayant configuré leurs propres clés Bictorys encaissent en direct et ne passent jamais par ce circuit.** Conséquence directe : le chemin A de l'additif s'applique intégralement (blocage réel des fonds possible sur une boutique `expired`), le chemin B ne s'applique pas.

---

## 7. Chiffres mesurés, avec leur date

Mesurés le 2026-08-09/10, cette session, sauf mention contraire :

- **Favicon en production** : `https://www.tekki.shop/favicon.png` pèse **834,4 Kio** (853 708 octets), confirmé par inspection réseau Lighthouse — pas le fichier local `public/favicon.png` (269 Kio), pas encore déployé.
- **Reversements (`payouts`)** : les **15 lignes historiques** en base sont **toutes à 3 %** de commission, **y compris les 3 lignes appartenant à des boutiques `plan='pro'`** (690, 540, 1 883 FCFA). **C'est le comportement correct** (voir §4 point 3, sens corrigé le 2026-08-10) : ces 3 boutiques Pro n'ont pas configuré leurs propres clés Bictorys (§7 ci-dessous, « 3 boutiques Pro sans clé propre »), donc leurs paiements transitent par la plateforme et doivent 3 % comme n'importe quelle autre boutique — **aucune perte, aucun remboursement à faire**. (Une correction précédente de cette phrase, écrite plus tôt dans la même session, affirmait l'inverse — c'était une erreur, corrigée ici avant qu'elle ne s'installe.)
- **Boutiques proches ou au-delà de la limite de produits — état final au 2026-08-13, deux listes distinctes, deux messages différents.** Portée du plafond : `plan='decouverte'` (10 produits, 97 boutiques) **et** `plan='trial'` (10 produits aussi via `normalizePlan()`, 1 375 boutiques — les boutiques legacy en essai avant tout paiement, voir commentaire dans `plan-features.ts`). `business`/`pro` sont illimités, non concernés. **Aucun nom de marchand disponible dans les deux listes** : `profiles.first_name`/`last_name` est `NULL` pour les 19, seuls le nom de boutique et le téléphone existent en base.

  **Liste 1 — message d'annonce (déjà au plafond ou au-delà, 14 boutiques)** : leurs produits actifs restent tous visibles, elles ne peuvent plus en ajouter ni en réactiver.

  | Produits actifs | Boutique | Plan | Téléphone | Dernier produit | Dernière commande |
  |---|---|---|---|---|---|
  | 17 | EXCELLENT'B FASHION | decouverte | +229 68 21 67 43 | 2026-08-12 | 2026-08-11 |
  | 17 | DENERY PRO AUDIO | decouverte | +225 05 74 31 48 77 | 2026-06-16 | 2026-06-16 |
  | 17 | Le Coin Frais | decouverte | +221 77 371 52 54 | 2026-05-30 | 2026-05-21 |
  | 16 | La Vitrine Togo | decouverte | +228 71 16 07 07 | 2026-06-20 | 2026-07-11 |
  | 14 | AK GROUP HOLDING | trial | +226 77 17 76 36 | 2026-08-03 | jamais |
  | 14 | Diak boutique | trial | +225 07 67 95 90 25 | 2026-06-10 | jamais |
  | 14 | Yombelé boutique | decouverte | +221 77 462 66 49 | 2026-06-19 | 2026-06-18 |
  | 14 | Oumoushka_Collection | decouverte | +221 77 814 86 64 | 2026-06-22 | jamais |
  | 13 | Lamaindorproduits | decouverte | +221 77 255 58 84 | 2026-06-28 | jamais |
  | 12 | Madany.nails | trial | +221 01 01 03 65 30 | 2026-06-12 | jamais |
  | 12 | Elie Cosmétiques | decouverte | +225 05 66 40 15 00 | 2026-06-08 | jamais |
  | 12 | K.B.Y multi services | decouverte | +225 07 06 24 77 34 | 2026-07-28 | 2026-07-30 |
  | 11 | Khadidiatou parfumerie | decouverte | +221 77 563 32 41 | 2026-06-07 | jamais |
  | 11 | LONGRICH TEAM ASCENSION D | trial | +225 07 08 43 35 28 | 2026-06-08 | jamais |

  **Liste 2 — message préventif (8 à 10 produits, pas encore bloquées, 5 boutiques)** : information avant restriction, pas annonce.

  | Produits actifs | Boutique | Plan | Téléphone | Dernier produit | Dernière commande |
  |---|---|---|---|---|---|
  | 10 | Avenue 12 Shop | decouverte | +225 05 56 26 40 91 | 2026-07-10 | 2026-07-01 |
  | 9 | Atlas Glousse | trial | +225 05 03 65 70 58 | 2026-08-12 | jamais |
  | 9 | Ophir Univers | trial | +225 01 60 74 74 11 | 2026-07-19 | jamais |
  | 9 | TOUTICI | decouverte | +225 05 05 02 78 81 | 2026-07-28 | jamais |
  | 8 | Supermarché Agricole CI | decouverte | +225 07 16 42 51 92 | 2026-07-26 | 2026-07-26 |

  **19 boutiques au total, chiffres à remesurer avant tout envoi — déjà bougés deux fois depuis le 2026-08-09.**
- **Boutiques Pro** : **3 boutiques** sans clé Bictorys propre (`bictorys_secret_key IS NULL`) — pertinent pour la règle de commission du §4.
- **Audit sécurité** : zéro preuve d'exploitation sur les deux angles vérifiés. Angle 1 — boutiques `trial_model='free_orders'` actives (`status='active'` ou `is_active=true`) sans événement `shop_events.event_name='plan_activated'` correspondant : 0 résultat. Angle 2 — commandes avec `released_at` renseigné sans paiement `completed` associé : 0 résultat.
- **Décomposition des commandes (§3)** : 514 commandes au total, 14 à écart de remise non nul, 54 325 FCFA concernés, invariant vérifié sans exception.

**Mesures de référence LCP/poids, 9 août 2026, avant toute modification de rendu :**

| | ABI&CO | Glow Eternel |
|---|---|---|
| LCP | 10,6 s | 9,1 s |
| Poids total | 1 620 Kio | 1 615 Kio |
| Other | 840,6 Kio | 844,2 Kio |
| Script | 307,6 Kio | 307,6 Kio |
| Image | 315,7 Kio | 302,7 Kio |
| Font | 117,4 Kio | 117,6 Kio |

Budget visé au §10.1 de `AI_RULES.md` : **2,5 s**. Les deux boutiques sont donc à environ **quatre fois le budget**.

**Cause principale identifiée** : le favicon (834,4 Kio, voir ci-dessus), servi sur chaque page indépendamment de la boutique. Le fichier réellement servi correspond à `public/old-favicon.png` (853 708 octets sur disque) ; `public/favicon.png` (269 Kio) existe localement mais n'est pas déployé.

`Other` et `Script` sont quasi identiques à l'octet près entre les deux boutiques, dont les catalogues diffèrent : c'est une **charge fixe**, pas du contenu marchand — cohérent avec le favicon comme cause principale.

**Ces chiffres seront caducs dès que le sous-lot 2a touchera `ProductGrid.tsx`** (partagé par l'accueil et la fiche produit). Nouvelle mesure de référence à prendre juste après le correctif favicon (§4, point 2) — c'est celle-là, pas celle-ci, qui fera foi pour le §10.1.

---

## 8. Hygiène documentaire — points ouverts, non traités

**Règles critiques dupliquées.** `AI_RULES.md` reproduit intégralement le contenu de `NOTE_MASQUAGE_COMMANDES.md` (§4) et de `NOTE_ERREURS_SUPABASE_NON_VERIFIEES.md` (§3, "Vérification des erreurs"), au lieu d'y renvoyer. Deux copies d'une même règle divergent tôt ou tard. À trancher : une source unique (probablement les `NOTE_*.md`, plus détaillées, avec le contexte et l'exemple) et un renvoi depuis `AI_RULES.md`, pas l'inverse — mais c'est une décision à prendre, pas à exécuter seul.

**`IMPROVEMENTS.md` chevauche `REPRISE.md`.** Statuts ✅🔄⬜🚫 sur bugs/UX/fonctionnalités, plus ancien format, fonction proche de ce que `REPRISE.md` fait maintenant plus précisément (avec dates, fichiers, lignes). À fusionner — probablement absorber ce qui est encore pertinent dans `REPRISE.md` et retirer `IMPROVEMENTS.md`, mais non vérifié ligne à ligne, donc non tranché ici. Fichier laissé non suivi en attendant (voir §1).

**`README.md` reste le boilerplate `create-next-app`** — aucune information sur TEKKIShop, aucune instruction spécifique au projet. Suivi tel quel (§1) pour qu'il ne redisparaisse pas, mais son contenu est à réécrire entièrement : ce que fait le projet, comment le lancer avec les variables d'environnement réelles (voir `AI_RULES.md` §10), où trouver `AI_RULES.md`/`REPRISE.md`.

---

## 9. Couverture pays — ouvert le 2026-08-12, hors périmètre de la livraison 1

Trois listes de pays coexistent, non alignées : liste officielle `/start` (6 Afrique + 5 Europe/Canada), liste historique `/onboarding`/Réglages (17, dont CM/GN/CD/GA/MG/MA en plus), détection automatique par ville (12). Détail complet et chiffres par pays dans l'échange du 2026-08-12 — non reproduit ici en entier, à redemander si besoin plutôt que résumé une deuxième fois.

**`BF`/`BK` — même pays, deux codes.** ~~À unifier après la livraison 1, pas avant.~~ **Fait le 2026-08-16, en avance sur ce plan — voir §4.30.** Ce qui suit reste comme trace de l'état découvert le 2026-08-12, dépassé depuis. Le Burkina Faso existait sous deux codes distincts dans `shops.country` (`BF`, code ISO, utilisé par le nouveau `/start` ; `BK`, code Bictorys, hérité de l'ancien flux) — documenté dans le code lui-même (`src/lib/utils/country-groups.ts:11-20`) et confirmé en base : **49 boutiques en `BF`, 76 en `BK`, soit 125 au total.** Conséquence directe : le Burkina Faso était en réalité le **troisième marché** de la plateforme (125), pas le Bénin (124) comme l'aurait laissé croire un comptage naïf sur `BF` seul ou sur `BK` seul. **Tout chiffre par pays produit avant cette unification est à relire à la lumière de ce split** — et depuis le 2026-08-16, tout chiffre par pays doit compter sur `BK` uniquement.

**Les huit marchands hors couverture (5 `CM`, 2 `CD`, 1 `MA`) — reformulation du problème, apportée par l'utilisateur le 2026-08-12, à ne pas investiguer maintenant.** Ce ne sont pas des marchands bloqués par le tunnel d'achat de leurs clients : ils se sont inscrits avec un indicatif téléphonique sénégalais ou ivoirien puis ont changé le pays dans leurs réglages ensuite. Ils n'ont jamais eu besoin du paiement en ligne côté acheteur — vitrine + paiement à la livraison, usage très répandu dans leur marché. **Ce qui les bloque réellement : ils ne peuvent pas payer leur propre abonnement à TEKKIShop**, faute de devise et de moyen de paiement supportés pour leur pays. Bictorys ne couvre pas encore ces pays, et l'intégration d'un second agrégateur est un chantier long depuis le Sénégal. **Question ouverte qui en découle, non investiguée** : peut-on encaisser un abonnement dans ces pays par un autre moyen que Bictorys (carte bancaire via Stripe, par exemple, déjà en place pour l'Europe/Canada) ?

**Europe/Canada** : codé (Stripe Connect, devise EUR/CAD) mais jamais exercé en conditions réelles — **0 des 8 boutiques EU/CA (7 FR + 1 draft) n'a `stripe_connect_enabled = true`**. BE/LU/CH/CA n'ont aucune boutique à ce jour.

**Résidus `FCFA` en dur dans le tunnel** : `src/app/[shop-slug]/commander/checkout/BictorysCheckout.tsx:139` (utilisé) et `OrangeMoneyCheckout.tsx:152` (mort, importé nulle part — à confirmer avant suppression, ne pas nettoyer en marge d'une autre tâche).

---

## 10. Boutiques sans aucune vente — ouvert le 2026-08-13, distinct du plafond produits

Déclenché en creusant la liste des 14 boutiques au plafond produits (§9) : l'utilisateur avait noté que « onze des quatorze n'ont jamais reçu de commande » avant d'écrire ses messages. **Vérifié faux avant d'aller plus loin** : sur les 14, **6 ont déjà reçu au moins une commande** (EXCELLENT'B FASHION, DENERY PRO AUDIO, Le Coin Frais, La Vitrine Togo, Yombelé boutique, K.B.Y multi services — dates dans le tableau du §9), **8 n'en ont jamais reçu**. La donnée était déjà transmise dans la conversation, pas retrouvée par une nouvelle mesure.

**Ampleur réelle, mesurée sur toute la plateforme** : sur les boutiques ayant au moins un produit actif (969), **902 n'ont jamais reçu de commande — 93,1 %.**

**Corrélation produits/ventes — dans le sens inverse de l'hypothèse initiale** :

| Tranche de produits actifs | Boutiques | Ont vendu au moins une fois | % |
|---|---|---|---|
| 1-3 | 889 | 29 | 3,3 % |
| 4-10 | 49 | 21 | 42,9 % |
| 11+ | 31 | 17 | 54,8 % |

Plus une boutique a de produits, plus elle a de chances d'avoir vendu — les 14 boutiques du §9 sont donc l'exception dans leur propre tranche (11+), pas la confirmation d'un problème de catalogue trop lourd.

**Le 93,1 % affiné le 2026-08-13, à utiliser à la place du chiffre brut.** Sur les 902 boutiques jamais vendues, **743 (82,4 %) sont déjà invisibles** (`shops.is_active=false` — essai `legacy` expiré, jamais payé, mécanisme ci-dessous) : leur absence de vente est peut-être une conséquence de ne plus être visibles, pas la cause première. **Seules 159 boutiques sont à la fois visibles aujourd'hui et n'ont toujours pas vendu**, sur 190 boutiques visibles avec au moins un produit actif — **83,7 %**. C'est ce chiffre-là qui décrit une boutique vivante qui ne vend pas, pas 93,1 % qui mélange ça avec des boutiques mortes.

**Taux de vente par ancienneté** (boutiques avec produit actif, indépendamment de leur visibilité) :

| Ancienneté | Boutiques | Ont vendu | % |
|---|---|---|---|
| Moins de 7 jours | 25 | 1 | 4,0 % |
| 8 à 30 jours | 118 | 9 | 7,6 % |
| 31 à 90 jours | 821 | 55 | 6,7 % |
| Plus de 90 jours | 5 | 2 | 40,0 % |

Répartition inattendue : 821 boutiques sur 969 (84,7 %) tombent dans la tranche 31-90 jours, quasiment aucune au-delà — la plateforme concentre son catalogue actif sur une fenêtre de 1 à 3 mois, pas étalée dans le temps. Le taux de vente reste plat autour de 4-8 % entre 7 et 90 jours — ne s'améliore pas nettement avec l'âge sur cette plage. La tranche « plus de 90 jours » (40 %) repose sur seulement 5 boutiques, pas assez pour conclure.

**Mécanisme de fin d'essai `legacy`, confirmé (`src/app/api/cron/trial-expiry/route.ts`)** : cron quotidien, `plan='trial' AND is_active=true AND trial_model='legacy' AND trial_ends_at < maintenant` → **`is_active=false`**, un message WhatsApp avec lien `/dashboard/upgrade`. Pas de commandes retenues, pas de dégradation progressive comme le modèle `free_orders` — la boutique disparaît entièrement. **1 098 boutiques `legacy` sont déjà dans cet état aujourd'hui — répartition détaillée (produit actif à l'extinction, ventes préalables, chronologie) en §11.**

---

## 11. Le modèle `free_orders` est construit mais quasiment jamais atteint — ouvert le 2026-08-13, corrigé le 2026-08-13

**Correction du 2026-08-13 : ce n'est pas une fuite à réparer.** La lecture initiale de cette section partait d'une prémisse fausse : que `/start` v4 (commit `c66503c`, 6 août) était déjà en production. **Vérifié faux à l'époque** : `c66503c` était ancêtre de `refonte-start`, absent de `main`. Rien à corriger avant le déploiement : c'était l'état normal de l'ancien monde, pas une régression. **Dépassé depuis le 2026-08-17 : `/start` v4 et la landing v6 sont fusionnés sur `main` et en production — voir §21.**

**Découverte en creusant la correction — plus précise que « tout passe par `/onboarding` » :** `main` a son propre `/start`, une **version antérieure (v3)**, en production depuis le **16 juillet** (commit `dcd1808`, trois semaines avant même l'existence du modèle `free_orders`). La landing actuellement en production y renvoie directement (`src/app/page.tsx` sur `main`, lignes 169 et 391) — c'est déjà le CTA principal, pas `/onboarding`. Son action serveur (`signUpFromStart`, `main:src/app/start/actions.ts`) crée le compte et la boutique en une seule opération mais **n'écrit jamais `trial_model`** → défaut de colonne `'legacy'` (migration `072`). Donc **deux parcours distincts produisent déjà des boutiques `legacy` en production aujourd'hui, indépendamment l'un de l'autre et indépendants du travail de `refonte-start`** : `/onboarding` (filet de sécurité générique) et `/start` v3 (CTA principal actuel, live depuis juillet). Les « 54 boutiques legacy depuis le 6 août » ne sont donc pas nécessairement passées par `/onboarding` — la majorité a plus probablement transité par ce `/start` v3, déjà en place. Au déploiement de la Livraison 1, `refonte-start` remplace intégralement les fichiers de la route `/start` (`page.tsx`, `StartFlow.tsx`, `actions.ts`, `data.ts`, `layout.tsx` diffèrent tous entre les deux branches, même chemin) — la v3 legacy disparaît d'un coup, remplacée par la v4 `free_orders`, sans risque de double parcours sur cette route précise.

« Nala Store » (la seule boutique `free_orders`, créée le 7 août en `draft`, jamais terminée, aucun `phone_whatsapp`) est très probablement un test de préversion de l'utilisateur sur `dev`/`refonte-start`, pas un mystère à élucider.

**Faits toujours valides malgré la correction ci-dessus** :
- `shop_visits` est **vide sur toute la plateforme — 0 ligne, 0 boutique**. Cause : `VisitBeacon` (`[shop-slug]/page.tsx:87,132`) ne se monte que si `shop.trial_model === 'free_orders'` — une seule boutique sur 1 543 qualifie. Le compteur de visites ne s'exécute jamais pour une boutique `legacy`, quel que soit son trafic réel : zéro n'est pas une mesure de zéro visiteur, c'est l'absence structurelle de toute mesure pour cette population. Reste vrai après déploiement pour les 1 542 boutiques `legacy` existantes (voir Question 2 ci-dessous) — seules les boutiques créées après coup via le nouveau `/start` seront mesurées.
- Les deux chiffres qui décrivent l'état réel du produit selon l'utilisateur restent : le taux d'aboutissement des paiements en ligne (12 %, §6) et le taux de boutiques sans aucune vente (93,1 % brut / 83,7 % affiné, §10).

---

**Question 1 — Après le déploiement, `/onboarding` reste-t-il atteignable par un marchand sans boutique ?**

Tous les chemins vers `/onboarding` trouvés dans `src/` (recherche exhaustive `'/onboarding'`, guillemets simples et doubles) :

| Origine | Condition de déclenchement |
|---|---|
| `middleware.ts:198-204` | `owner` authentifié sur toute route `/dashboard/*`, sans `shop_id` **ou** sans `onboarding_completed` |
| `dashboard/page.tsx:43`, `dashboard/layout.tsx:34,43`, `dashboard/products/page.tsx:27`, `dashboard/products/[id]/page.tsx:29`, `dashboard/orders/page.tsx:68`, `dashboard/orders/[id]/page.tsx:83` | même condition, redite au niveau page (filet redondant avec le middleware) |
| `onboarding/setup/page.tsx:23` | redirige vers `/onboarding` lui-même si `!shop_id` — boucle interne à l'assistant, pas une nouvelle entrée |
| `HeroInput.tsx:18` (défaut `redirectTo='/onboarding'`) | son seul appelant, `CountryLandingPage.tsx:527`, surcharge déjà en `redirectTo="/start"` — le défaut n'est jamais atteint en pratique |
| `auth.ts:147` (ancienne action `signUp`) | **code mort, aucun appelant** (déjà confirmé lors de l'investigation précédente) |

**Conclusion : le seul chemin réellement actif est la garde `/dashboard/*` du middleware — tout le reste redirige déjà vers `/start` ou est inatteignable.** Pour qu'un nouveau marchand y tombe après déploiement, il faudrait qu'il soit authentifié (cookies posés) **avec un profil sans `shop_id` ou sans `onboarding_completed`**, puis qu'il navigue vers `/dashboard`.

Tracé sur `completeSignupFromStart` (`start/actions.ts:214-285`, la fonction de compte de `/start` v4) : `auth.signUp()` (ligne 245) pose la session, puis **`shop_id` et `onboarding_completed` sont posés ensemble, dans la même requête `UPDATE`** (lignes 274-277) — aucune fenêtre où le profil est authentifié avec l'un sans l'autre, dans le cas nominal. Le parcours `/start` (écrans 10-11, premier produit puis boutique en ligne) reste côté client dans `/start`, ne touche jamais `/dashboard` avant que ce couple soit posé.

**Scénario résiduel, réel mais étroit** : si `completeSignupFromStart` est interrompue **entre** `auth.signUp()` (ligne 245) et la mise à jour finale (ligne 274-277) — coupure réseau, fermeture d'onglet, crash serveur — le compte existe mais le profil reste sans `shop_id`. Si ce marchand revient ensuite se connecter (`/login`, il a un identifiant valide : téléphone + PIN), `/login` le redirige vers `/dashboard` (`middleware.ts:211-213`), qui le renvoie aussitôt vers `/onboarding` faute de `shop_id`. En terminant l'assistant `/onboarding`, il obtiendrait une **seconde boutique, en `legacy`**, tandis que le brouillon `free_orders` créé par `/start` resterait à l'état `draft`, orphelin — la promesse des « 3 commandes offertes » serait ratée pour ce marchand précis. **C'est une fuite réelle, mais une fenêtre de rattrapage sur échec partiel, pas le chemin normal** — non chiffrable sans télémétrie sur les abandons `/start` entre écran 9 et écran 10, qui n'existe pas aujourd'hui.

**Non couvert par cette réponse** : le comportement du client (est-ce que `useEffect`/état React de `StartFlow` retente automatiquement en cas d'échec réseau à cette étape précise ?) — hors du périmètre de cette lecture, qui s'est arrêtée au serveur.

---

**Question 2 — Que deviennent les 1 542 boutiques `legacy` existantes après le déploiement ?**

**Recherche exhaustive de toute écriture de `trial_model`** (`grep -rn "trial_model" src/`) : deux points d'écriture seulement, tous deux des `INSERT` à la création — `createDraftShop` (`start/actions.ts:70`) et `resolveShop` (`start/actions.ts:141`). **Aucun `UPDATE` ne touche `trial_model` nulle part dans `src/`. Aucune bascule `legacy → free_orders` n'existe en code.** Ce n'est pas un oubli caché à retrouver — c'est une fonctionnalité qui n'a jamais été écrite.

**Conséquence directe, confirmée par la logique déjà documentée en §10** : sans intervention, **les 1 542 boutiques `legacy` restent définitivement sous l'ancien régime** — `is_active`/`plan` gouvernent leur accès, coupure sèche à `trial_ends_at` (`cron/trial-expiry`), aucune commande retenue, aucun palier de dégradation, aucun écran de fin d'essai façon `free_orders`. Le déploiement de la Livraison 1 ne change rien pour elles : le nouveau modèle ne s'applique qu'aux boutiques créées après coup via le nouveau `/start`.

**C'est une décision produit non prise, pas un bug** : faire basculer (tout ou partie) des 1 542 boutiques `legacy` vers `free_orders` demanderait d'écrire cette fonctionnalité (quelle boutique, avec quel quota de commandes offertes de départ, quel nouvel horodatage de fin d'essai) — rien de tel n'existe aujourd'hui, ni en migration ni en code applicatif.

---

**Répartition des 1 098 boutiques `legacy` déjà éteintes** (mesurée le 2026-08-13, critère identique à celui du cron : `plan='trial' AND is_active=false AND trial_model='legacy'`) :

- **685 (62,4 %) avaient un produit actif** au moment de la mesure, 413 (37,6 %) n'en avaient aucun. *Proxy sur l'état actuel des produits — aucune trace historique de l'état exact au moment de l'extinction n'existe en base ; à prendre comme indicatif, pas comme une mesure au jour près.*
- **1 seule sur 1 098 avait déjà reçu une commande avant extinction** (toute commande, tout statut confondus) — 1 097 n'en ont jamais reçu.
- **Vague, pas un flux régulier** : 1 071 des 1 098 (97,5 %) se sont éteintes en une seule semaine, celle du **3 août**, contre seulement 27 la semaine suivante (10 août) et rien avant. Le run du cron du 11 août a désactivé 9 boutiques ce jour-là — rythme redevenu faible et régulier depuis.
- **La vague n'est pas une coïncidence de fins d'essai groupées** : les `trial_ends_at` des boutiques éteintes lors de cette semaine s'étalent en réalité sur plusieurs mois, de mi-mai à début août (207 finissaient leur essai la semaine du 15 juin, 282 celle du 22 juin, 212 celle du 20 juillet, etc.) — ces essais avaient donc expiré depuis longtemps sans que la boutique soit désactivée, jusqu'à ce que le cron les rattrape tous d'un coup début août. **Cause du retard non établie depuis `src/` seul** (le code du cron lui-même date d'avant cette période — commit `4c86473` — donc pas un ajout récent ; la table `cron_health` ne conserve que le dernier run, pas d'historique permettant de dater un dysfonctionnement antérieur). À signaler comme point ouvert, pas comme conclusion.

---

## 12. Extinction du 3 août — ce que vit un marchand, et le vrai taux de réussite du produit — ouvert le 2026-08-13

Suite directe du §10/§11 : la vague d'extinction (1 071 boutiques en une semaine, essais expirés depuis mi-mai) soulève des questions distinctes, sur l'expérience du marchand et sur la mesure de fond du produit. Traité comme sujet à part entière, au même titre que le taux d'aboutissement des paiements en ligne (§6).

**1. Les trois écrans, tracés dans le code :**

- **Tableau de bord du marchand** (`dashboard/layout.tsx:82-137`) : un écran de blocage plein cadre — logo/initiale de la boutique, *« Votre période de configuration est terminée »*, puis *« Pour que votre boutique soit visible par vos clients et commencer à recevoir des commandes, choisissez un plan d'abonnement »*, avec un bouton **« Activer ma boutique → »** vers `/dashboard/upgrade`. Ce message ne dit jamais explicitement que la boutique a été désactivée — la formulation, écrite pour le cas normal (marchand qui n'a jamais activé), sonne comme une étape de configuration en attente, pas comme une coupure après des semaines en ligne.
- **Lien public de la boutique** (`[shop-slug]/layout.tsx:49-110`) : un cadenas 🔒, le nom/logo de la boutique, et — pour ce cas précis (`trial_model='legacy'`, `plan='trial'`) — le message *« Cette boutique n'est pas encore active. Revenez bientôt ! »*. Même défaut de formulation : le texte affiché pour une boutique qui a *déjà vendu* est identique à celui d'une boutique qui n'a jamais ouvert. Un bouton WhatsApp vers le marchand s'affiche si `phone_whatsapp` est renseigné, plus un petit lien *« Si vous êtes le propriétaire, réactivez votre abonnement »* vers `/dashboard/upgrade`.
- **Client qui tente de commander** : `[shop-slug]/layout.tsx` enveloppe toutes les routes de la boutique (fiche produit, tunnel de commande) — le client voit exactement le même écran cadenas que la page d'accueil, avant même d'atteindre la fiche produit ou le formulaire de commande. Aucun parcours de commande n'est accessible.

**2. Notification — aucune preuve d'un préavis, et signal fort d'un problème d'envoi plus large :**

Le cron `trial-expiry` envoie un SMS (via Lafricamobile — `sendWhatsApp` n'est qu'un alias historique de `sendSMS`, ce n'est pas WhatsApp) **au moment de l'extinction**, si `phone_whatsapp` est renseigné (989 des 1 098 boutiques, 90 %, en ont un). Un cron distinct, `trial-reminder`, est programmé pour un rappel à J-3 avant l'échéance (`0 9 * * *`) — mais **il n'appelle jamais `recordCronRun`**, contrairement aux autres crons : aucune trace de ses exécutions n'existe dans `cron_health`, impossible de confirmer s'il a tourné pendant la période où le rattrapage a eu lieu. Il n'existe par ailleurs aucune table de log pour les SMS de cycle de vie de boutique — `notification_logs` ne couvre que les notifications liées aux commandes (`orders/route.ts`, `webhooks/bictorys`), jamais les crons trial/subscription.

**Signal préoccupant, indépendant du rattrapage** : le dernier run de chacun des trois crons d'extinction a notifié **zéro marchand**, malgré des numéros disponibles dans l'immense majorité des cas — `trial-expiry` (11 août, 9 désactivées, 0 notifiées), `subscription-expiry` (12 août, 3 désactivées, 0 notifiées), `free-orders-trial-expiry` (7 août, 1 expirée, 0 notifiée). Trois mécanismes différents, trois jours différents, le même résultat. **Pas une preuve formelle** (aucun accès aux journaux Lafricamobile depuis `src/`), mais un faisceau d'indices cohérent avec un envoi SMS actuellement cassé, pas seulement avec des numéros manquants.

**3. Réactivation — oui, en libre-service, visible depuis les deux écrans morts.** `/dashboard/upgrade` (accessible depuis le bouton du tableau de bord bloqué et depuis le lien de la page publique) permet de choisir un plan et de payer ; `activatePlan` (`src/lib/billing/activate-plan.ts:61-67`) repose `plan`, `is_active=true` et `subscription_ends_at` en un seul `UPDATE`. Aucune action manuelle de l'équipe n'est nécessaire.

**4. Combien sont revenues depuis l'extinction — mesuré sur l'ensemble des essais `legacy` terminés (1 245 boutiques : les 1 098 encore mortes + 147 qui ne le sont plus), pas seulement depuis le 3 août :**

| Situation aujourd'hui | Boutiques | % des 1 245 |
|---|---|---|
| Toujours mortes (`plan='trial'`, `is_active=false`) | 1 098 | 88,2 % |
| Payantes et actives aujourd'hui | 42 | 3,4 % |
| Passées en plan payant, puis re-désactivées depuis (abonnement expiré à son tour) | 95 | 7,6 % |
| Réactivées (`is_active=true`) sans changer de plan (`plan` toujours `'trial'`) | 10 | 0,8 % |

**Seules 42 boutiques sur 1 245 (3,4 %) sont vivantes aujourd'hui après avoir un jour terminé leur essai `legacy`.** Les 10 « réactivées sans payer » sont une anomalie non expliquée depuis `src/` — aucun code trouvé ne repose `is_active=true` sur une boutique `plan='trial'` en dehors de la création ; probablement une intervention manuelle, non confirmée.

**5. Fiabilité du cron `trial-expiry` — garantie partielle, pas totale :**

Il existe un vrai filet : `cron/cron-health-check` (`vercel.json`, quotidien 11h UTC) surveille `trial-expiry` (et 2 autres crons critiques) via `cron_health.last_run`, et envoie une alerte SMS à `ADMIN_ALERT_PHONE` si la dernière exécution dépasse 28 h ou s'est terminée en erreur.

**Mais un défaut structurel limite sa fiabilité, constaté en le vérifiant maintenant** : `trial-expiry` (`route.ts`) n'écrit `cron_health` **que s'il y a au moins une boutique à désactiver ce jour-là** — retour anticipé avant `recordCronRun` sinon. **Vérifié à l'instant : le dernier run enregistré date du 11 août 10h01 ; aucun run n'est enregistré pour le 12 août**, alors que le cron est programmé chaque jour à 10h. Impossible de distinguer depuis la base si le cron a tourné le 12 août et n'a simplement rien trouvé à désactiver (probable, le rythme est redescendu à quelques boutiques par jour), ou s'il a échoué silencieusement — les deux cas produisent exactement la même absence de trace. **C'est précisément le type de panne qui a permis au retard de mi-mai à début août de s'accumuler sans être détecté.** `trial-reminder` n'est pas couvert par `cron-health-check` du tout. Et si le problème d'envoi SMS signalé au point 2 touche aussi l'alerte elle-même (`sendWhatsApp` du garde-fou emprunte le même canal Lafricamobile), le filet de sécurité pourrait être silencieux au moment précis où il serait utile.

**6. Le vrai taux de réussite, mesuré pour la première fois :** sur les **1 245 boutiques `legacy` dont l'essai est arrivé à échéance** (mortes ou revenues, peu importe leur état aujourd'hui), **58 ont reçu au moins une commande de toute leur existence — 4,7 %.** C'est ce chiffre, pas le 93,1 % du §10 (qui mélange des boutiques encore en plein essai), qui répond à la question « une boutique qui va jusqu'au bout de son essai gratuit, a-t-elle vendu ». Cohérent avec le reste : sur les 58 qui ont vendu, une seule appartient au groupe des 1 098 encore mortes — les 57 autres sont concentrées dans les 147 qui ont un jour payé, ce qui suggère que la vente précède le paiement plus souvent que l'inverse (un marchand qui vend est plus enclin à s'abonner), sans que cette lecture soit vérifiée davantage ici.

---

## 13. Panne SMS — cause confirmée par l'utilisateur (crédit Lafricamobile), mais bien plus ancienne et plus large que l'extinction du 3 août — ouvert le 2026-08-13, cause corrigée le 2026-08-13 (voir §15)

**Correction du 2026-08-13, à lire avant tout le reste de cette section : « sent »/« succès » ci-dessous ne veut jamais dire « livré ».** Test direct par l'utilisateur depuis le tableau de bord Lafricamobile (hors code) : deux envois affichés « Envoyé » côté fournisseur, aucun n'est arrivé sur les deux téléphones testés — leurs propres statistiques indiquent 26,5 % de délivrabilité, 55 % de rejets. **`notification_logs.status = 'sent'` signifie seulement que Lafricamobile a accepté la requête API, jamais que le SMS a atteint un téléphone.** Les « 4 jours qui ont marché » et le raisonnement crédit/solde du point 1 ci-dessous doivent donc être lus comme une analyse de **taux d'acceptation par l'API**, pas de livraison réelle — le canal doit être considéré non fonctionnel depuis le début jusqu'à preuve du contraire de Lafricamobile. Cause retenue : délivrabilité côté fournisseur, probablement un nom d'expéditeur non approuvé chez les opérateurs — pas un problème de crédit, de code, ou d'identifiants. Le 401 reste inexpliqué et devient secondaire. Détail en §15.

L'utilisateur a identifié la cause commerciale (crédit Lafricamobile épuisé, pas de rechargement automatique en mobile money). Mesuré depuis le code ce qui l'a rendu invisible. *(Lecture du soir du 13 août — voir la correction ci-dessus et le §15 pour la cause retenue au final.)*

**1. Date du dernier SMS parti avec succès — mesurée dans `notification_logs` (la seule table qui trace des envois individuels ; ne couvre que les confirmations/alertes de commande, pas les crons).** Sur toute l'historique de la table (depuis le 21 mai) : seuls **4 jours** ont vu au moins un envoi réussi — 22 juin (8), 23 juin (2), 7 juillet (2), **29 juillet (6)**. **Aucun succès depuis le 29 juillet 10h46 UTC — 14 jours d'échec continu au moment de cette mesure (13 août).** Fait plus important que la date elle-même : **la panne n'est pas récente**. Le code d'erreur dominant, `Lafricamobile HTTP 401` (488 occurrences), apparaît dès le **23 juin** — le tout premier jour d'usage de Lafricamobile en production (avant cette date, les échecs sont `Twilio credentials not configured`, l'ancien prestataire, migration terminée le 20-22 juin) — et reste constant jusqu'au 12 août. **Sur ~7 semaines d'usage de Lafricamobile, 4 jours isolés ont fonctionné ; le reste a échoué avec un 401.** Un HTTP 401 est une erreur d'authentification, pas un code réponse dédié « crédit épuisé » — cohérent avec un compte Lafricamobile suspendu faute de solde s'ils ne distinguent pas les deux cas côté API, mais ce point précis n'est vérifiable que côté Lafricamobile, pas depuis `src/`.

**2. Tout ce qui est muet — recherche exhaustive de `sendWhatsApp`/`sendSMS` dans `src/` (20 points d'appel, tous passent par le même client Lafricamobile cassé) :**

| Domaine | Exemples | Fichiers |
|---|---|---|
| Commande client | confirmation de commande (client), alerte nouvelle commande (marchand), commande annulée, changement de statut de livraison | `api/orders/route.ts`, `api/webhooks/bictorys/route.ts`, `lib/actions/orders.ts` |
| Paiement en ligne | lien de téléchargement produit numérique (après paiement Bictorys ou Stripe) | `api/webhooks/bictorys/route.ts:531`, `api/webhooks/stripe/route.ts:154` |
| Stock | alerte stock bas (marchand), stock de retour (client abonné à l'alerte) | `api/orders/route.ts:507`, `lib/actions/products.ts:149` |
| Cycle de vie boutique | rappel essai J-3, extinction essai, rappel/expiration abonnement, boutique activée, essai `free_orders` expiré/activé | tous les crons `trial-*`, `subscription-*`, `lib/billing/*.ts` |
| Compte | réinitialisation du code PIN | `lib/actions/auth.ts:272` |
| Commandes retenues | notification client qu'une commande retenue est prête à traiter | `api/cron/notify-held-orders/route.ts` |
| Interne | alerte reversement manuel requis (admin), alerte de panne cron (admin), campagnes SMS admin | `api/cron/process-pending-payouts/route.ts`, `api/cron/cron-health-check/route.ts`, `api/admin/sms-campaign/route.ts` |

**Rien n'est épargné : c'est l'intégralité de la couche SMS de la plateforme, marchand et client confondus, muette depuis mi-fin juin avec de rares exceptions.**

**3. Ce que fait le code à l'échec — vérifié point d'appel par point d'appel, aucun ne remonte dans Sentry :**

- **Bien géré (échec visible pour l'utilisateur ou tracé en base)** : `lib/actions/auth.ts:274-277` (réinitialisation PIN) — `if (!success)` retourne une erreur explicite à l'écran, avec `console.error`. `api/orders/route.ts:561-570` et `597-607`, `api/webhooks/bictorys/route.ts:420-429` et `446-455` (confirmations de commande) — chaque tentative est écrite dans `notification_logs` avec `status`/`error_message`, c'est la seule raison pour laquelle le point 1 ci-dessus est mesurable.
- **Avalé silencieusement, le vrai défaut signalé par l'utilisateur** : **`api/webhooks/bictorys/route.ts:531`** et **`api/webhooks/stripe/route.ts:154`** — lien de téléchargement d'un produit numérique après paiement réussi : `await sendWhatsApp(...)`, résultat jamais lu, aucun `if`, aucun log, aucune trace en base. **Un client qui paie un produit numérique et dont l'envoi échoue ne reçoit jamais son fichier, et personne ne le sait.** `api/cron/cron-health-check/route.ts:50` — l'alerte du garde-fou lui-même n'est pas vérifiée (`await sendWhatsApp(...)` sans lire le résultat) : si la même panne touche ce message, l'alerte échoue sans laisser de trace non plus. `api/cron/trial-expiry/route.ts:54`, `subscription-expiry/route.ts:89`, `free-orders-trial-expiry/route.ts:54` — résultat lu seulement pour incrémenter un compteur (`notified++`), l'erreur elle-même (`result.error`) n'est jamais journalisée. `subscription-reminder/route.ts:69` et `trial-onboarding/route.ts:100` — le booléen `success` est écrit dans `login_attempts` (déduplication), mais le message d'erreur est perdu. `api/cron/notify-held-orders/route.ts:51` — `if (!success) continue` : la commande sera retentée au prochain passage, silencieusement, sans qu'aucune alerte ne signale que ça échoue en boucle. `lib/actions/products.ts:149`, `lib/billing/activate-plan.ts:103`, `lib/actions/orders.ts:102,161`, `lib/billing/shop-status.ts:72`, `api/cron/process-pending-payouts/route.ts:83` — `.catch(err => console.error(...))` : journalisé, mais uniquement dans les logs de fonction Vercel (éphémères, non consultables depuis la base, jamais remontés). **Aucun `Sentry.captureException` nulle part dans `src/lib/notifications/` ni dans les points d'appel des crons** — vérifié par recherche exhaustive.

**4. Solde Lafricamobile — non exposé par le code actuel.** `sendLafricamobileSms` (`lib/notifications/lafricamobile.ts`) capture la réponse brute de l'API (`rawResponse`) mais ne la journalise ni ne l'analyse jamais — seul un statut HTTP `ok`/`!ok` est retenu, jamais le contenu XML de la réponse. **Aucune colonne ne stocke la réponse brute dans `notification_logs`** (seulement `status` et `error_message`, dérivés du booléen `success`). Le code ne consulte donc aucun solde, et rien dans `src/` n'indique si l'API Lafricamobile expose un champ solde dans sa réponse ou un point d'accès de consultation dédié — la documentation en commentaire (`https://lamsms.lafricamobile.com/api`) est la seule piste, non vérifiée ici (hors périmètre de cette lecture, qui s'est arrêtée au code du dépôt).

**5. Conséquence commerciale — mesurée.** `components/landing-v6/PricingV6.tsx:45,174` liste *« Notifications SMS automatiques »* comme un avantage exclusif Business et Pro (`—` pour Découverte, `✓` pour les deux autres) — confirmé exactement comme décrit. *Note historique : au moment de cette mesure (2026-08-13), cette page n'était pas encore en production (landing v6, voir §0/§11) — la page tarifs alors en ligne sur `main` ne mentionnait pas les SMS comme différenciateur. Cette page est en production depuis le 2026-08-17 (§21), avec la ligne SMS déjà retirée — voir point 4 plus bas dans ce paragraphe/§15 point 4.* Sur les **70 boutiques payantes Business/Pro** (54 + 16), **8 ont reçu au moins une commande depuis le 29 juillet** (3 Business, 5 Pro) — ce sont des marchands qui payaient pour cette fonctionnalité précise et dont les clients comme eux-mêmes n'ont rien reçu sur ces commandes.

**6. Le cron aveugle — correctif identifié, pas appliqué (aucun code écrit, sur consigne).** Le motif fautif : appeler `recordCronRun` seulement après un retour anticipé quand il n'y a rien à traiter, au lieu de l'appeler dans tous les cas. **Deux crons ont exactement ce défaut** : `trial-expiry` (déjà signalé en §12) et **`api/cron/verify-subscription-payments/route.ts:54-56`** (retour anticipé si `pendingTransactions.length === 0`, sans `recordCronRun` avant). **Le correctif existe déjà ailleurs dans le même dépôt, à copier telle quelle** : `api/cron/subscription-expiry/route.ts:54-56` appelle `recordCronRun('subscription-expiry', 'ok', { deactivated: 0, ... })` **avant** son retour anticipé — c'est le bon motif. `free-orders-trial-expiry` et `notify-held-orders` n'ont pas ce défaut précis (pas de retour anticipé, la fonction traverse toujours jusqu'au `recordCronRun` final même à zéro résultat).

Angle mort plus large que ces deux bugs : **sur les 16 crons de `vercel.json`, seuls 5 appellent `recordCronRun` ne serait-ce qu'une fois** (`trial-expiry`, `subscription-expiry`, `free-orders-trial-expiry`, `verify-subscription-payments`, `notify-held-orders`). Les 11 autres (`cleanup`, `cron-health-check` lui-même, `expire-pending`, `process-pending-payouts`, `process-refunds`, `purge-draft-shops`, `reminders`, `review-requests`, `subscription-reminder`, `trial-onboarding`, **`trial-reminder`**) n'ont aucune trace d'exécution, jamais. Et `cron-health-check` (le garde-fou) ne surveille de toute façon que 3 noms de job (`CRITICAL_JOBS`, `cron-health-check/route.ts:7-11`) — corriger les 2 bugs de retour anticipé ne couvrirait donc que 3 crons sur 16.

**7. Décomposition des 1 246 boutiques `legacy` dont l'essai est arrivé à échéance (chiffre réévalué à l'instant : 1 246, contre 1 245 un peu plus tôt dans la soirée — une boutique de plus a franchi `trial_ends_at` entre les deux mesures, écart normal, pas une erreur) :**

- **Produit actif** : 829 (66,5 %) en ont un aujourd'hui, 417 (33,5 %) n'en ont aucun. Même réserve que pour le §12 point 1 : proxy sur l'état actuel, pas une mesure au jour de l'échéance.
- **Lien partagé : donnée inexistante, pour toutes les boutiques, pas seulement celles-ci.** Le seul mécanisme de partage du tableau de bord (`components/dashboard/ShareWhatsAppButton.tsx`) est un lien `https://wa.me/?text=...` ouvert côté client, sans aucun appel serveur — rien n'est jamais journalisé, quel que soit `trial_model`. Aucune approximation possible : la question n'a pas de réponse dans ce système, il n'y a jamais eu de mesure à faire.
- **Visite reçue : donnée inexistante, structurellement, pour ces 1 246 boutiques précisément** (déjà établi en §11) — `VisitBeacon` ne se monte que si `trial_model='free_orders'`, jamais pour `legacy`. `shop_visits` est vide pour cette population entière, pas seulement pour les 14 du §9. Zéro n'est pas une mesure de zéro visite, c'est l'absence de toute mesure.
- **Ce qui reste mesurable et qui répond, par défaut, à « où s'arrêtent-elles »** : la seule étape du parcours réellement instrumentée pour une boutique `legacy` est la commande (§12 point 6 : 58/1 245, 4,7 %). Entre avoir un produit actif (66,5 %) et recevoir une commande (4,7 %), tout ce qui se passe — un client voit-il la boutique, la partage-t-on, combien de fois — est un angle mort total du système de mesure actuel, pas seulement un chiffre bas.

**8. Le périmètre de la Livraison 1 (§0) tient-il encore — réponse honnête.** Les sept étapes mécaniques figées hier (fusion PR #4, commit Lot 1, migrations orphelines, favicon, fusion des branches, recette, déploiement) ne sont techniquement affectées par rien de ce qui a été mesuré ce soir — aucun fichier en jeu ne recoupe le Lot 1, les migrations 090/091, ou le favicon. **La question n'est donc pas mécanique, elle est commerciale et opérationnelle** :

- Déployer la Livraison 1 met en ligne `PricingV6.tsx`, qui **commence à vendre activement** « Notifications SMS automatiques » comme argument payant Business/Pro — une promesse que la page tarifs actuelle (sur `main`) ne fait pas encore. Déployer sans confirmer le SMS réparé revient à commencer à vendre plus fort une fonctionnalité qui, mesuré ce soir, a fonctionné 4 jours sur les 7 dernières semaines.
- Le modèle `free_orders` que la Livraison 1 active dépend du même cron/canal SMS pour ses propres notifications d'expiration (`free-orders-trial-expiry`, dernier run le 7 août, 0 notifiée) — sans correction, les premières vraies boutiques `free_orders` referont exactement l'expérience des 1 098 legacy : essai qui se termine sans préavis, cron dont le silence est indiscernable d'un fonctionnement normal.
- Rien de tout ça n'était su au moment du gel du périmètre hier — ce ne sont pas des éléments explicitement exclus (le tunnel de commande, BF/BK, Stripe Connect le sont) mais des faits nouveaux, découverts par les mesures de ce soir, qui pèsent sur la décision de déployer maintenant plutôt que sur le contenu du Lot 1 lui-même.

**Pas une recommandation tranchée ici — une mise en balance factuelle, la décision reste à l'utilisateur** : recharger Lafricamobile règle la cause immédiate mais pas la promesse marketing déjà écrite dans `PricingV6.tsx`, ni l'angle mort de `cron-health-check`. Rien n'empêche techniquement de déployer la Livraison 1 telle quelle et de traiter le SMS comme un chantier séparé, urgent mais distinct — comme le tunnel de commande l'est pour la Livraison 2.

---

## 14. Panne SMS, suite — reconstitution fine et décision sur le périmètre — 2026-08-13

**1. Les 4 jours qui ont marché, reconstitués minute par minute (`notification_logs`).** `lafricamobile.ts` est déployé le **20 juin** (commit `7bd82ab`) — deux jours avant le premier succès. Le 22 juin, la chronologie complète montre trois régimes dans la même journée : de 7h17 à 13h05, échec `Lafricamobile SMS non configuré (variables d'environnement manquantes)` — le code était déployé mais les identifiants Vercel ne l'étaient pas encore ; à 15h48, deux `fetch failed` ; puis à partir de 20h08, **8 succès d'affilée sans un seul échec jusqu'à la fin de la journée**. Conclusion pour ce jour précis : les identifiants ont été renseignés dans l'environnement entre 15h48 et 20h08 le 22 juin — **ces envois passaient bien par Lafricamobile, pas par Twilio** (le code Twilio n'existe plus dans cette branche à cette date). Le 29 juillet montre le même type de rupture nette : 6 succès consécutifs de 7h11 à 10h46, puis, à partir de 13h13, uniquement des `Lafricamobile HTTP 401` jusqu'à la fin de la journée — aucun échec avant la bascule, aucun succès après. **Ce n'est pas le profil d'une clé invalide** (qui échouerait 100 % du temps, tout le temps) **mais celui d'un solde qui s'épuise en cours de journée** — cohérent avec l'hypothèse de l'utilisateur, une fois la fausse piste du 22 juin écartée. Le 401 comme code renvoyé pour un compte à sec (au lieu d'un 402/403 dédié) reste à confirmer côté Lafricamobile, mais le comportement observé (bascule nette, intra-journée, jamais de retour en arrière le même jour) ne pointe pas vers autre chose.

**2. Le lien de téléchargement — pas d'envoi par e-mail, confirmé.** `sendWhatsApp`/`sendSMS` sont les seuls appels aux deux points d'envoi automatique (`webhooks/bictorys/route.ts:531`, `webhooks/stripe/route.ts:154`) — `lib/notifications/email.ts` existe et est utilisé ailleurs (confirmations de commande classiques, relances d'avis) mais **jamais pour la livraison d'un produit numérique**. Le seul filet est manuel : `resend-digital` (créé le **13 juillet**, commit `d82b75e`), dont le message de commit dit explicitement *« quand la livraison digitale échoue silencieusement dans le webhook (tokens non créés, client non notifié), le marchand n'avait aucun moyen d'envoyer le lien manuellement »* — **l'incident que l'utilisateur soupçonnait est confirmé par le message de commit lui-même**, daté en plein milieu de la panne SMS en cours. Le correctif de l'époque a ajouté un bouton de secours pour le marchand, pas une correction de l'envoi automatique — les deux sites restent, aujourd'hui encore, sans vérification ni journalisation (§13 point 3).

Mesuré sur les **20 commandes de produits numériques payées depuis le 23 juin** : 19 ont un lien de téléchargement (`download_tokens`) créé en base, 1 n'en a jamais eu aucun. Sur les 19, 16 ont été effectivement téléchargées au moins une fois, 3 ne l'ont jamais été malgré l'existence du lien. **Au total, 4 commandes sur 20 (20 %) n'ont jamais abouti à un téléchargement.** Limite de cette mesure, à ne pas dépasser : l'envoi SMS du lien n'étant pas journalisé dans `notification_logs` (contrairement aux confirmations de commande), impossible de dire si les 16 téléchargements réussis viennent de l'envoi automatique (qui a pu tomber dans une des fenêtres où Lafricamobile fonctionnait) ou d'une intervention manuelle du marchand via `resend-digital` — les deux chemins aboutissent au même token, la base ne distingue pas lequel a été utilisé.

**3. Correctif Sentry — proposé, pas écrit.** Priorité 1, les deux sites qui n'ont aujourd'hui ni vérification ni trace d'aucune sorte : `webhooks/bictorys/route.ts:531` et `webhooks/stripe/route.ts:154` — lire le résultat de `sendWhatsApp`, et sur échec appeler `Sentry.captureException` avec le contexte (order_id, shop_id, produit) plutôt que de laisser la promesse ignorée. Priorité 2 : `cron/cron-health-check/route.ts:50`, l'alerte du garde-fou lui-même — vérifier son propre envoi et le remonter dans Sentry si l'alerte échoue, sinon le garde-fou peut être silencieux exactement quand il servirait. Priorité 3, les crons qui comptent déjà `notified`/`success` sans jamais journaliser l'erreur (`trial-expiry`, `subscription-expiry`, `free-orders-trial-expiry`, `subscription-reminder`, `trial-onboarding`) : ajouter `Sentry.captureException` (ou au minimum `console.error` avec le détail de `result.error`, aujourd'hui perdu) sur chaque échec individuel, pas seulement sur l'échec global de la requête HTTP du cron.

**4. Correctif crons aveugles — proposé, pas écrit.** Pour `trial-expiry` et `verify-subscription-payments/route.ts:54-56` : reproduire exactement le motif déjà correct de `subscription-expiry/route.ts:54-56` — appeler `recordCronRun` **avant** le retour anticipé du cas « rien à traiter », pas seulement à la fin du chemin normal. **Noms de tâches à ajouter à `CRITICAL_JOBS`** (`cron-health-check/route.ts:7-11`, qui n'en surveille que 3 aujourd'hui) : `free-orders-trial-expiry` (le cron d'expiration du modèle que la Livraison 1 s'apprête à activer, silence actuellement indétectable) et `trial-reminder` (zéro trace d'exécution, jamais, le seul rappel préventif du produit). `subscription-reminder`, `notify-held-orders` et `trial-onboarding` sont candidats mais moins urgents — leur échec dégrade l'expérience sans couper l'accès à la boutique, contrairement aux quatre premiers.

**5. Les 8 boutiques Business/Pro concernées** (commandes depuis le 29 juillet 10h46, dernier envoi SMS confirmé réussi) :

| Boutique | Plan | Téléphone | Commandes depuis le 29/07 |
|---|---|---|---|
| Jwu E-Tik | Pro | +22398334756 | 15 |
| Glow Eternel | Business | +2250585301466 | 10 |
| ABI&CO | Pro | +2250777220466 | 8 |
| Viens on s'connaît | Pro | +221781362728 | 6 |
| digitalglobalshop.com | Pro | +2250505796706 | 4 |
| Ouze collection | Business | +221761354067 | 2 |
| Chinabusiness | Business | +221774550599 | 1 |
| Global Market | Pro | +221778120209 | 1 |

**6. Modification conditionnelle de `PricingV6.tsx` — préparée, non appliquée, à n'utiliser que si le SMS ne repart pas dans les 24 h.** Deux lignes, aucune autre : supprimer `{ label: 'Notifications SMS automatiques' },` (ligne 45, liste de fonctionnalités du plan Business — le plan Pro y renvoie via « Tout ce qu'offre Business », pas de troisième occurrence à traiter) et supprimer `<tr><th scope="row">Notifications SMS auto</th><td className="no">—</td><td className="yes">✓</td><td className="yes">✓</td></tr>` (ligne 174, tableau comparatif). Rien d'autre sur la page ne mentionne les SMS.

**7. Le trou entre « a un produit » et « reçoit une commande » n'est pas un chiffre bas, c'est une absence de mesure — à ne pas confondre.** Rappel volontairement répété ici en clôture du sujet SMS, parce que c'est la même faille de fond : aucune trace de partage de lien (le bouton `ShareWhatsAppButton` n'appelle jamais le serveur, pour aucune boutique, quel que soit son statut) ; aucune visite mesurée pour 1 542 boutiques `legacy` sur 1 543 (`shop_visits` structurellement vide hors `trial_model='free_orders'`). Entre le produit publié et la commande reçue, la plateforme ne sait rien de ce qui s'est passé — pas « peu de trafic », **aucune mesure de trafic du tout**. Tant que ce trou existe, aucune réponse à « pourquoi 95 % des marchands ne vendent pas » ne peut être autre chose qu'une hypothèse non vérifiable depuis les données actuelles.

**Décision de l'utilisateur sur le périmètre de la Livraison 1** : le périmètre tient, avec une seule condition. Recharge et test Lafricamobile en cours aujourd'hui. Si le SMS repart dans les 24 h, déploiement tel quel. S'il ne repart pas, la ligne « Notifications SMS automatiques » sort de `PricingV6.tsx` avant le déploiement (modification préparée au point 6 ci-dessus), remise en place le jour où le canal fonctionne réellement. Sentry (point 3) et la correction des crons aveugles (point 4) partent en Livraison 2 avec le tunnel de commande — aucun des deux ne bloque le déploiement.

---

## 15. Panne SMS — cause finale (délivrabilité fournisseur, pas le crédit) et deux chantiers chiffrés pour la Livraison 2 — 2026-08-13

**Test décisif de l'utilisateur, hors code** : deux SMS envoyés depuis l'interface Lafricamobile elle-même (pas depuis la plateforme), sur deux numéros différents, tous deux affichés « Envoyé » côté fournisseur — **aucun n'est arrivé**. Des tests antérieurs du 16 juin et du 15 juillet, faits par l'utilisateur dans les mêmes conditions, montrent le même résultat. Ce test élimine le code, les identifiants API et le solde du compte : rien de tout ça n'intervient dans un envoi fait depuis leur propre tableau de bord. **Cause retenue : délivrabilité côté Lafricamobile/opérateurs (26,5 % de délivrabilité, 55 % de rejets selon leurs propres chiffres), probablement un nom d'expéditeur non approuvé.** Le 401 constaté en §13/§14 reste réel mais devient secondaire — il explique une partie des refus, pas l'essentiel du problème, qui est en amont. Un ticket est ouvert chez le fournisseur.

**Conséquence sur la lecture des « 4 jours qui ont marché » (§13 point 1, §14 point 1) : à corriger.** Ce ne sont pas des SMS délivrés, seulement acceptés par l'API — voir la correction ajoutée en tête de §13. Il est possible qu'aucun SMS n'ait jamais été réellement délivré depuis ce compte, sur toute la période mesurée.

**1. L'envoi en masse de 596 SMS — identifié par l'utilisateur comme sa propre campagne admin vers les boutiques Découverte inactives.** Vérifié dans le code : `api/admin/sms-campaign/route.ts` est **un chemin totalement séparé** des notifications automatiques — il importe `sendSMS` directement (donc le même transport Lafricamobile cassé), mais **n'écrit jamais dans `notification_logs`, ni dans aucune autre table** ; le seul résultat produit est le JSON `{ sent, failed, skipped }` retourné à l'appel, jamais persisté. Confirme exactement ce que l'utilisateur soupçonnait : une partie de la chaîne n'est pas instrumentée — en réalité cette route-là ne l'est pas du tout. Détail technique qui borne la mesure : la route plafonne à 500 `shopIds` par appel (`route.ts:50-52`), donc 596 correspond forcément à au moins deux campagnes distinctes, pas un seul envoi.

**Combien de marchands, à quelle date — non récupérable depuis la base.** Aucune table ne conserve la sélection envoyée ni la date de l'appel ; la seule trace possible serait les journaux de fonction Vercel pour cette route (durée de rétention non vérifiée ici) ou la mémoire/l'historique de navigateur de l'utilisateur. **Non approximé plutôt que deviné**, conformément à la consigne : si l'utilisateur veut recontacter les marchands visés, la reconstitution ne peut venir que de Vercel (si les journaux existent encore) ou d'une nouvelle requête sur le critère de ciblage probable (`plan='decouverte' AND is_active=false` **aujourd'hui**) — qui donnerait qui correspond au critère maintenant, pas qui a été réellement sélectionné au moment de l'envoi (des boutiques ont pu changer de statut depuis).

**2. Coût d'un repli automatique e-mail sur échec SMS (question générale, les ~20 points d'appel de §13 point 2) — taux de remplissage mesurés :**

| Population | Ont un e-mail | Total | Taux |
|---|---|---|---|
| Boutiques (marchand) | 117 | 1 543 | **7,6 %** |
| Clients (acheteur, toutes commandes) | 75 | 389 | **19,3 %** |

**Ces taux limitent fortement la valeur d'un repli générique.** Pour la plupart des ~20 messages (alertes marchand notamment — stock bas, nouvelle commande, expiration d'essai/abonnement), un repli e-mail ne couvrirait que 7,6 % des cas où le SMS échoue. Coût du chantier lui-même : `lib/notifications/email.ts` a déjà 3 gabarits (`sendNewOrderAlertEmail`, `sendOrderConfirmationEmail`, `sendReviewRequestEmail`, tous via Resend, déjà en production) — mais seuls 3 des ~20 types de message ont un équivalent e-mail aujourd'hui. Un repli générique demanderait : (a) un gabarit e-mail par type de message manquant (≈17 à écrire), (b) une fonction d'orchestration commune (tenter le SMS, sur échec et si un e-mail existe, tenter l'e-mail) à brancher sur les ~20 points d'appel. **Chantier de plusieurs jours, pas une petite tâche, pour un gain plafonné à moins de 8 % côté marchand.** Pas recommandé tel quel — le rapport effort/couverture est mauvais pour la majorité des cas.

**3. Livraison numérique — cas différent, chiffré séparément, pour la Livraison 2 avant le tunnel de commande.** Ici l'e-mail s'ajoute au SMS, ne le remplace pas — les deux points d'appel sont déjà identifiés (`webhooks/bictorys/route.ts:531`, `webhooks/stripe/route.ts:154`) et un gabarit existant (`sendOrderConfirmationEmail`) est directement adaptable en `sendDigitalDownloadEmail`. **Le taux de remplissage e-mail des acheteurs de produits numériques est nettement meilleur que la moyenne générale : 12 sur 20 (60 %)**, mesuré sur les mêmes 20 commandes numériques payées depuis le 23 juin que le §14 point 2 — probablement parce qu'un paiement en ligne (Stripe en particulier) collecte plus souvent une adresse e-mail qu'une commande physique classique. **Chantier estimé petit** : un gabarit e-mail à écrire (clone du gabarit de confirmation existant), deux points d'appel à modifier pour ajouter l'envoi e-mail en parallèle du SMS (jamais à la place), condition sur `client_email` non vide — motif déjà en place ailleurs dans le code (`orders/route.ts:518`). Pas de nouvelle infrastructure : Resend est déjà configuré et utilisé en production. **Recommandé avant le tunnel de commande en Livraison 2**, comme demandé — c'est le chantier qui répare directement les 4 échecs sur 20 mesurés en §14 point 2, avec une couverture réelle (60 %, pas 7,6 %).

**4. Modification de `PricingV6.tsx` — appliquée, pas seulement préparée.** Le test étant revenu négatif (SMS accepté par l'API, non livré), la condition posée précédemment est remplie dans son sens défavorable. Les deux lignes déjà identifiées ont été retirées : `{ label: 'Notifications SMS automatiques' },` (liste de fonctionnalités du plan Business) et la ligne `Notifications SMS auto` du tableau comparatif. Rien d'autre modifié dans le fichier. À remettre en place le jour où un test réel (livraison confirmée sur un téléphone, pas seulement un statut « Envoyé ») valide le canal.

**Deuxième endroit oublié, corrigé le 2026-08-14 — même décision, appliquée avec retard.** `AFRICA_PLANS.business.features` (`src/lib/billing/plans.ts`, source réellement affichée par `/dashboard/upgrade`, `TrialEndScreen` Cas A et le checkout Stripe — `PricingV6.tsx` n'était pas encore en production au moment de cette note, voir §0/§11 ; en production depuis le 2026-08-17, §21) listait toujours `'Confirmations automatiques par SMS'` comme avantage Business, alors même que la ligne équivalente de `PricingV6.tsx` avait déjà été retirée la veille pour la même raison. Repéré en consolidant la liste de features de `StripeSubscriptionCheckoutForm.tsx` (copie codée en dur, elle-même déjà corrigée) vers cette source unique — la consolidation aurait sinon propagé l'affirmation fausse à un écran de paiement supplémentaire au lieu de la corriger. Ligne retirée. Même condition de remise en place que le point 4 ci-dessus : uniquement quand un test réel confirme que Lafricamobile délivre.

**Périmètre de la Livraison 1, mise à jour finale** : inchangé pour le reste. Sentry et la correction des crons aveugles restent en Livraison 2 avec le tunnel de commande — rien de nouveau ne les fait basculer en bloquant. Le chantier « e-mail en plus du SMS pour la livraison numérique » (point 3 ci-dessus) rejoint la Livraison 2, avant le tunnel de commande comme demandé. Le repli e-mail générique (point 2) n'est pas retenu en l'état — taux de remplissage marchand trop bas pour justifier le chantier tel que posé.

---

## 16. Le canal WhatsApp n'existe pas — audit de la cascade et note de cadrage — 2026-08-13

**Deux corrections de l'utilisateur au §15** : (1) le SMS n'est pas mort, mal paramétré — ticket ouvert chez Lafricamobile, cause probablement un nom d'expéditeur non approuvé, pas le crédit ; (2) la hiérarchie voulue par le produit est **WhatsApp d'abord, SMS en repli si WhatsApp échoue, e-mail en tout dernier recours** — l'e-mail n'a jamais été pensé comme un remplacement du SMS, mais comme le troisième maillon d'une cascade à trois niveaux. Le plafond de 7,6 %/19,3 % (§15 point 2) est confirmé correct — ce sont bien `shops.email`/`clients.email`, des champs distincts de l'e-mail synthétique `auth.users.email`.

**Audit de la cascade — elle n'existe pas, à aucun des trois niveaux annoncés.**

**1a. `sendWhatsApp` envoie-t-il par WhatsApp ?** Non. Recherche exhaustive de toute intégration à l'API Business de Meta (`graph.facebook.com`, jeton WhatsApp Business, WABA) dans `src/` : **aucune occurrence, nulle part**. Le seul résultat pour « meta » est `lib/meta/conversions-api.ts`, l'API de suivi publicitaire (événements Lead/CompleteRegistration pour Meta Ads), sans rapport avec l'envoi de messages. `src/lib/notifications/` ne contient que trois fichiers : `email.ts`, `lafricamobile.ts`, `whatsapp.ts` — et `whatsapp.ts` (déjà cité en §13) est un simple alias : `export const sendWhatsApp = sendSMS`, qui appelle `sendLafricamobileSms`. **Ce n'est pas récent : `sendWhatsApp` a toujours envoyé du SMS, y compris avant Lafricamobile** — le commit `727dd02` (5 juin) qui introduit ces notifications s'intitule lui-même *« notifications via SMS Twilio »*. Le nom `sendWhatsApp` n'a jamais correspondu à son contenu, depuis la toute première ligne de code.

**1b. Un enchaînement réel existe-t-il — WhatsApp, puis SMS, puis e-mail ?** Non, et il ne peut pas exister tel quel puisque le premier maillon n'est jamais parti par un canal distinct du second (les deux sont le même appel Lafricamobile). Recherche d'une fonction d'orchestration (`notifyClient`, `notifyShop`, `sendNotification` générique, tout wrapper de repli) dans `src/lib/` et `src/app/api/` : les seuls résultats pertinents sont `lib/actions/notifications.ts` (le système de notifications internes en cloche du tableau de bord — `shop_notifications`, sans rapport avec SMS/e-mail/WhatsApp) et `lib/push/send.ts` (notifications push web, un quatrième canal, jamais mêlé aux trois autres). **Aucune fonction, nulle part dans le dépôt, ne tente un canal puis un autre sur échec du premier.**

**1c. Sur les vingt sites d'appel, combien tentent plus d'un canal ?** **Zéro en cascade. Deux en parallèle inconditionnel** — à distinguer précisément, ce n'est pas la même chose. `api/orders/route.ts` et `api/webhooks/bictorys/route.ts` envoient, pour la confirmation de commande (acheteur) et l'alerte nouvelle commande (marchand), un SMS **et**, si une adresse est renseignée, un e-mail — mais les deux partent indépendamment, aucun n'est conditionné à l'échec de l'autre : l'e-mail part que le SMS ait réussi ou non, ce n'est pas un repli, c'est une redondance qui ne joue que si les deux champs sont remplis en même temps. Les 18 autres sites d'appel (rappels d'essai, alertes stock, réinitialisation PIN, etc.) ne tentent qu'un seul canal, sans aucun filet.

**Réponse directe à la question posée : la chaîne n'existe pas.** Il n'y a jamais eu trois niveaux dégradés — il y a un seul canal réel (SMS via Lafricamobile, faussement étiqueté « WhatsApp » dans le code depuis l'origine), et une poignée de cas où un second canal (e-mail) tourne en parallèle sans lien logique avec le premier. Tant que Lafricamobile n'est pas fiable, **les marchands n'ont aujourd'hui aucun canal de notification automatique qui fonctionne de façon garantie** — pas un canal dégradé qui retombe sur un autre.

---

**2. Le domaine `@beautydesk.app` — où il est écrit, ce que le changer casserait, sa visibilité. Réponse écrite, rien modifié.**

Écrit à deux endroits, en code dupliqué et non partagé : `src/lib/actions/auth.ts:16` (fonction `phoneToEmail`, utilisée par `signIn` ligne 37, `signUp` mort ligne 96, `requestPinReset` ligne 222, `confirmPinReset` ligne 290) et `src/app/start/actions.ts:14` (même nom de fonction, même logique, utilisée par `completeSignupFromStart` ligne 228). Un commentaire existe déjà au-dessus de la version d'`auth.ts` (ligne 13) : *« IMPORTANT: ce domaine est un identifiant interne Supabase — ne pas changer »* — quelqu'un avait déjà identifié le risque.

**Casserait-il la connexion des comptes existants ? Oui, pour la totalité d'entre eux, sans exception possible.** `phoneToEmail` est une fonction pure, déterministe : `signIn` reconstruit cette adresse à chaque connexion (ligne 37) et la compare à ce que Supabase Auth a en base. Changer le suffixe change ce que la fonction produit ; ce qui est stocké dans `auth.users.email` pour les comptes déjà créés ne change pas tout seul. Résultat : chaque tentative de connexion recalculerait une adresse qui ne correspond plus à rien, pour tous les comptes créés avant le changement — un changement de domaine sans migration de données équivaut à un verrouillage total de la base d'utilisateurs existante. Une migration corrigeant `auth.users.email` pour chaque compte concerné serait nécessaire en même temps, pas après.

**Visible par un utilisateur ou dans un export ? Rien trouvé, dans les endroits vérifiés — pas une garantie d'audit complet.** Aucun affichage de `user.email`/`profile.email` trouvé dans `src/app/dashboard/` ni `src/components/dashboard/`. L'export commandes (`api/export/orders/route.ts`, la fonctionnalité « Export Excel » du plan Pro) et la page admin des boutiques (`app/admin/shops/page.tsx`) ne contiennent aucune colonne `email`. Le seul champ e-mail affiché quelque part côté marchand est `shops.email` (le champ réel, optionnel, mesuré en §15 — `dashboard/upgrade/checkout/page.tsx:58-65`, pour préremplir Stripe), sans rapport avec l'adresse synthétique d'authentification. Recherche faite sur les pages et exports existants, pas sur les journaux serveur ni sur ce que Supabase Auth expose par ailleurs dans sa propre console d'administration (hors périmètre de ce dépôt).

---

**3. Note de cadrage — cascade de notification et collecte d'adresses. Aucun code, une base de décision.**

**Ce qui existe déjà vs. ce qu'il faut construire, niveau par niveau :**

| Niveau | État actuel | À construire |
|---|---|---|
| WhatsApp | N'existe pas — le nom trompe depuis l'origine | Ouvrir l'accès à l'API Business de Meta (démarche externe, délai hors du contrôle du code — l'utilisateur mentionne que ce n'est pas encore ouvert) ; puis un client d'envoi dédié, distinct de `lafricamobile.ts` |
| SMS | Existe, Lafricamobile, délivrabilité en cours de correction côté fournisseur | Rien côté code une fois le nom d'expéditeur approuvé — le transport fonctionne, seule la délivrabilité était en cause |
| E-mail | Existe (Resend), 3 gabarits sur ~9 types de message distincts | Gabarits manquants (fin d'essai, rappel J-3, livraison numérique — détail plus bas) + fonction d'orchestration commune pour que le repli soit réel, pas parallèle |
| Orchestration (le repli lui-même) | N'existe pas du tout, à aucun niveau | Une fonction unique par type de message : tenter le canal 1, sur échec (pas seulement absence du contact) tenter le canal 2, etc. — remplace les ~20 appels directs à `sendWhatsApp`/`sendSMS` dispersés dans le code |

**Où collecter l'e-mail.** L'intuition de l'utilisateur (pas pendant `/start`, à la première commande reçue) tient pour une raison de fond, pas seulement d'ergonomie : à la première commande, le marchand a une motivation concrète (« je veux être prévenu si mon SMS ne marche pas »), alors que pendant le quiz, l'e-mail serait un champ de plus sans justification immédiate — cohérent avec les 60 % mesurés en §15 chez les acheteurs de produits numériques via Stripe, où la motivation (recevoir son fichier) est immédiate et explicite. **Un autre moment mérite d'être considéré, pour une raison différente** : l'écran de fin d'essai (`TrialEndScreen`, cas A et B, `dashboard/page.tsx:142-185`) — le marchand y est déjà confronté à l'idée que son accès peut se couper, exactement le contexte où « laisse-moi une adresse pour ne rien perdre » a un sens immédiat, potentiellement plus fort que « ta première commande ». Les deux ne s'excluent pas.

**Taux de complétion actuel de `/start` — donnée inexistante au 2026-08-13, à dire clairement plutôt qu'approximer.** À cette date, `/start` v4 n'était pas encore en production : une seule boutique existait avec `trial_model='free_orders'` (« Nala Store », en `draft`) — un seul point, qui n'avait même pas terminé le parcours. Aucun taux de complétion ne pouvait être calculé avec une seule ligne. L'ancien `/start` (v3, en production sur `main` depuis le 16 juillet) ne pouvait pas servir de proxy : il crée le compte et la boutique en une seule action sans étape de « brouillon » préalable comme le fait v4. **`/start` v4 est en production depuis le 2026-08-17 (§21) — cette mesure redevient possible, non refaite depuis.**

**Récupérer les 1 426 marchands existants sans e-mail (1 543 − 117, §15).** Pas de campagne unique — l'inventaire prouve qu'un envoi en masse depuis l'admin ne laisse aucune trace de qui a été touché (§15 point 1), donc pas davantage une bonne idée de mesure que de méthode. Deux emplacements cohérents avec la logique « au moment où ça sert » plutôt qu'un rappel générique : le tableau de bord après une commande sans que l'e-mail soit renseigné (bandeau discret, fermable, pas une modale bloquante — à réutiliser du motif déjà validé pour les bannières d'essai/abonnement dans `dashboard/layout.tsx`) ; et l'écran de fin d'essai lui-même pour les marchands qui l'atteignent sans e-mail connu. Fréquence : une seule sollicitation par contexte, pas de répétition programmée — redemander à chaque visite serait le chemin le plus court vers l'ignorer complètement puis vers l'agacement, sans donnée pour arbitrer une cadence plus fine que « une fois, puis silence » à ce stade.

**Ce qui peut basculer sur e-mail une fois l'adresse connue :**

| Message | Gabarit Resend | État |
|---|---|---|
| Confirmation de commande (acheteur) | `sendOrderConfirmationEmail` | Existe, déjà branché (`orders/route.ts`, `webhooks/bictorys/route.ts` — **pas `webhooks/stripe/route.ts`, un trou distinct constaté ce soir**) |
| Alerte nouvelle commande (marchand) | `sendNewOrderAlertEmail` | Existe, déjà branché |
| Fin d'essai (legacy et free_orders) | — | À écrire |
| Rappel J-3 avant fin d'essai | — | À écrire |
| Livraison numérique | — | À écrire — déjà chiffré en §15 point 3, priorité Livraison 2 |

**Coût comparé — ordre de grandeur, pas une facture.** SMS Lafricamobile à 15 FCFA le message, 26,5 % de délivrabilité mesurée par le fournisseur lui-même → **57 FCFA par message réellement reçu** (calcul de l'utilisateur, confirmé : 15 / 0,265 ≈ 56,6). Resend : offre gratuite jusqu'à 3 000 e-mails/mois, palier payant ensuite de l'ordre de 20 USD pour 50 000 e-mails/mois (à confirmer sur leur page tarifs actuelle — **non vérifié depuis ce dépôt, connaissance générale seulement, pas une donnée mesurée comme le reste de cette note**) — soit, si ce palier s'applique, de l'ordre de 0,20 à 0,25 FCFA par e-mail envoyé, **sans même tenir compte d'un taux de délivrabilité** (Resend ne publie pas de taux comparable à celui de Lafricamobile dans les sources consultées ici). Écart brut, avant toute vérification tarifaire : plusieurs centaines de fois moins cher par message envoyé, et probablement du même ordre par message reçu si la délivrabilité e-mail via un expéditeur authentifié (SPF/DKIM, déjà en place vu que Resend est en production) reste proche de 100 % — ce dernier point n'est pas mesuré ici non plus, à vérifier avant de fonder une décision dessus.

---

## 17. Retour arrière `free_orders` → `legacy` — mode d'emploi, pas une note — 2026-08-13

**Ce que ce n'est pas : un interrupteur universel.** `UPDATE shops SET trial_model = 'legacy'` coupe correctement toute la logique conditionnelle (`isTrial`, `VisitBeacon`, écrans de fin d'essai, quota à la commande) — mais **abandonne les commandes déjà retenues**. `setShopStatus` (`lib/billing/shop-status.ts:40`), seul point qui sait les libérer, retourne immédiatement dès que `trial_model !== 'free_orders'`. Une commande retenue au moment de la bascule reste `is_held=true` pour toujours, même après paiement — `legacy` n'a jamais eu ce concept, aucun code ne va la rechercher.

**Conséquence directe : sûr avant la première commande retenue, dangereux après.** Vérifier systématiquement avant toute bascule, jamais l'inverse.

**Procédure, dans cet ordre, jamais l'étape 3 seule :**

```sql
-- 1. Y a-t-il des commandes retenues ? Si oui, LIBÉRER D'ABORD.
SELECT o.id, o.shop_id FROM orders o
JOIN shops s ON s.id = o.shop_id
WHERE o.is_held = true AND o.released_at IS NULL
  AND s.trial_model = 'free_orders';

-- 2. Libérer avant bascule
UPDATE orders SET is_held = false, released_at = now()
WHERE is_held = true AND released_at IS NULL
  AND shop_id IN (SELECT id FROM shops WHERE trial_model = 'free_orders');

-- 3. Alors seulement
UPDATE shops SET trial_model = 'legacy' WHERE trial_model = 'free_orders';
```

**Ce que l'étape 2 ne fait PAS** : elle ne débloque aucun fonds ni ne notifie personne — elle lève seulement le masquage (`is_held`/`released_at`), pour que la commande redevienne une commande normale, visible, dans une boutique qui va devenir `legacy`. Si la commande portait un paiement en ligne retenu (produit digital notamment, voir `ADDITIF-argent-commandes-retenues.md`), vérifier séparément que les fonds correspondants sont traités correctement pour ce cas précis avant de considérer la bascule terminée — non couvert par cette procédure, à traiter boutique par boutique si le cas se présente.

**Portée** : la requête `SELECT` de l'étape 1 cible spécifiquement les boutiques qu'on s'apprête à basculer (via la jointure sur `trial_model = 'free_orders'`) — à restreindre par `shop_id` si la bascule ne concerne qu'une boutique précise, jamais lancer l'étape 2 sur toutes les boutiques `free_orders` si l'intention est d'en revenir une seule.

**Garde-fou ajouté dans le code** (`lib/billing/shop-status.ts`) : si `setShopStatus` est appelé sur une boutique `trial_model !== 'free_orders'` qui a encore des commandes retenues, l'anomalie est remontée dans Sentry au lieu de disparaître dans un retour silencieux — ne répare rien, rend le cas visible s'il se produit malgré la procédure.

---

## 18. Séquence de fusion complète — préparée le 2026-08-13, étapes 1 et 2 exécutées ce jour-là, étapes 3 et 4 dépassées par les événements (voir §21)

**Statut au 2026-08-17 : cette séquence ne décrit pas le merge qui a finalement mis la Livraison 1 en production.** Étapes 1 et 2 ci-dessous ont bien eu lieu le 2026-08-13, mais `refonte-start` a ensuite continué à recevoir des semaines de travail supplémentaire (§4.24 à §4.34 : commission différenciée, frais de retrait, auth country managers, pages pays, etc.) sans que `dev`/`main` ne suivent, et `main` a reçu au moins un changement hors de cette séquence. L'étape 3 (recette sur `dev`) et l'étape 4 (`dev` → `main`) décrites ici n'ont donc jamais eu lieu telles qu'écrites. **Le merge qui a réellement mis tout ce travail en production a eu lieu le 2026-08-17, par un chemin différent (`refonte-start` → `main` directement, puis `main` → `dev`) — détaillé en §21.** Section conservée intégralement ci-dessous comme trace de la méthode (simulation `git merge-tree`, points de non-retour, etc.), réutilisée telle quelle pour §21.

Quatre étapes, dans cet ordre, conformes au §0. Chacune vérifiée par une simulation en lecture seule (`git merge-tree`, ne modifie rien) avant d'être écrite ici — pas devinée.

**État constaté avant de commencer** (`git fetch`, puis comparaison des trois branches) :
- `main` : 9 commits que `refonte-start` n'a pas (dont tout le travail migrations `066`-`091`, PR #4, etc.).
- `refonte-start` : 29 commits que `dev` n'a pas.
- `dev` : **aucun commit que `refonte-start` n'a pas** — son unique commit propre (`3fdad82`, 31 juillet, "retrait des revenus abonnements") est exactement le point d'où `refonte-start` est parti. `dev` n'a donc rien d'unique à préserver ; il est simplement resté immobile pendant que `refonte-start` avançait. C'est pour ça qu'il est encore à la migration `065` — il n'a jamais reçu les commits qui apportaient `066` à `091`, pas parce qu'il les a explicitement évités.

### Étape 1 — `main` → `refonte-start`

```
git checkout refonte-start
git pull origin refonte-start
git merge origin/main
```

**Conflits attendus, vérifiés par simulation (`git merge-tree --write-tree --messages origin/refonte-start origin/main`) — exactement deux, rien d'autre** : `AI_RULES.md` et `REPRISE.md`, tous les deux en `add/add` (les deux branches ont "ajouté" ces fichiers indépendamment, sans ancêtre commun, depuis qu'ils étaient gitignorés). **Aucun conflit sur `supabase/migrations/` ni sur aucun fichier de code** — vérifié par la même simulation, pas supposé.

**Résolution des deux conflits — vérifiée sûre, pas juste préférée** : `main` n'a pas touché `AI_RULES.md` depuis `5adff5d` (10 août) ni `REPRISE.md` depuis `23528ab` (10 août) — les deux datent d'avant le début de cette session. `refonte-start` contient 583 insertions cumulées sur ces deux fichiers depuis, 12 suppressions seulement (des états mis à jour, pas du contenu perdu). La version de `refonte-start` est un sur-ensemble strict — garder la sienne entièrement est sûr :
```
git checkout --ours AI_RULES.md REPRISE.md
git add AI_RULES.md REPRISE.md
git commit
```

**Vérifier avant de continuer** : `npm run build` passe ; `git status` propre ; `grep -rn "<<<<<<<" .` ne trouve rien (sécurité en plus de la simulation, au cas où l'état réel aurait divergé de la simulation entre-temps).

**Point de non-retour de cette étape : le `git push`.** Avant le push, tout est local — `git merge --abort` pendant le conflit, ou `git reset --hard origin/refonte-start` après un commit non poussé, annulent proprement. Après `git push origin refonte-start`, la branche partagée bouge et Vercel redéploie la préversion associée — pas la production, encore réversible, mais plus un simple `reset` local.

### Étape 2 — `refonte-start` → `dev`

```
git checkout dev
git pull origin dev
git merge origin/refonte-start
```

**Résultat attendu : fast-forward propre, aucun conflit.** `dev` n'ayant aucun commit unique (voir plus haut), et `refonte-start` contenant déjà tout ce que `dev` a une fois l'étape 1 poussée, ce merge avance simplement le pointeur de `dev` — pas de commit de fusion à créer, pas de résolution. **Si git ne propose pas un fast-forward à ce moment (message autre que « Fast-forward »), c'est le signal qu'un commit a été fait directement sur `dev` entre la vérification ci-dessus et l'exécution — s'arrêter et vérifier avant de continuer, ne pas forcer.**

**Vérifier avant de continuer** : `npm run build` passe sur `dev` ; la préversion Vercel de `dev` se redéploie sans erreur.

**Point de non-retour : le `git push origin dev`.** Mêmes conséquences que l'étape 1 — préversion, pas production.

### Étape 3 — Recette manuelle

Sur la préversion de `dev`, la liste complète du §0 (captures de référence en main avant de commencer — c'est la condition posée pour lancer l'étape 1 elle-même, pas seulement celle-ci) plus les ajouts du 2026-08-13 sur les commandes retenues. **Aucune commande git — étape humaine.** Si un point échoue : correctif sur `refonte-start`, refaire l'étape 1 et 2 pour ce correctif précis, recommencer la recette sur ce point. Ne jamais corriger directement sur `dev`.

**Point de non-retour : aucun — c'est la vérification elle-même. Tant qu'elle n'est pas déclarée complète, l'étape 4 n'a pas lieu.**

### Étape 4 — `dev` → `main`

```
git checkout main
git pull origin main
git merge origin/dev
```

**Avant d'exécuter, revérifier — ne pas se fier à l'état constaté aujourd'hui** : `git log origin/main` peut avoir bougé entre maintenant et le jour de cette étape (un hotfix direct sur `main`, par exemple). Refaire `git fetch` et si besoin une nouvelle simulation `git merge-tree` juste avant, pas supposer que la simulation de ce soir est encore valable.

**Résultat attendu si rien n'a bougé sur `main` entre-temps : fast-forward propre**, `main` ayant déjà reçu tout son contenu propre via le commit de fusion de l'étape 1 (`main` devient un ancêtre de `dev` à ce stade). Si `main` a reçu un commit direct depuis, un vrai merge (pas fast-forward) sera nécessaire — revérifier les conflits avant de continuer.

**Point de non-retour réel de toute la séquence : le `git push origin main`.** C'est le seul des quatre qui déclenche un déploiement de production — les 1 543 boutiques existantes, `tekki.shop`, tout ce qui est en ligne aujourd'hui. Après ce push, un retour arrière demande un nouveau déploiement (revert commit ou redéploiement de la révision précédente depuis Vercel), pas un simple `reset` local — coûteux en attention même si techniquement rapide. Les trois étapes précédentes ne touchent que des préversions ; celle-ci seule est la bascule réelle.

**Étape 1 exécutée le 2026-08-13, poussée** (`eaea8a2`). Simulée avec `git merge-tree` avant exécution : exactement 2 conflits (`AI_RULES.md`, `REPRISE.md`, add/add), 0 sur le code ou les migrations — confirmé à l'exécution. Résolution `--ours` sur les deux fichiers, vérifiée par `git diff origin/main -- AI_RULES.md REPRISE.md` : 0 ligne perdue sur `AI_RULES.md`, et sur `REPRISE.md` les seules lignes disparues sont soit l'horodatage, soit des versions antérieures déjà remplacées par une version plus complète côté `refonte-start` — pas de perte de fait. `npm run build` propre avant et après. Apporté par `main` au passage : PR #4 (`088`/`089`), les `.md` précédemment gitignorés (`NOTE_MASQUAGE_COMMANDES.md` compris — renvoi ajouté vers `redact.ts` dans `0aa4c51`), `scripts/check-function-privileges.sql`, les maquettes HTML.

**Étape 2 exécutée le 2026-08-13, poussée** (`3fdad82..b8cde8d`). Fast-forward exact, sans conflit ni commit de fusion, comme prévu — `dev` n'avait aucun commit propre à préserver. `npm run build` propre après. `dev` et `refonte-start` pointent désormais sur le même commit (`b8cde8d`). ~~**Prochaine étape : étape 3, recette manuelle sur la préversion `dev`**~~ — voir §19 pour les deux points vérifiés avant de la lancer (base de données partagée, test des comportements dépendant d'un cron). **Dépassé : `refonte-start` a continué d'avancer après ce point sans que cette recette n'ait jamais eu lieu sur cet état précis — voir la note en tête de §18 et §21 pour ce qui s'est réellement passé.**

---

## 19. Recette sur préversion — base partagée et crons non enregistrés — 2026-08-13

Deux questions posées avant le lancement de la recette (étape 3 du §18), parce qu'elles en conditionnent la faisabilité.

**1. Sur quelle base de données pointe la préversion `dev` ?**

**Un seul projet Supabase lié à ce dépôt** (`fvkamhditdyvadljjrvn`, « ministore » — confirmé par `supabase projects list`, §1/§18), aucune trace nulle part dans ce dépôt d'un usage de Supabase Branching (qui apparaîtrait comme un projet distinct) ni d'une seconde base. **Il est donc extrêmement probable que la préversion pointe sur la même base que la production** — mais **non vérifiable depuis cette session** : le CLI Vercel local n'est pas authentifié (`vercel whoami` → `Not authorized`, `.vercel/` présent mais non lié), impossible d'interroger directement le tableau de bord des variables d'environnement par branche. **À vérifier par l'utilisateur directement dans Vercel → Settings → Environment Variables** : si `NEXT_PUBLIC_SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` ne portent pas de restriction d'environnement (case « Preview » cochée avec la même valeur que « Production », ou aucune restriction du tout — le comportement par défaut à l'ajout d'une variable), c'est confirmé.

**Résolu le 2026-08-14 — clés Bictorys/Stripe séparées entre Preview et Production dans Vercel, valeurs Preview confirmées sandbox/test.** Le paiement réel du 2026-08-13 sur `d89a50b2…` (§4 point 16) était antérieur à cette séparation, cause identifiée, fonds récupérés manuellement via le dashboard Bictorys. La préversion `dev` ne peut plus déclencher de débit réel — question close, plus de conditionnel à vérifier avant une étape de recette impliquant un paiement.

**Rappel à garder en tête, au-delà de cet incident, non investigué maintenant** : une clé sandbox/test Bictorys n'est pas une garantie parfaite d'équivalence avec la production — elle peut passer par un environnement séparé côté fournisseur, capable de masquer un comportement qui ne se verrait qu'en conditions réelles. Si un comportement de paiement diffère entre la préversion et la production après le déploiement, ne pas exclure d'emblée cette piste.

**Requête de nettoyage des boutiques de test — préparée, non exécutée** : `scripts/cleanup-recette-test-shops.sql`. `SELECT` d'abord (filtré sur `trial_model='free_orders'` et une date à ajuster au début réel de la recette — cette population n'est atteignable que via la préversion `dev` tant que `main` n'a pas le nouveau `/start`, donc le filtre par date seul est déjà raisonnablement sûr), relecture boutique par boutique, puis `DELETE` filtré explicitement par `id`, jamais par la seule condition de date. **Cascade vérifiée avant d'écrire le fichier** (`information_schema`, pas supposée) : `orders`, `products`, `clients`, `payments`, etc. suivent la boutique en `ON DELETE CASCADE` — rien à nettoyer à la main. **Ce qui ne suit pas** : `profiles.shop_id` passe à `NULL` (`ON DELETE SET NULL`), le compte `auth` du marchand de test survit détaché — réutiliser le même numéro de téléphone pour un nouveau passage de recette échouera tant que ce compte n'est pas supprimé séparément (API Admin Supabase Auth, pas du SQL).

**2. Comment tester un comportement dépendant d'un cron, sans cron enregistré sur la préversion ?**

Le code de la route existe et répond normalement sur la préversion (Vercel n'empêche pas d'appeler `/api/cron/...` manuellement — seul l'enregistrement automatique par le planificateur ne se fait qu'au déploiement de production). **Mais les deux crons cités interrogent toute la base, pas une boutique précise** — vérifié en lisant leur code, pas supposé :

- `free-orders-trial-expiry` : `WHERE trial_model='free_orders' AND status='trial' AND trial_ends_at < now()` — sans filtre par boutique.
- `notify-held-orders` : `WHERE is_held=true AND held_notified_at IS NULL AND released_at IS NULL AND created_at < now()-48h` — même chose.

**Deux objectifs de test distincts, deux méthodes différentes — pas une seule réponse aux deux options envisagées :**

- **Voir l'écran de fin d'essai (Cas A/B)** : n'a pas besoin du cron du tout. `TrialEndScreen` (`dashboard/page.tsx:142`) se déclenche sur `shop.status === 'expired'`, rien d'autre — `trial_ends_at` n'est lu par cet écran que pour l'affichage, pas pour la condition. **Le plus sûr : `UPDATE shops SET status='expired' WHERE id='<id boutique de test>'`**, en pilotant `free_orders_used`/`free_orders_quota` pour choisir Cas A (`used >= quota`) ou Cas B (`used < quota`). Chirurgical, ne touche que la boutique de test, ne déclenche aucun effet de bord (pas d'événement `shop_events`, pas de tentative SMS).
- **Tester le cron lui-même (sa requête, la transition, l'événement, la notification)** : nécessite d'appeler la route réelle — mais **d'abord relancer sa `SELECT` en lecture seule pour voir tout ce qu'elle toucherait**, avant d'appeler l'endpoint, puisqu'il traite tout ce qui correspond, pas seulement la boutique de test. Avec la population actuelle (quasi nulle), le risque est faible aujourd'hui, mais la vérification prend une requête et évite de traiter une vraie boutique par accident si une autre a atteint son terme entre-temps.
  ```
  curl -H "Authorization: Bearer $CRON_SECRET" https://<url-de-la-préversion-dev>/api/cron/free-orders-trial-expiry
  curl -H "Authorization: Bearer $CRON_SECRET" https://<url-de-la-préversion-dev>/api/cron/notify-held-orders
  ```
  `CRON_SECRET` est la même variable que celle déjà en production (§15) — si la réponse à la question 1 est oui, elle s'applique aussi à la préversion sans configuration séparée. URL de la préversion à récupérer dans la liste des déploiements Vercel pour la branche `dev`, pas devinable depuis ce dépôt.

**Pour `notify-held-orders` précisément : `notified` affichera 0 quel que soit le bon fonctionnement du reste**, tant que Lafricamobile n'est pas réparé (§13-§16) — `sendWhatsApp` échoue avant que `held_notified_at` ne soit posé (ligne 51-53 du fichier, `if (!success) continue`). Ne pas lire un `notified: 0` comme un échec de recette sur ce point précis — seulement `checked` (le nombre de commandes retenues correctement identifiées) mesure la partie testable aujourd'hui.

---

## 20. Écrans de fin d'essai `free_orders` — les trois variantes exactes, constaté en recette le 2026-08-14

Condition commune à tout écran (`dashboard/page.tsx:143`) : `isFreeOrders && shop?.status === 'expired'`. Rien ne s'affiche en dehors de cette condition — pas de quatrième variante possible.

**Cas A — quota atteint** (`dashboard/page.tsx:144-145`) : `shop.free_orders_used >= shop.free_orders_quota`. Écran : cumul collecté, commandes retenues en attente, choix de plan.

**Cas B — essai expiré par la date, quota non atteint** (`dashboard/page.tsx:161`, le `else` de la condition ci-dessus) : calcule `visitCount` depuis `shop_visits` (filtré depuis `trial_started_at`) et le transmet à `TrialEndScreen`. **`TrialEndScreen.tsx` se sous-divise lui-même en deux variantes, selon une condition qui n'est pas dans `dashboard/page.tsx`** (ligne 133, `const neverOpened = visitCount === 0`) :
- **`visitCount === 0`** (variante vue en recette le 2026-08-14) : *« Ta boutique est prête, mais personne ne l'a encore vue. »*
- **`visitCount > 0`** (le troisième sous-cas demandé — boutique avec des visites mais zéro vente) : *« Ta boutique est prête. Il lui manque des visiteurs. »*, avec le nombre exact de visites affiché.

Les deux variantes de Cas B partagent le même bloc Assistant IA et le même bloc de prolongation (`TrialEndScreen.tsx:169-215`) — seul le titre/sous-titre change.

**Déclencheur réel de la prolongation (« Partager ma boutique »), tracé dans `TrialEndScreen.tsx:143-152` (`handleShareAndExtend`) et `lib/actions/trial.ts` (`extendTrialByShare`) :** le clic déclenche les deux actions en parallèle, sans dépendance entre elles — `window.open` vers un lien `wa.me` préempli (fenêtre WhatsApp cliente, aucune confirmation d'envoi possible côté serveur) et l'appel serveur `extendTrialByShare()` en même temps, via `startTransition`. **La prolongation de 7 jours est posée au clic sur le bouton, jamais à l'ouverture effective du lien par qui que ce soit.** `extendTrialByShare` (trial.ts:36-42) vérifie seulement `trial_model='free_orders'`, `status='expired'`, quota non atteint, et `trial_extended_at` encore `NULL` — aucune vérification de partage réel. Un marchand qui clique puis ferme l'onglet WhatsApp sans rien envoyer obtient quand même ses 7 jours.

**Décision de l'utilisateur, 2026-08-14 : choix assumé, pas une lacune — ne rien coder ici.** Aucun signal serveur fiable n'existe via un lien `wa.me` (WhatsApp ne notifie jamais TEKKIShop qu'un message a été envoyé ou ouvert), et le coût d'un faux « partage » est jugé faible — au pire un essai prolongé de 7 jours sans partage réel, pas une perte pour la plateforme ni pour un tiers. Rien à construire tant que ce coût reste faible.

**Lien "Tu peux aussi activer ta boutique dès maintenant"** (`TrialEndScreen.tsx:209-215`) : `href="/dashboard/upgrade"`, confirmé. La boutique est déjà publique à ce stade (`is_active=true`, `status='expired'` reste visible dans ce modèle — §12) : "activer" ne peut donc désigner ici que le passage à un plan payant, jamais la visibilité, qui n'est pas en jeu sur cet écran.

---

## 21. Fusion réelle `refonte-start` → `main` → `dev` — exécutée et en production, 2026-08-17

**C'est cette section, pas le §18, qui décrit le merge ayant effectivement mis la Livraison 1 (et tout ce qui s'est accumulé depuis, §4.24-§4.34) en production.** Le §18 (2026-08-13) avait préparé et exécuté ses étapes 1/2 sur un état de branches bien plus ancien ; entre le 13 et le 17 août, `refonte-start` a continué de recevoir des semaines de travail (pages pays Mali/Bénin/Burkina Faso, commission différenciée par pays, frais de retrait facturés au marchand, authentification séparée des country managers, correctifs `/dashboard/upgrade`), tandis que `dev` restait figé et que `main` recevait au moins un changement hors de cette séquence. Le merge du 17 août est reparti de cet état constaté ce jour-là, pas de celui du 13.

**Ordre exécuté, sur demande explicite de l'utilisateur de revenir au plan initial du §18 (sync puis push production puis propagation) plutôt que « dev → main »** : `refonte-start` → `main` directement (pas via `dev`), puis `main` → `dev`.

**Vérifications avant** : état des trois branches revérifié par `git fetch` juste avant le premier push (pas supposé identique à une vérification plus ancienne) ; simulation `git merge-tree` en lecture seule confirmant l'absence de conflit sur le code et les migrations ; `npm run build` propre sur `refonte-start` avant le push.

**Étape 1 — `refonte-start` → `main`.** Poussé sur `origin/main`. **Point de non-retour de toute la séquence** — c'est ce push qui a déclenché le déploiement de production Vercel pour les boutiques existantes. Build Vercel vérifié propre après coup.

**Étape 2 — `main` → `dev`.** Fast-forward, `dev` synchronisé sur le même commit que `main`/`refonte-start`.

**Déploiement confirmé en ligne** par un contrôle direct sur `www.tekki.shop` après que le déploiement Vercel a fini de rouler (surveillé en tâche de fond via une boucle `curl` sur une route qui n'existe que dans le nouveau code) : `/cm/login`, `/mali`, `/licence` répondent tous `200` en production — confirmant que la landing v6, les pages pays, et l'authentification country manager sont bien live, pas seulement construites.

**Chantiers laissés ouverts, non résolus par ce merge — à ne pas considérer comme clos parce que le merge l'est :**
- **Webhook Bictorys (§4, points 27/28)** : reste ouvert, mais confirmé par l'utilisateur comme n'affectant que la recette sur préversion, pas un problème de production. Non bloquant, non résolu.
- **PITR (Point-In-Time Recovery) Supabase, palier 7 jours (~115 $/mois)** : approuvé en principe par l'utilisateur, **explicitement reporté** — pas activé. Nécessite une dernière confirmation de coût avant activation réelle, puisque c'est une dépense récurrente engagée.
- **Sauvegarde Storage vers Cloudflare R2** : workflow GitHub Actions écrit et poussé (`.github/workflows/storage-backup.yml`), mais ne peut pas encore tourner — 7 secrets GitHub à créer, sur indication explicite que l'utilisateur s'en charge lui-même de son côté.
- **Retrait manuel des country managers via Bictorys** : laissé en processus manuel, volume attendu trop faible (moins de 10 comptes) pour justifier l'automatisation maintenant.
- **`/cm/login` — flux d'invitation jamais testé de bout en bout avec un vrai compte réel.** Vérifié seulement visuellement (écran de connexion) et par lecture de code.
- **Fusion du contenu `PricingV6` ↔ `AFRICA_PLANS.features`** : les deux listes de fonctionnalités restent distinctes par décision explicite de l'utilisateur — non tranché, pas prévu pour cette session.
- **Second test de retrait en conditions réelles (taux de commission modifié, Bénin/Togo)** : non fait activement — l'utilisateur a choisi de surveiller le premier vrai retrait d'un marchand réel plutôt que de forcer un test, pour ce point précis.
- **Colonnes `seller_stage`/`selling_channel`/`pain_point` de `/start`** : écrites à la création de boutique, jamais relues nulle part dans le code — piste documentée pour une future personnalisation de l'Assistant IA, pas urgente (voir §4.34 pour le détail exact du flux Q3/Q5 → écran de révélation).

---

## 22. Bannière d'annonce homepage + désharmonisation du bleu sur `/start` — deux correctifs, 2026-08-17, poussés directement sur `main`

Deux correctifs mineurs, sans rapport l'un avec l'autre, traités ensemble après confirmation que la Livraison 1 est stable en production. Poussés directement sur `main` (pas de passage par `refonte-start`/`dev` au préalable), puis `dev` resynchronisé derrière (`main` → `dev`, fast-forward), sur décision explicite de l'utilisateur.

**1. Bannière d'annonce de la homepage — pointait vers `tekki.pro`.** `src/app/page.tsx`, seul endroit où elle existe : confirmé par recherche de la classe CSS `announcement-bar` sur tout `src/` — absente de `CountryLandingV6.tsx` (donc pas sur `/togo`/`/mali`/`/benin`/`/burkina-faso`) et de tout autre composant partagé, ce n'est pas un composant réutilisé comme `LandingNav`/`LandingFooter`, juste un bloc local à la homepage. Contenu remplacé : *« Tu proposes des services ? Découvre TEKKIPro → »* (lien externe vers `tekki.pro`) devient *« 3 premières commandes offertes »* + lien interne *« Je me lance → »* vers `/start`. Un premier essai de copie plus longue (*« Tes 3 premières commandes sont offertes »* / *« Je crée ma boutique → »*) débordait en dessous de ~400px de large (vérifié par capture d'écran headless à 320/375/430px) — raccourci, et `flex-wrap: wrap` ajouté à `.announcement-bar` (`landing-v6.css`) en filet de sécurité pour tout texte futur trop long sur très petit écran.

**2. Bleu de `/start` toujours désaccordé — cause trouvée : une deuxième source de couleur, jamais touchée par le correctif du 9 août.** Le correctif `c4f1ff9` (9 août) avait changé `--color-primary` (`globals.css`, la couleur du dashboard) de `#0EA5E9` à `#0252EA` — sa propre note de commit signalait déjà à l'époque deux autres bleus non touchés (`.landing-scope` à `#2E90FA`, et `landing-v6.css` à `#176bff`), « à confirmer si à aligner », jamais fait depuis. **Ce que la vérification de ce soir a établi, en plus** : `.landing-scope` (globals.css) n'est en réalité appliqué nulle part dans le code — `grep` exhaustif sur `className="landing-scope"` : zéro résultat. C'est du CSS mort, pas une source réellement active. La vraie deuxième source est ailleurs, jamais mentionnée dans la note du 9 août : `src/app/start/start.module.css` définit ses **propres** variables locales à `.wrap` (`--bl: #2E90FA`, `--bld: #175CD3`, `--blp: #EFF6FF`), écrites une seule fois à la création du fichier (commit `c66503c`, seul commit ayant jamais touché ce fichier — vérifié par `git log`) et jamais reliées à `--color-primary` ni à aucune variable globale. Le correctif du 9 août ne pouvait donc pas les atteindre, quel que soit son contenu — ce n'est pas un fichier oublié dans une liste, c'est un système de couleur entièrement autonome. **Décision de l'utilisateur, entre les deux bleus réellement actifs sur le site aujourd'hui : aligner `/start` sur celui de `landing-v6.css` (`#176bff`/`#0d54d8`), pas sur celui du dashboard (`#0252EA`)** — cohérent avec le fait que `/start` est atteint depuis la landing et garde un ton marketing/quiz. Appliqué : `--bl`/`--bld`/`--blp` de `start.module.css` alignés sur `--blue`/`--blue-2`/`--blue-3` de `landing-v6.css`, plus la valeur `rgba()` du `box-shadow` de `.btnBl` (utilisait `#2E90FA` en dur, recalculée pour `#176bff`). Vérifié visuellement par capture d'écran headless (`/start` et `/` côte à côte) : même teinte de bleu désormais.

**Vérifications avant push** : `npx tsc --noEmit` propre, `npm run build` propre (`rm -rf .next` avant, cache potentiellement corrompu sinon — pratique déjà établie cette session), captures d'écran headless Chrome à plusieurs largeurs pour confirmer visuellement les deux correctifs sans régression.

---

## 23. Nettoyage assets obsolètes + rattrapage suivi des maquettes de référence — 2026-08-17

Deux lots distincts, tranchés par l'utilisateur après audit des 11 fichiers non trackés restants (le troisième, `AGENTS.md`/`CLAUDE.md`/`IMPROVEMENTS.md`, reste intouché — non tranché, voir §1/§8).

**1. Suppression des 4 anciens assets — jamais suivis par git, confirmés non référencés.** `public/old-favicon.png` (853 708 octets, la source de l'ancien favicon 834 Kio remplacé — voir §4 point 2), `old-icone-tekkishop.svg`, `old-logo.svg`, `old-logo_white.svg`. **Untracked depuis avant cette session** (déjà `??` dans `git status` en tout début de session) — `git rm` ne s'applique qu'à des fichiers suivis, donc aucune opération git possible dessus : suppression par `rm` direct sur le disque, confirmée par `git status` (plus aucune trace, aucun diff produit puisque git ne les connaissait pas). Recherche exhaustive dans `src/` avant suppression : zéro référence à leur nom, cohérent avec le constat déjà fait le 9 août (remplacés par `public/favicon.png` et `src/app/icons/[size]/route.tsx`).

**2. Suivi des 7 maquettes HTML manquantes — rattrapage, pas une nouvelle production.** `tekkishop-commande-v2.html`, `europe-canada`, `licence`, `pourquoi-pas-shopify`, `produits-digitaux-landing-95`, `produits-digitaux`, `start-v4` — datées du 9-10 août (jamais touchées depuis, jamais créées ni modifiées par un agent cette session), même nature que les 5 déjà suivies (`tekkishop-boutique-accueil.html`, `boutique-produit`, `landing-v6`, `flow-start`, `nouvelle-landing`) : maquettes de référence ayant servi à construire les pages aujourd'hui en production (`/licence`, `/produits-digitaux`, `/europe-canada`, `/pourquoi-pas-shopify`). Contrairement à `AGENTS.md`/`CLAUDE.md`/`IMPROVEMENTS.md`, rien dans REPRISE.md ne les excluait explicitement — oubli du suivi initial du 10 août (§1), pas une décision. `git add` + commit séparé (`9897c4c`), diff vérifié avant push : 7 fichiers, `new file mode 100644` chacun, 4 635 insertions au total, **0 suppression, 0 fichier existant modifié** — confirmé fichier par fichier (`git show --stat` sur chacun), pas seulement sur l'agrégat.

**État final des branches après ce lot** : `main` = `dev` = `9897c4c` (`git rev-parse` sur les quatre — `main`, `dev`, `origin/main`, `origin/dev` — identiques). `refonte-start` reste à `e004e6c`, **4 commits derrière** (`9a072c1` audit REPRISE.md §21, `c77bda8` bannière/bleu §22, `d93d019` CGU/privacy, `9897c4c` ce lot) — non resynchronisé, non demandé.

---

## 24. Chantiers documentés, non commencés

**Refonte du dashboard marchand — spec déposée le 2026-08-17. Ne pas commencer.**

Fichiers : `SPEC-refonte-dashboard-marchand.md` (spec complète) et `tekkishop-dashboard-mockup.html` (maquette interactive de référence, mobile + desktop) — trackés dans ce commit, même politique que les autres specs de la racine (voir §1). **Aucun plan d'exécution rendu, aucune exploration du code du dashboard actuel effectuée** — sur consigne explicite, ce chantier attend son tour.

**Objectif** (résumé du §1 de la spec) : le dashboard actuel est un menu à plat de 10 entrées, construit fonctionnalité après fonctionnalité sans hiérarchie d'ensemble. La refonte vise une hiérarchie à 3 niveaux (quotidien / occasionnel / administratif), pensée pour un marchand qui consulte depuis son téléphone et n'a souvent jamais utilisé d'outil e-commerce. **Périmètre strict UI/UX** — la spec exclut explicitement toute logique métier, appel API, schéma de données ou calcul (commissions, statuts, soldes).

**9 lots (§13 de la spec), dans l'ordre prévu** :
1. Fondations (tokens + composants partagés)
2. Navigation (barre mobile basse + sidebar desktop groupée)
3. Accueil / Tableau de bord
4. Commandes
5. Produits
6. Revenus
7. Clients
8. Paramètres
9. Assistant IA

**Position dans la file d'attente des chantiers, trois chantiers dans cet ordre** :
1. Refonte du tunnel de commande (Livraison 2, `SPEC-refonte-tunnel-commande.md`, 4 lots A-D, aucun commencé — §4 point 0, priorité immédiate après la Livraison 1).
2. Lots 2 à 5 de la refonte des boutiques publiques (`SPEC-v2-refonte-boutiques-publiques.md` — §4 point 5).
3. **Refonte du dashboard marchand** (ce chantier) — après les deux précédents.

---

## 25. Fuite `resend-digital` — corrigée, 2026-08-17, hors calendrier du tunnel de commande

**Trouvée en préparant le plan du tunnel de commande (§26), traitée séparément et immédiatement — fuite d'argent réelle, pas de la dette technique.** `api/dashboard/orders/[id]/resend-digital/route.ts` (bouton marchand « Générer un nouveau lien et envoyer au client », créé le 13 juillet comme filet de secours pour les échecs SMS — voir §14) générait un vrai `download_token` en ne vérifiant que l'appartenance de la commande à la boutique et son statut non-retenu (`isOrderBlocked`) — **aucune vérification qu'un paiement en ligne avait réellement été encaissé.** Il posait aussi lui-même `status: 'completed'` comme effet de bord, sans condition.

**Ampleur mesurée avant correctif, une fois le projet Supabase lié dans cet environnement (`supabase link --project-ref fvkamhditdyvadljjrvn`, réussi sans mot de passe) :** exactement **2 commandes** en base concernées (`payment_type` hors `online_full`/`online_deposit`, contenant au moins un produit digital, avec au moins un `download_token` généré) :
- `8c485241…`, K.B.Y multi services, `on_site`, 2 000 FCFA, créée le 2026-07-19 — **5 tokens générés en 1h30 le même jour, 6 téléchargements réels au total**, commande aujourd'hui `cancelled`.
- `ff652b97…`, LolaShop, `on_delivery`, 3 000 FCFA, créée le 2026-08-06 — **4 tokens générés en 13 minutes, 1 téléchargement réel**, commande aujourd'hui `pending`.

**Correctif** : recherche exhaustive de tout endroit du code écrivant `orders.status = 'completed'` — exactement 3 : le webhook Bictorys (`webhooks/bictorys/route.ts:506`, après paiement confirmé), le webhook Stripe (`webhooks/stripe/route.ts:122`, idem), et `resend-digital` lui-même (le seul sans garde). Confirmé par ailleurs que `advanceOrderStatus` (l'action normale de progression marchand) ne peut structurellement jamais produire `'completed'` (`STATUS_TRANSITIONS`, `completed: null`, aucune transition n'y mène). `status === 'completed'` est donc un signal fiable de paiement en ligne réellement confirmé, jamais atteignable par une action marchand. Ajouté comme precondition avant toute génération de token ; retiré l'écriture inconditionnelle du statut, devenue inutile (le statut est déjà garanti `'completed'` par la precondition). Vérifié directement sur les deux commandes réelles ci-dessus : aucune des deux n'est `'completed'` — le correctif les aurait bloquées.

**Cas légitime préservé, vérifié** : une commande digitale payée en ligne dont l'envoi WhatsApp automatique échoue (la raison d'être originelle de ce bouton, §14) a déjà son statut à `'completed'` posé par le webhook avant même que le marchand ne clique — le correctif ne change rien pour ce cas.

**`npx tsc --noEmit` et `npm run build` propres. Diff isolé, un seul fichier, aucun changement UI nécessaire** (`DigitalDeliveryCard.tsx` affichait déjà `data.error` sur réponse non-`ok`, testé par lecture du composant).

**Vérification demandée le 2026-08-17 : `orders.status === 'completed'` compté sans condition sur `payment_type` dans le calcul de revenu marchand (`dashboard/page.tsx:102`, `api/dashboard/revenue/route.ts:74`) — confirmé comme comportement sûr, pas une fuite. Ne pas « corriger » ce point dans une session future sans relire ceci.** Après le correctif ci-dessus, `'completed'` n'est atteignable que par les deux webhooks de paiement (Bictorys, Stripe), toujours après confirmation réelle d'un paiement en ligne, jamais par une action marchand. L'absence de condition sur `payment_type` est donc sans effet : l'ensemble des commandes pouvant atteindre `'completed'` est déjà restreint aux commandes digitales réellement payées en ligne — il n'existe aucun chemin où une commande payée en espèces ou à la livraison devienne `'completed'`. **`status === 'delivered'`, dans le même calcul et lui aussi sans condition sur `payment_type`, est un cas différent et déjà documenté séparément** (ligne 451 de ce document, décision du 2026-08-10) : `advanceOrderStatus` ne vérifie aucun paiement, une commande à paiement en ligne jamais réellement encaissée peut donc être avancée jusqu'à `delivered` par le marchand et compter comme revenu — gap connu, accepté, non résolu, sans rapport avec le correctif `resend-digital` de ce soir.

---

## 26. Refonte du tunnel de commande — Lot A (structure), 2026-08-17

Premier des 4 lots de `SPEC-refonte-tunnel-commande.md` (§13 de la spec), plan complet validé avant tout code (état des lieux : spec lue en entier, code exploré, classification logique/présentation par lot, les 4 sujets hors périmètre — FCFA en dur, restitution stock, idempotence webhook, commission — vérifiés par ricochet, aucun touché par ce chantier sauf le FCFA du gabarit e-mail, réservé au lot D). **Aucun changement de logique de calcul dans ce lot — vérifié par un diff filtré avant commit, voir plus bas.**

**Fichiers touchés** : `commander/page.tsx`, `commander/OrderForm.tsx`. `commander/success/page.tsx` ajouté au périmètre de la refonte (décision explicite de l'utilisateur — c'est la page de confirmation visée par le §9 de la spec, absente à tort de la liste du §1) mais **pas encore modifiée** : ses défauts propres (recalcul local de `shop.primary_color`, incohérence tu/vous dans le même fichier) seront traités aux lots B/D selon ce qu'elle affiche.

**Ce qui a changé :**
- **`commander/page.tsx`** : 3 requêtes `shops` séparées regroupées en une seule (corrige 3.12) ; `--brand` consommé via la chaîne `'var(--brand)'` passée en prop plutôt qu'un recalcul local `shop.primary_color ?? '#0EA5E9'` — troisième occurrence du même anti-pattern documenté au §2, voir la note ajoutée à la suite de ce paragraphe pour ne pas fausser le compte des occurrences restantes ; `max-w-lg mx-auto` retiré pour laisser la place à la disposition large.
- **`OrderForm.tsx`** : stepper supprimé, quatre sections toujours visibles et sans numérotation (corrige 3.1) — Ce que tu commandes / Comment tu reçois ta commande (masquée entièrement si digital) / Où on te joint / Comment tu paies ; disposition deux colonnes ≥960px avec relevé collant à droite (formulaire seul en dessous) ; garde anti-ghost-click déplacée du changement d'étape (disparu) vers le montage du composant, 400 ms, même mécanisme ; notes devenues un bouton « Ajouter une précision » dépliable, hors sections (§5 de la spec) ; emojis retirés (🔒✅💬💳⚠️ et le ✓ devant « Code valide », même registre) ; « Satisfaction garantie » remplacé par les deux mentions factuelles du §10 (paiement traité par Bictorys, lien de suivi envoyé) ; bloc de récapitulatif dupliqué en milieu de formulaire (résidu de l'ancienne étape 2) supprimé, redondant avec la section 1 juste au-dessus dans un flux à défilement continu. Code promo laissé en place tel quel, son déplacement dans le relevé partagé étant explicitement le lot B.

**Vérification que la logique est intacte, pas seulement affirmée** : `git diff -- OrderForm.tsx` filtré sur les lignes touchant réseau/paiement/montants (`fetch`, `submit`, `payment_type`, `totalPrice`, `subtotal`, `deposit`, `api/orders`, `itemsSubtotal`, `promoAmount`, `total_price`) — aucune occurrence de `fetch`/`api/orders` dans le diff (la fonction `submitOrder` et son appel serveur n'ont pas bougé d'une ligne), les autres lignes trouvées ne sont que des repositionnements du même contenu vers `recapContent`/`ctaButton` (blocs réutilisés aux 3 endroits — formulaire mobile, barre collante, colonne desktop — au lieu d'être écrits une fois).

**Fausse alerte trouvée et corrigée en cours de route, signalée pour ce qu'elle est.** Les premiers tests avec l'outil de capture d'écran habituel (`chrome --headless --screenshot --window-size=430,...`) montraient un texte coupé sur toute la page à 430 px, de façon reproductible — pris pour un vrai débordement horizontal, des retouches ont été faites en réaction (texte de réassurance en `flex-wrap`, `max-w-lg` restauré sur la barre collante mobile, un doublon de bouton CTA introduit par erreur supprimé). **Vérifié ensuite au protocole DevTools directement (`Emulation.setDeviceMetricsOverride` + `Runtime.evaluate` sur `document.documentElement.scrollWidth`), pas via le flag CLI** : `scrollWidth === clientWidth === 430`, **aucun débordement réel**. Le flag `--window-size` de l'outil de capture produisait une mesure fausse ce soir-là (cause non identifiée — environnement Chrome headless instable, plusieurs lancements ont aussi crashé avec des erreurs `CVDisplayLinkCreateWithCGDisplay`/`NSCGSTransaction`), pas le code. Les retouches faites en chassant ce faux problème restent (le retrait du doublon de bouton est une vraie amélioration ; les deux autres sont des filets de sécurité raisonnables, sans effet démontré) — mais aucune n'a corrigé de bug réel de débordement, il n'y en avait pas. **Pour toute session future qui utiliserait le même outil de capture (`chrome --headless --window-size=...`) et verrait un débordement horizontal suspect sur cette page ou une autre : vérifier par le protocole DevTools avant de conclure à un vrai bug** — la méthode qui a mis fin à cette fausse alerte est documentée ci-dessus, à réutiliser telle quelle.

**Vérifié pour de vrai, deux fois** : mesure `scrollWidth`/`clientWidth` (430 px et 1280 px, zéro débordement aux deux) et capture d'écran via le protocole DevTools (`Page.captureScreenshot`, fiable contrairement au flag CLI) — mobile et desktop tous deux conformes visuellement à la disposition attendue. `npx tsc --noEmit` et `npm run build` propres après chaque changement.

**Prochaine étape** : lot B (le relevé — composant partagé, décomposition complète, code promo déplacé avec retrait possible, remise sur quantité affichée). Lots C et D restent en attente de leurs plans spécifiques (migration `paymentType`, traitement `delivery_type` pour panier tout-digital) à proposer séparément avant code, comme convenu.

---

## 27. Refonte du tunnel de commande — Lot B (le relevé), 2026-08-17/18

Deuxième des 4 lots (§13 de la spec). Composant partagé créé et câblé sur les 3 surfaces React du tunnel (tunnel lui-même, confirmation, suivi) — l'e-mail (4ᵉ surface, HTML statique, ne peut pas consommer un composant React) est explicitement **reporté au lot D**, sur décision explicite : son propre chantier (FCFA en dur, emojis, ligne de livraison digitale, tutoiement) touche exactement les mêmes lignes que la décomposition, pas de sens à les toucher deux fois séparément.

**Nouveau fichier** : `src/components/shop/OrderSummary.tsx` — hors de toute arborescence de route, réutilisé par `commander/` (tunnel + confirmation, imbriquée dessous) et `commande/[token]/` (suivi, arborescence distincte). Affiche sous-total, remise quantité (si fournie), réduction promo (avec retrait — corrige 3.9), livraison, total, puis un bloc distinct « Tu paies maintenant » / « Puis X à la livraison » (ou « En espèces à la livraison » quand rien n'est dû en ligne — §6 de la spec). Mode interactif (saisie/retrait du code promo) uniquement si `onApplyPromo`/`onRemovePromo` sont fournis — le tunnel seul ; confirmation et suivi passent des données déjà persistées, affichage pur.

**Point tranché avant le code, pas deviné : la ligne « remise sur quantité » n'existe que dans le tunnel.** `order_items` ne persiste que le prix déjà remisé (`unit_price`) et le pourcentage (`quantity_discount_pct`), jamais le montant économisé ni le prix d'origine — confirmation, suivi et (plus tard) e-mail ne peuvent donc pas la reconstituer exactement. Trois options soumises à l'utilisateur : migration pour persister le montant à la création (la bonne solution), reconstruction approximative depuis le pourcentage (risque d'afficher un chiffre inexact sur un écran qui parle d'argent, écarté), ou line absente sur les 3 surfaces concernées avec sous-total/total qui restent exacts (**choisi**). **Ce n'est pas un renoncement à l'exactitude, c'est un report explicite** : la migration (nouvelle colonne `quantity_discount_amount` sur `order_items`, remplie par `api/orders/route.ts` au moment du calcul déjà exact côté serveur) reste à faire, en **chantier séparé, avec son propre plan à valider avant tout code** — elle ne fait pas partie du calendrier des lots A-D et ne doit pas y être rattachée par erreur dans une session future.

**Fichiers touchés** :
- **`OrderForm.tsx`** : `recapContent` reconstruit sur `OrderSummary` au lieu du bloc ad hoc du lot A ; ajout de `rawSubtotal` (sous-total plein, avant remise quantité) et `qtyDiscountAmount` (= `rawSubtotal - itemsSubtotal`) comme valeurs d'affichage dérivées — **`itemsSubtotal`, `promoAmount`, `total`, `deposit` restent calculés exactement comme avant, aucune formule financière modifiée**, uniquement de nouvelles valeurs dérivées pour l'affichage ; ajout de `amountNow`/`amountLater`, dérivés des mêmes `total`/`deposit`/`paymentType`/`isDigital` sans nouveau calcul ; section « Code promo » autonome supprimée (son contenu vit désormais dans le relevé) ; aperçu « Sous-total » redondant sous la liste d'articles supprimé (le relevé, toujours visible via la barre collante mobile, le remplace).
- **`commander/success/page.tsx`** : requête `orders` enrichie (`delivery_price`, `delivery_zone_name`, `promo_code`, `promo_discount_pct`, `discount_amount`) ; bloc « Paiement » remplacé par `OrderSummary` ; ligne « Total » redondante retirée du bloc Articles. **Corrections demandées par l'utilisateur pendant qu'on est dans ce fichier, hors périmètre strict du lot B** : recalcul local `shop?.primary_color ?? '#0EA5E9'` remplacé par `'var(--brand)'` (troisième occurrence de l'anti-pattern, voir §2) — y compris le cas `${color}20` (astuce alpha hexadécimale, incompatible avec une chaîne `var(...)`, remplacé par `color-mix(in srgb, var(--brand) 12%, white)`) ; incohérence tu/vous corrigée (« Votre paiement... » → « Ton paiement... », « Vous aimerez aussi » → « Tu aimeras aussi »). **Emojis (✓, 📦) volontairement non touchés ici** : pas demandés pour ce passage, laissés pour le lot D avec le reste du retrait d'emojis.
- **`commande/[token]/page.tsx`** : requête enrichie de la même façon (+ `deposit_amount`) ; bloc « Paiement » (qui n'affichait qu'un libellé + le total, sans décomposition — 3.4) remplacé par `OrderSummary` ; `PAYMENT_LABELS`, devenue inutilisée, supprimée. **Aucune autre modification** — couleur et tutoiement restent explicitement au lot D pour ce fichier (déjà dans le périmètre déclaré de la spec depuis le début, contrairement à `success/page.tsx` qui a rejoint le périmètre en cours de route).

**Vérifié, pas seulement compilé** : `npx tsc --noEmit`/`npm run build` propres. Rendu visuel via le protocole DevTools (méthode validée au lot A) : tunnel mobile (430 px) et desktop (1280 px), interaction réelle du lien « J'ai un code promo » (clic simulé, le champ de saisie apparaît bien). Page de suivi testée avec une **vraie commande de production** (Jwu E-Tik, `payment_type='on_delivery'`, 230 000 FCFA) — décomposition correcte, « En espèces à la livraison » affiché comme attendu, zéro erreur JavaScript en console (vérifié via `Runtime.enable`/`Runtime.exceptionThrown`, pas juste l'absence de crash visible).

**Prochaine étape** : lots C (modes de paiement — migration `paymentType` à proposer séparément avant code) et D (sorties — email, tutoiement/emojis restants sur `commande/[token]/page.tsx`, e-mail de confirmation avec sa propre décomposition HTML statique).
